/* Додаткові завдання · Модуль 05 — Node.js/Express */
(function () {
  'use strict';
  var EXT = {
    '05-01': [
      { level:'medium', uk:'Напиши скрипт Node.js, що рекурсивно читає всі файли з розширенням .js у заданій директорії (з аргументу командного рядка process.argv[2]) через модуль fs/promises. Для кожного файлу виводь відносний шлях і кількість рядків. Обробляй помилки при доступі до директорії та окремих файлів.', ru:'Напиши скрипт Node.js, рекурсивно читающий все .js файлы в указанной директории (из аргумента командной строки process.argv[2]) через модуль fs/promises. Для каждого файла выводи относительный путь и количество строк. Обрабатывай ошибки при доступе к директории и отдельным файлам.' },
      { level:'hard',   uk:'Побудуй HTTP-сервер лише на вбудованому модулі http без Express: маршрути GET /users (список), GET /users/:id (за ідентифікатором), POST /users (створення). Вручну розбирай URL і тіло запиту через потоки (data/end events). Зберігай дані у Map. Повертай JSON і правильні HTTP-статуси.', ru:'Создай HTTP-сервер только на встроенном модуле http без Express: маршруты GET /users (список), GET /users/:id (по идентификатору), POST /users (создание). Вручную разбирай URL и тело запроса через потоки (data/end events). Храни данные в Map. Возвращай JSON и правильные HTTP-статусы.' },
      { level:'hard',   uk:'Реалізуй систему публікації/підписки на основі EventEmitter: клас PubSub із методами subscribe(event, handler), publish(event, data) і unsubscribe(event, handler). Продемонструй роботу: 3 обробники підписані на одну подію, одна публікація, потім відписка одного обробника і повторна публікація — лише 2 обробники спрацьовують.', ru:'Реализуй систему публикации/подписки на основе EventEmitter: класс PubSub с методами subscribe(event, handler), publish(event, data) и unsubscribe(event, handler). Продемонстрируй работу: 3 обработчика подписаны на одно событие, одна публикация, затем отписка одного обработчика и повторная публикация — только 2 обработчика срабатывают.' },
      { level:'extra',  uk:'Створи CLI-утиліту watch-and-compile: стежить за директорією src/ через fs.watch із рекурсивним режимом. При збереженні .md-файлу компілює Markdown у HTML через пакет marked, зберігає результат у dist/ із тим самим іменем файлу. Підтримуй аргументи --src, --dist і --watch. Обробляй швидкі послідовні зміни (debounce 300 мс).', ru:'Создай CLI-утилиту watch-and-compile: следи за директорией src/ через fs.watch в рекурсивном режиме. При сохранении .md-файла компилируй Markdown в HTML через пакет marked, сохраняй результат в dist/ с тем же именем файла. Поддерживай аргументы --src, --dist и --watch. Обрабатывай быстрые последовательные изменения (debounce 300 мс).' },
    ],
    '05-02': [
      { level:'medium', uk:'Створи Express-сервер із двома маршрутами: GET /health повертає { status: "ok", uptime: process.uptime(), timestamp } і POST /echo повертає тіло запиту без змін. Підключи express.json() і express.urlencoded(). Перевір обидва маршрути через curl і Postman.', ru:'Создай Express-сервер с двумя маршрутами: GET /health возвращает { status: "ok", uptime: process.uptime(), timestamp } и POST /echo возвращает тело запроса без изменений. Подключи express.json() и express.urlencoded(). Проверь оба маршрута через curl и Postman.' },
      { level:'hard',   uk:'Реалізуй кастомне logging middleware: для кожного запиту записуй рядок із методом, URL, статус-кодом і часом відповіді у мілісекундах до файлу logs/access.log через fs.appendFileSync. Логуй перед відправкою відповіді (через res.on("finish")). Переконайся, що middleware не блокує цикл подій.', ru:'Реализуй кастомное logging middleware: для каждого запроса записывай строку с методом, URL, кодом статуса и временем ответа в миллисекундах в файл logs/access.log через fs.appendFileSync. Логируй перед отправкой ответа (через res.on("finish")). Убедись, что middleware не блокирует цикл событий.' },
      { level:'hard',   uk:'Побудуй Express-роутер для ресурсу /products: GET / (список із пагінацією ?page&limit), GET /:id (404 якщо не знайдено), POST / (201, валідуй наявність name і price), PUT /:id (200), DELETE /:id (204). Використовуй Map як сховище в пам\'яті. Повертай правильні HTTP-статуси і JSON.', ru:'Создай Express-роутер для ресурса /products: GET / (список с пагинацией ?page&limit), GET /:id (404 если не найден), POST / (201, валидируй наличие name и price), PUT /:id (200), DELETE /:id (204). Используй Map как хранилище в памяти. Возвращай правильные HTTP-статусы и JSON.' },
      { level:'extra',  uk:'Побудуй архітектуру плагінних middleware: файл middlewares/index.js експортує масив фабрик [ corsFactory, helmetFactory, rateLimitFactory, loggerFactory ]. Функція applyMiddlewares(app, factories) застосовує їх послідовно. Реалізуй rateLimitFactory: блокує IP-адресу після 10 запитів за 60 секунд (Map + Date.now()), повертає 429 з Retry-After заголовком.', ru:'Создай архитектуру плагинных middleware: файл middlewares/index.js экспортирует массив фабрик [ corsFactory, helmetFactory, rateLimitFactory, loggerFactory ]. Функция applyMiddlewares(app, factories) применяет их последовательно. Реализуй rateLimitFactory: блокирует IP после 10 запросов за 60 секунд (Map + Date.now()), возвращает 429 с заголовком Retry-After.' },
    ],
    '05-03': [
      { level:'medium', uk:'Реалізуй маршрут GET /search?q=term&page=1&limit=10, що шукає у масиві items (у пам\'яті) за рядком q (без урахування регістру), повертає пагіновану відповідь { data, page, limit, total, totalPages }. Валідуй параметри: page і limit мають бути цілими числами > 0, q — рядок від 1 символу. Повертай 400 при невалідних значеннях.', ru:'Реализуй маршрут GET /search?q=term&page=1&limit=10, выполняющий поиск в массиве items (в памяти) по строке q (без учёта регистра), возвращающий постраничный ответ { data, page, limit, total, totalPages }. Валидируй параметры: page и limit должны быть целыми числами > 0, q — строка от 1 символа. Возвращай 400 при невалидных значениях.' },
      { level:'hard',   uk:'Створи вкладені Express-роутери: /api/v1/users/:userId/posts/:postId із Router({ mergeParams: true }) на кожному рівні. Додай param middleware на userId (перевіряє, що це ціле число і зберігає знайденого користувача в req.user) і на postId (перевіряє належність поста користувачу). Повертай 404 при невалідних id.', ru:'Создай вложенные Express-роутеры: /api/v1/users/:userId/posts/:postId с Router({ mergeParams: true }) на каждом уровне. Добавь param middleware для userId (проверяет целое число и сохраняет найденного пользователя в req.user) и для postId (проверяет принадлежность поста пользователю). Возвращай 404 при невалидных id.' },
      { level:'hard',   uk:'Реалізуй middleware-захист маршруту через заголовок x-api-key: функція apiKeyGuard перевіряє наявність і відповідність ключа значенню з process.env.API_KEY. Застосовуй лише до методів POST, PUT і DELETE через router.use(method middleware). Повертай { error: "Unauthorized", code: 401 } для відсутнього або невірного ключа.', ru:'Реализуй middleware-защиту маршрута через заголовок x-api-key: функция apiKeyGuard проверяет наличие и соответствие ключа значению из process.env.API_KEY. Применяй только к методам POST, PUT и DELETE через router.use(method middleware). Возвращай { error: "Unauthorized", code: 401 } для отсутствующего или неверного ключа.' },
      { level:'extra',  uk:'Спроєктуй декларативний реєстр маршрутів: масив об\'єктів { method, path, middlewares: [], handler } у routes/index.js. Напиши registerRoutes(app, routes), що проходить по масиву і реєструє кожен маршрут, передаючи middlewares і handler. Підтримуй per-route обробник помилок (поле onError у конфігурації). Напиши generateApiDocs(routes), що повертає Markdown-список усіх маршрутів.', ru:'Спроектируй декларативный реестр маршрутов: массив объектов { method, path, middlewares: [], handler } в routes/index.js. Напиши registerRoutes(app, routes), проходящий по массиву и регистрирующий каждый маршрут, передавая middlewares и handler. Поддерживай per-route обработчик ошибок (поле onError в конфигурации). Напиши generateApiDocs(routes), возвращающий Markdown-список всех маршрутов.' },
    ],
    '05-04': [
      { level:'medium', uk:'Створи маршрут GET /greet, що читає заголовок Accept-Language і повертає JSON із локалізованим привітанням для uk, ru і en. Якщо заголовок відсутній — відповідай en. Якщо мова явно запитана, але не підтримується — повертай 406 Not Acceptable із { supported: ["uk","ru","en"] }. Підтримуй формат "uk-UA,uk;q=0.9".', ru:'Создай маршрут GET /greet, читающий заголовок Accept-Language и возвращающий JSON с локализованным приветствием для uk, ru и en. Если заголовок отсутствует — отвечай en. Если язык явно запрошен, но не поддерживается — возвращай 406 Not Acceptable с { supported: ["uk","ru","en"] }. Поддерживай формат "uk-UA,uk;q=0.9".' },
      { level:'hard',   uk:'Реалізуй узгодження контенту на GET /data через res.format(): повертай JSON для application/json, CSV для text/csv і plain text для text/plain. Напиши функцію csvSerialize(data: object[]): string, що серіалізує масив об\'єктів у CSV з заголовком, екрануванням ком і лапок. Перевір із curl -H "Accept: text/csv".', ru:'Реализуй согласование контента на GET /data через res.format(): возвращай JSON для application/json, CSV для text/csv и plain text для text/plain. Напиши функцию csvSerialize(data: object[]): string, сериализующую массив объектов в CSV с заголовком, экранированием запятых и кавычек. Проверь с curl -H "Accept: text/csv".' },
      { level:'hard',   uk:'Побудуй потоковий endpoint GET /stream: надсилай 100 рядків даних із затримкою 50 мс між кожним через res.write() і setTimeout в рекурсивній функції. Встанови заголовки Transfer-Encoding: chunked і Content-Type: text/event-stream. Клієнт має бачити дані, що надходять поступово. Реалізуй коректне закриття потоку на req.on("close").', ru:'Создай потоковый endpoint GET /stream: отправляй 100 строк данных с задержкой 50 мс между каждой через res.write() и setTimeout в рекурсивной функции. Установи заголовки Transfer-Encoding: chunked и Content-Type: text/event-stream. Клиент должен видеть данные, поступающие постепенно. Реализуй корректное закрытие потока на req.on("close").' },
      { level:'extra',  uk:'Реалізуй HTTP-кешування: обчислюй ETag як md5-хеш даних через crypto.createHash("md5"). Додавай Last-Modified заголовок. Обробляй If-None-Match і If-Modified-Since у запиті. Якщо дані не змінились — повертай 304 Not Modified без тіла. Напиши тест із supertest, що перевіряє правильну послідовність: перший запит → 200, другий із ETag → 304.', ru:'Реализуй HTTP-кэширование: вычисляй ETag как md5-хеш данных через crypto.createHash("md5"). Добавляй заголовок Last-Modified. Обрабатывай If-None-Match и If-Modified-Since в запросе. Если данные не изменились — возвращай 304 Not Modified без тела. Напиши тест с supertest, проверяющий правильную последовательность: первый запрос → 200, второй с ETag → 304.' },
    ],
    '05-05': [
      { level:'medium', uk:'Налаштуй Express-застосунок із cors (whitelist: ["http://localhost:3000", "https://mydomain.com"]), helmet (усі дефолтні заголовки безпеки), morgan ("combined" формат із записом у файл), express.json({ limit: "10kb" }). Перевір заголовки безпеки через curl -I і переконайся, що X-Powered-By відсутній.', ru:'Настрой Express-приложение с cors (whitelist: ["http://localhost:3000", "https://mydomain.com"]), helmet (все дефолтные заголовки безопасности), morgan ("combined" формат с записью в файл), express.json({ limit: "10kb" }). Проверь заголовки безопасности через curl -I и убедись, что X-Powered-By отсутствует.' },
      { level:'hard',   uk:'Напиши два middleware: bodySizeLimiter відхиляє запити з Content-Length або реальним тілом понад 10 КБ зі статусом 413 і { error: "Payload Too Large" }; htmlSanitizer рекурсивно проходить по req.body і видаляє усі HTML-теги зі рядкових полів через RegExp /<[^>]+>/g. Застосовуй обидва глобально.', ru:'Напиши два middleware: bodySizeLimiter отклоняет запросы с Content-Length или реальным телом свыше 10 КБ со статусом 413 и { error: "Payload Too Large" }; htmlSanitizer рекурсивно проходит по req.body и удаляет все HTML-теги из строковых полей через RegExp /<[^>]+>/g. Применяй оба глобально.' },
      { level:'hard',   uk:'Реалізуй middleware requestId: генеруй UUID v4 через crypto.randomUUID(), прикріпляй до req.id і відповіді у заголовку X-Request-Id. Передавай req.id до всіх записів morgan через morgan.token("request-id", (req) => req.id). Включай requestId і у всі відповіді з помилками у форматі { error, requestId }.', ru:'Реализуй middleware requestId: генерируй UUID v4 через crypto.randomUUID(), прикрепляй к req.id и ответу в заголовке X-Request-Id. Передавай req.id во все записи morgan через morgan.token("request-id", (req) => req.id). Включай requestId и во все ответы с ошибками в формате { error, requestId }.' },
      { level:'extra',  uk:'Побудуй кешувальне middleware cacheMiddleware(ttl) для GET-маршрутів: кешуй відповіді у Map із ключем url + sortedQueryString. Встановлюй TTL через setTimeout і видаляй запис після закінчення. Додавай заголовки Cache-Status: HIT/MISS і X-Cache-Ttl: N. Реалізуй адмін-маршрут DELETE /admin/cache/:key для ручного скидання і GET /admin/cache/stats із { size, keys }.', ru:'Создай кеширующее middleware cacheMiddleware(ttl) для GET-маршрутов: кешируй ответы в Map с ключом url + sortedQueryString. Устанавливай TTL через setTimeout и удаляй запись по истечении. Добавляй заголовки Cache-Status: HIT/MISS и X-Cache-Ttl: N. Реализуй admin-маршрут DELETE /admin/cache/:key для ручного сброса и GET /admin/cache/stats с { size, keys }.' },
    ],
    '05-06': [
      { level:'medium', uk:'Реалізуй REST API книгарні: GET /books (список із ?genre фільтром), GET /books/:id (404 якщо не знайдено), POST /books (201, обов\'язкові поля title і author), PUT /books/:id (200), DELETE /books/:id (204). Зберігай у Map. Повертай 400 для невалідного тіла. Перевір кожен маршрут через curl.', ru:'Реализуй REST API книжного магазина: GET /books (список с ?genre фильтром), GET /books/:id (404 если не найден), POST /books (201, обязательные поля title и author), PUT /books/:id (200), DELETE /books/:id (204). Храни в Map. Возвращай 400 для невалидного тела. Проверь каждый маршрут через curl.' },
      { level:'hard',   uk:'Додай HATEOAS-посилання до API книгарні: кожна відповідь містить поле _links із self (URL поточного ресурсу), collection (URL списку) і, якщо є authorId, related (URL автора). Використовуй req.protocol і req.get("host") для побудови абсолютних URL. Дотримуйся формату RFC 5988.', ru:'Добавь HATEOAS-ссылки в API книжного магазина: каждый ответ содержит поле _links с self (URL текущего ресурса), collection (URL списка) и, при наличии authorId, related (URL автора). Используй req.protocol и req.get("host") для построения абсолютных URL. Следуй формату RFC 5988.' },
      { level:'hard',   uk:'Реалізуй версіонування API: /api/v1/books і /api/v2/books на одному застосунку з окремими express.Router() екземплярами. v2 додає поле authors (масив рядків) і повертає заголовок Deprecation: true для v1. Обидві версії підключаються через app.use("/api", versionRouter), що перемикає на потрібний роутер за URL-префіксом.', ru:'Реализуй версионирование API: /api/v1/books и /api/v2/books на одном приложении с отдельными express.Router() экземплярами. v2 добавляет поле authors (массив строк) і возвращает заголовок Deprecation: true для v1. Обе версии подключаются через app.use("/api", versionRouter), переключающийся на нужный роутер по URL-префиксу.' },
      { level:'extra',  uk:'Побудуй REST API з повним набором фільтрів: GET /products?category=X&minPrice=10&maxPrice=500&sort=price&order=asc&page=2&limit=20. Напиши middleware parseQueryFilters, що парсить і валідує кожен параметр і зберігає у req.filters. Застосовуй фільтри до масиву даних у сервісному шарі. Повертай заголовки X-Total-Count і Link (RFC 5988: prev, next, first, last).', ru:'Создай REST API с полным набором фильтров: GET /products?category=X&minPrice=10&maxPrice=500&sort=price&order=asc&page=2&limit=20. Напиши middleware parseQueryFilters, парсящий и валидирующий каждый параметр и сохраняющий в req.filters. Применяй фильтры к массиву данных в сервисном слое. Возвращай заголовки X-Total-Count и Link (RFC 5988: prev, next, first, last).' },
    ],
    '05-07': [
      { level:'medium', uk:'Реалізуй POST /auth/login: приймай { email, password }, перевіряй по списку користувачів у пам\'яті і повертай підписаний JWT (jsonwebtoken, алгоритм HS256, expiry 15 хв). Напиши middleware verifyToken, що зчитує заголовок Authorization: Bearer <token> і декодує токен. Захисти GET /profile цим middleware — повертай дані поточного користувача з payload.', ru:'Реализуй POST /auth/login: принимай { email, password }, проверяй по списку пользователей в памяти и возвращай подписанный JWT (jsonwebtoken, алгоритм HS256, срок 15 мин). Напиши middleware verifyToken, читающий заголовок Authorization: Bearer <token> и декодирующий токен. Защити GET /profile этим middleware — возвращай данные текущего пользователя из payload.' },
      { level:'hard',   uk:'Додай повний потік refresh-токенів: POST /auth/login повертає { accessToken (15 хв), refreshToken (7 днів) }. POST /auth/refresh приймає refreshToken у тілі, перевіряє підпис і наявність у Map, повертає нові обидва токени (ротація). POST /auth/logout видаляє refreshToken із Map. Повертай 401 для протермінованих або відсутніх токенів.', ru:'Добавь полный поток refresh-токенов: POST /auth/login возвращает { accessToken (15 мин), refreshToken (7 дней) }. POST /auth/refresh принимает refreshToken в теле, проверяет подпись и наличие в Map, возвращает оба новых токена (ротация). POST /auth/logout удаляет refreshToken из Map. Возвращай 401 для просроченных или отсутствующих токенов.' },
      { level:'hard',   uk:'Реалізуй контроль доступу на основі ролей: JWT-payload містить поле role ("admin" або "user"). Напиши фабрику middleware requireRole(...roles), що перевіряє роль користувача і повертає 403 { error: "Forbidden" } при недостатніх правах. Захисти GET /users (лише admin), DELETE /users/:id (лише admin), PATCH /users/me (user і admin).', ru:'Реализуй контроль доступа на основе ролей: JWT-payload содержит поле role ("admin" или "user"). Напиши фабрику middleware requireRole(...roles), проверяющую роль пользователя и возвращающую 403 { error: "Forbidden" } при недостаточных правах. Защити GET /users (только admin), DELETE /users/:id (только admin), PATCH /users/me (user и admin).' },
      { level:'extra',  uk:'Побудуй повну систему автентифікації: bcrypt (saltRounds: 12) для хешування паролів, JWT access-токени (15 хв), refresh-токени у Map із ротацією при кожному використанні. GET /auth/me повертає { id, email, role } із payload поточного access-токена. Напиши middleware isAuthenticated, що об\'єднує verifyToken і requireRole. Додай захист від brute-force (блокуй IP після 5 невдалих спроб входу).', ru:'Создай полную систему аутентификации: bcrypt (saltRounds: 12) для хеширования паролей, JWT access-токены (15 мин), refresh-токены в Map с ротацией при каждом использовании. GET /auth/me возвращает { id, email, role } из payload текущего access-токена. Напиши middleware isAuthenticated, объединяющий verifyToken и requireRole. Добавь защиту от brute-force (блокируй IP после 5 неудачных попыток входа).' },
    ],
    '05-08': [
      { level:'medium', uk:'Налаштуй Multer для завантаження одного зображення на POST /upload. У fileFilter перевіряй MIME-тип (дозволяй лише image/jpeg і image/png) і відхиляй інші із кодом 400. Встанови limits: { fileSize: 2 * 1024 * 1024 }. При успіху повертай { url: "/uploads/filename", originalName, size }.', ru:'Настрой Multer для загрузки одного изображения на POST /upload. В fileFilter проверяй MIME-тип (разрешай только image/jpeg и image/png) и отклоняй остальные с кодом 400. Установи limits: { fileSize: 2 * 1024 * 1024 }. При успехе возвращай { url: "/uploads/filename", originalName, size }.' },
      { level:'hard',   uk:'Реалізуй завантаження галереї товару: POST /products/:id/images приймає до 5 зображень (multer .array("images", 5)). Перейменовуй кожен файл у crypto.randomUUID() + оригінальне розширення. Зберігай масив метаданих { filename, url, size, uploadedAt } у Map по productId. Повертай список усіх зображень товару.', ru:'Реализуй загрузку галереи товара: POST /products/:id/images принимает до 5 изображений (multer .array("images", 5)). Переименовывай каждый файл в crypto.randomUUID() + оригинальное расширение. Храни массив метаданных { filename, url, size, uploadedAt } в Map по productId. Возвращай список всех изображений товара.' },
      { level:'hard',   uk:'Побудуй endpoint імпорту CSV POST /import/users: приймай CSV-файл через Multer (зберігай у пам\'яті через memoryStorage). Парси вміст у потоковому режимі через csv-parse з опцією columns: true. Валідуй кожен рядок (обов\'язкові email, name; age — число 18–120). Повертай { imported: N, skipped: N, errors: [{ row, field, message }] }.', ru:'Создай endpoint импорта CSV POST /import/users: принимай CSV-файл через Multer (храни в памяти через memoryStorage). Парси содержимое в потоковом режиме через csv-parse с опцией columns: true. Валидируй каждую строку (обязательные email, name; age — число 18–120). Возвращай { imported: N, skipped: N, errors: [{ row, field, message }] }.' },
      { level:'extra',  uk:'Створи конвеєр обробки зображень із Multer + Sharp: при завантаженні одного оригінального файлу генеруй три варіанти: thumbnail (150×150, fit: "cover"), medium (400×400, fit: "inside") і large (1200×1200, fit: "inside"). Зберігай усі три паралельно через Promise.all. Повертай { original, thumbnail, medium, large } із URL для кожного. Видаляй оригінал після генерації варіантів.', ru:'Создай конвейер обработки изображений с Multer + Sharp: при загрузке одного оригинального файла генерируй три варианта: thumbnail (150×150, fit: "cover"), medium (400×400, fit: "inside") и large (1200×1200, fit: "inside"). Сохраняй все три параллельно через Promise.all. Возвращай { original, thumbnail, medium, large } с URL для каждого. Удаляй оригинал после генерации вариантов.' },
    ],
    '05-09': [
      { level:'medium', uk:'Додай Zod-валідацію до POST /users: схема приймає name (рядок, мін. 2, макс. 50), email (z.string().email()), age (z.number().int().min(18).max(120)). При помилці валідації повертай статус 422 і масив { field, message } (витягнутий із ZodError.issues). Переконайся, що успішні запити проходять без змін.', ru:'Добавь Zod-валидацию к POST /users: схема принимает name (строка, мин. 2, макс. 50), email (z.string().email()), age (z.number().int().min(18).max(120)). При ошибке валидации возвращай статус 422 и массив { field, message } (извлечённый из ZodError.issues). Убедись, что успешные запросы проходят без изменений.' },
      { level:'hard',   uk:'Реалізуй фабрику middleware validateBody(schema: ZodSchema), що приймає будь-яку Zod-схему, валідує req.body і у разі помилки передає у next() об\'єкт ValidationError зі списком помилок { field, message }. Застосуй до всіх маршрутів-мутацій (POST, PUT, PATCH). Обробляй ValidationError у глобальному error middleware — повертай 422.', ru:'Реализуй фабрику middleware validateBody(schema: ZodSchema), принимающую любую Zod-схему, валидирующую req.body и при ошибке передающую в next() объект ValidationError со списком ошибок { field, message }. Применяй ко всем маршрутам-мутациям (POST, PUT, PATCH). Обрабатывай ValidationError в глобальном error middleware — возвращай 422.' },
      { level:'hard',   uk:'Валідуй query-параметри через Zod на GET /products: page через z.coerce.number().int().positive(), limit через z.coerce.number().int().min(1).max(100).default(20), sort через z.enum(["price","name","date"]).default("date"), order через z.enum(["asc","desc"]).default("asc"). Валідуй через .safeParse і повертай 400 із деталями при помилці.', ru:'Валидируй query-параметры через Zod на GET /products: page через z.coerce.number().int().positive(), limit через z.coerce.number().int().min(1).max(100).default(20), sort через z.enum(["price","name","date"]).default("date"), order через z.enum(["asc","desc"]).default("asc"). Валидируй через .safeParse и возвращай 400 с деталями при ошибке.' },
      { level:'extra',  uk:'Створи типізований будівельник маршрутів typedRoute({ inputSchema, outputSchema, handler }): автоматично валідує req.body через inputSchema перед викликом handler; повертає 422 при помилці вхідних даних. Після виконання handler валідує результат через outputSchema через .safeParse; якщо вихідна схема відхиляє відповідь — кидає 500 (помилка розробника). Задокументуй задум у коментарях.', ru:'Создай типизированный строитель маршрутов typedRoute({ inputSchema, outputSchema, handler }): автоматически валидирует req.body через inputSchema перед вызовом handler; возвращает 422 при ошибке входных данных. После выполнения handler валидирует результат через outputSchema через .safeParse; если выходная схема отклоняет ответ — бросает 500 (ошибка разработчика). Задокументируй замысел в комментариях.' },
    ],
    '05-10': [
      { level:'medium', uk:'Створи клас AppError, що розширює Error: поля statusCode (число), message і isOperational (boolean, true для передбачених помилок). Кидай AppError у маршрутних обробниках (напр. new AppError(404, "Не знайдено")). У глобальному error middleware перевіряй isOperational і відповідай відповідним statusCode і { error, statusCode } JSON.', ru:'Создай класс AppError, расширяющий Error: поля statusCode (число), message и isOperational (boolean, true для предвиденных ошибок). Бросай AppError в обработчиках маршрутов (напр. new AppError(404, "Не найдено")). В глобальном error middleware проверяй isOperational и отвечай соответствующим statusCode и { error, statusCode } JSON.' },
      { level:'hard',   uk:'Реалізуй утиліту asyncHandler(fn): огортає асинхронну функцію маршруту і перехоплює відхилення Promise через .catch(next). Рефакторуй усі async-обробники у застосунку — видали блоки try/catch і замість цього огорни кожен обробник через asyncHandler. Переконайся, що помилки все одно потрапляють до глобального error middleware.', ru:'Реализуй утилиту asyncHandler(fn): оборачивает асинхронную функцию маршрута и перехватывает отклонения Promise через .catch(next). Отрефакторь все async-обработчики в приложении — удали блоки try/catch и вместо этого оберни каждый обработчик через asyncHandler. Убедись, что ошибки всё равно попадают в глобальный error middleware.' },
      { level:'hard',   uk:'Побудуй централізований error middleware з чотирма гілками: ZodError → 422 із розпарсованими полями; AppError → її statusCode; JsonWebTokenError/TokenExpiredError → 401 із відповідним повідомленням; усі інші → 500 із прихованим повідомленням у продакшені. Виводь повний стек-трейс лише якщо NODE_ENV !== "production".', ru:'Создай централизованный error middleware с четырьмя ветвями: ZodError → 422 с разобранными полями; AppError → её statusCode; JsonWebTokenError/TokenExpiredError → 401 с соответствующим сообщением; все остальные → 500 со скрытым сообщением в продакшене. Выводи полный стек-трейс только если NODE_ENV !== "production".' },
      { level:'extra',  uk:'Налаштуй winston: у development — colorize console transport (рівень debug); у production — два транспорти: JSON-файл logs/combined.log (рівень info) і logs/error.log (лише error). Кожен запис містить timestamp, level, message, requestId, userId і path. Додай GET /admin/errors (захищений requireRole("admin")), що читає останні 50 рядків logs/error.log і повертає масив JSON.', ru:'Настрой winston: в development — colorize console transport (уровень debug); в production — два транспорта: JSON-файл logs/combined.log (уровень info) и logs/error.log (только error). Каждая запись содержит timestamp, level, message, requestId, userId и path. Добавь GET /admin/errors (защищённый requireRole("admin")), читающий последние 50 строк logs/error.log и возвращающий массив JSON.' },
    ],
    '05-11': [
      { level:'medium', uk:'Налаштуй dotenv: файл .env містить PORT, NODE_ENV, JWT_SECRET, CORS_ORIGIN, LOG_LEVEL. Напиши модуль src/config.js, що завантажує .env через dotenv.config(), перевіряє наявність обов\'язкових змінних (PORT, JWT_SECRET) і кидає Error з переліком відсутніх. Додай .env.example без реальних значень до репозиторію.', ru:'Настрой dotenv: файл .env содержит PORT, NODE_ENV, JWT_SECRET, CORS_ORIGIN, LOG_LEVEL. Напиши модуль src/config.js, загружающий .env через dotenv.config(), проверяющий наличие обязательных переменных (PORT, JWT_SECRET) и бросающий Error с перечнем отсутствующих. Добавь .env.example без реальных значений в репозиторий.' },
      { level:'hard',   uk:'Підтримуй середовищеві конфігурації: завантажуй .env.development або .env.production залежно від NODE_ENV через dotenv.config({ path: ".env." + process.env.NODE_ENV }). Напиши validateConfig(config), що логує попередження для необов\'язкових відсутніх змінних (LOG_LEVEL, SENTRY_DSN) і кидає для обов\'язкових.', ru:'Поддерживай конфигурации для среды: загружай .env.development или .env.production в зависимости от NODE_ENV через dotenv.config({ path: ".env." + process.env.NODE_ENV }). Напиши validateConfig(config), логирующий предупреждения для необязательных отсутствующих переменных (LOG_LEVEL, SENTRY_DSN) и бросающий для обязательных.' },
      { level:'hard',   uk:'Реалізуй ротацію секрету JWT: підтримуй JWT_SECRET (для підпису нових токенів) і JWT_SECRET_OLD (для перевірки старих). У verifyToken спробуй verify із JWT_SECRET, при помилці — спробуй JWT_SECRET_OLD. Якщо обидва не спрацювали — 401. Нові токени завжди підписуй лише через JWT_SECRET.', ru:'Реализуй ротацию секрета JWT: поддерживай JWT_SECRET (для подписи новых токенов) и JWT_SECRET_OLD (для проверки старых). В verifyToken попробуй verify с JWT_SECRET, при ошибке — попробуй JWT_SECRET_OLD. Если оба не сработали — 401. Новые токены всегда подписывай только через JWT_SECRET.' },
      { level:'extra',  uk:'Створи модуль config/schema.js із Zod-схемою для process.env: PORT через z.coerce.number().default(3000), NODE_ENV через z.enum(["development","test","production"]).default("development"), JWT_SECRET через z.string().min(32), CORS_ORIGIN через z.string().url(). Парси і перевіряй схему при запуску і експортуй типізований об\'єкт config. Додай тести для валідатора конфігурації.', ru:'Создай модуль config/schema.js с Zod-схемой для process.env: PORT через z.coerce.number().default(3000), NODE_ENV через z.enum(["development","test","production"]).default("development"), JWT_SECRET через z.string().min(32), CORS_ORIGIN через z.string().url(). Парси и проверяй схему при запуске и экспортируй типизированный объект config. Добавь тесты для валидатора конфигурации.' },
    ],
    '05-12': [
      { level:'medium', uk:'Ініціалізуй структуру проєкту: src/routes/, src/controllers/, src/middleware/, src/models/, src/utils/, src/config/. Створи простий DI-контейнер (plain object container) у src/container.js, що реєструє і вирішує залежності (UserService, PostService) і передає їх у контролери.', ru:'Инициализируй структуру проекта: src/routes/, src/controllers/, src/middleware/, src/models/, src/utils/, src/config/. Создай простой DI-контейнер (plain object container) в src/container.js, регистрирующий и разрешающий зависимости (UserService, PostService) и передающий их в контроллеры.' },
      { level:'hard',   uk:'Побудуй CRUD користувачів із JWT: POST /auth/register (bcrypt хешування, повертає { user, accessToken, refreshToken }), POST /auth/login, GET /users (лише admin), GET /users/me, PATCH /users/me (оновлення name і avatarUrl), DELETE /users/me. Зберігай у Map. Захисти маршрути через verifyToken і requireRole.', ru:'Создай CRUD пользователей с JWT: POST /auth/register (bcrypt хеширование, возвращает { user, accessToken, refreshToken }), POST /auth/login, GET /users (только admin), GET /users/me, PATCH /users/me (обновление name и avatarUrl), DELETE /users/me. Храни в Map. Защити маршруты через verifyToken и requireRole.' },
      { level:'hard',   uk:'Додай Zod-валідацію до всіх маршрутів-мутацій через validateBody middleware, глобальний error middleware (AppError + ZodError + JWT-помилки + невідомі), логування запитів через morgan у logs/access.log, GET /health із { status: "ok", uptime, memoryUsage, timestamp }.', ru:'Добавь Zod-валидацию ко всем маршрутам-мутациям через middleware validateBody, глобальный error middleware (AppError + ZodError + JWT-ошибки + неизвестные), логирование запросов через morgan в logs/access.log, GET /health с { status: "ok", uptime, memoryUsage, timestamp }.' },
      { level:'extra',  uk:'Напиши інтеграційні тести Jest + supertest: register (успіх і дублікат email), login (успіх і невірний пароль), refresh (успіх і відсутній токен), logout, GET /users/me (з токеном і без). Використовуй Map-сховище в пам\'яті, що скидається у beforeEach. Досягни 85%+ покриття рядків. Додай .github/workflows/test.yml, що запускає npm test на Node 18 та 20.', ru:'Напиши интеграционные тесты Jest + supertest: register (успех и дубликат email), login (успех и неверный пароль), refresh (успех и отсутствующий токен), logout, GET /users/me (с токеном и без). Используй Map-хранилище в памяти, сбрасываемое в beforeEach. Достигни 85%+ покрытия строк. Добавь .github/workflows/test.yml, запускающий npm test на Node 18 и 20.' },
    ],
  };
  Object.keys(EXT).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.tasks = (l.tasks || []).concat(EXT[id]);
  });
})();
