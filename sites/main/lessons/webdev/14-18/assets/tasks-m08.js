/* Додаткові завдання · Модуль 08 — SQL & Databases */
(function () {
  'use strict';
  var EXT = {
    '08-01': [
      { level:'medium', uk:'Створи схему блогу: таблиці users (id SERIAL PK, username VARCHAR(50) UNIQUE NOT NULL, email VARCHAR(255) UNIQUE NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()), categories (id, name, slug UNIQUE), articles (id, title, body TEXT, status VARCHAR(20) CHECK (status IN (\'draft\',\'published\')), user_id FK, category_id FK, created_at). Вставте по 5 тестових записів у кожну таблицю.', ru:'Создай схему блога: таблицы users (id SERIAL PK, username VARCHAR(50) UNIQUE NOT NULL, email VARCHAR(255) UNIQUE NOT NULL, created_at TIMESTAMPTZ DEFAULT NOW()), categories (id, name, slug UNIQUE), articles (id, title, body TEXT, status VARCHAR(20) CHECK (status IN (\'draft\',\'published\')), user_id FK, category_id FK, created_at). Вставьте по 5 тестовых записей в каждую таблицу.' },
      { level:'hard',   uk:'Напиши складний SELECT-запит для аналітики: отримай авторів, що опублікували більше 3 статей за останні 30 днів, упорядкованих за article_count DESC. Виведи: author_name, article_count_last_month, latest_article_title, total_articles_all_time. Реалізуй виключно через JOIN і GROUP BY, без підзапитів. Порівняй план виконання через EXPLAIN ANALYZE.', ru:'Напиши сложный SELECT-запрос для аналитики: получи авторов, опубликовавших больше 3 статей за последние 30 дней, упорядоченных по article_count DESC. Выведи: author_name, article_count_last_month, latest_article_title, total_articles_all_time. Реализуй исключительно через JOIN и GROUP BY, без подзапросов. Сравни план выполнения через EXPLAIN ANALYZE.' },
      { level:'hard',   uk:'Реалізуй UPDATE і DELETE з умовами: 1) UPDATE articles SET status=\'archived\' WHERE created_at < NOW() - INTERVAL \'1 year\' AND views_count < 100 — виведи кількість оновлених рядків через RETURNING id; 2) DELETE дублікати email у таблиці users, залишаючи запис із мінімальним id (через self-join або DELETE ... USING). Перевір результат через SELECT після кожної операції.', ru:'Реализуй UPDATE и DELETE с условиями: 1) UPDATE articles SET status=\'archived\' WHERE created_at < NOW() - INTERVAL \'1 year\' AND views_count < 100 — выведи количество обновлённых строк через RETURNING id; 2) DELETE дубликаты email в таблице users, оставляя запись с минимальным id (через self-join или DELETE ... USING). Проверь результат через SELECT после каждой операции.' },
      { level:'extra',  uk:'Спроєктуй повну схему e-commerce: users, products, categories (ієрархічна — self-referencing FK parent_id), orders, order_items, addresses, payments. Напиши повний DDL із FK, CHECK, UNIQUE, DEFAULT. Додай TRIGGER update_order_total(), що автоматично перераховує orders.total_amount при будь-якій зміні у order_items (INSERT/UPDATE/DELETE). Перевір тригер транзакційним тестом.', ru:'Спроектируй полную схему e-commerce: users, products, categories (иерархическая — self-referencing FK parent_id), orders, order_items, addresses, payments. Напиши полный DDL с FK, CHECK, UNIQUE, DEFAULT. Добавь TRIGGER update_order_total(), который автоматически пересчитывает orders.total_amount при любом изменении в order_items (INSERT/UPDATE/DELETE). Проверь триггер транзакционным тестом.' },
    ],
    '08-02': [
      { level:'medium', uk:'Напиши запити з JOIN для бази блогу: 1) INNER JOIN — статті з іменами авторів і назвами категорій; 2) LEFT JOIN — всі автори і кількість їхніх статей (включно з авторами без жодної статті); 3) RIGHT JOIN — всі категорії і кількість їхніх статей. Порівняй кількість рядків у результатах і поясни різницю у коментарях SQL.', ru:'Напиши запросы с JOIN для базы блога: 1) INNER JOIN — статьи с именами авторов и названиями категорий; 2) LEFT JOIN — все авторы и количество их статей (включая авторов без ни одной статьи); 3) RIGHT JOIN — все категории и количество их статей. Сравни количество строк в результатах и объясни разницу в комментариях SQL.' },
      { level:'hard',   uk:'Реалізуй звіт "Матриця активності" через FULL OUTER JOIN між users і articles за останні 30 днів. Результат: user_id, username, published_count, draft_count, last_activity_date. Користувачі без статей — published_count=0, статті без авторів (orphan) — username=\'(deleted)\'. Упорядкуй за last_activity_date DESC NULLS LAST, потім за username ASC.', ru:'Реализуй отчёт "Матрица активности" через FULL OUTER JOIN между users и articles за последние 30 дней. Результат: user_id, username, published_count, draft_count, last_activity_date. Пользователи без статей — published_count=0, статьи без авторов (orphan) — username=\'(deleted)\'. Упорядочь по last_activity_date DESC NULLS LAST, затем по username ASC.' },
      { level:'hard',   uk:'Напиши запит на основі CROSS JOIN для генерації розкладу: CROSS JOIN між weekdays (7 рядків: Monday..Sunday) і time_slots (8 рядків: 09:00, 10:00...16:00). Отримай всі 56 можливих слотів. Через LEFT JOIN до таблиці appointments (day, time, trainer_id) відфільтруй зайняті. Виведи: day, time_slot, is_available, appointment_id (NULL якщо вільний).', ru:'Напиши запрос на основе CROSS JOIN для генерации расписания: CROSS JOIN между weekdays (7 строк: Monday..Sunday) и time_slots (8 строк: 09:00, 10:00...16:00). Получи все 56 возможных слотов. Через LEFT JOIN к таблице appointments (day, time, trainer_id) отфильтруй занятые. Выведи: day, time_slot, is_available, appointment_id (NULL если свободен).' },
      { level:'extra',  uk:'Реалізуй рекомендаційну систему "Люди, яких ти можеш знати" через JOIN: знайди користувачів, з якими поточний user має спільних друзів (таблиця friendships: user_id, friend_id, created_at), але ще не дружить безпосередньо. Ранжуй за кількістю спільних друзів DESC. Виключи самого себе, вже наявних друзів і заблокованих користувачів (таблиця blocks). Обмеж TOP 10.', ru:'Реализуй рекомендательную систему "Люди, которых ты можешь знать" через JOIN: найди пользователей, с которыми текущий user имеет общих друзей (таблица friendships: user_id, friend_id, created_at), но ещё не дружит напрямую. Ранжируй по количеству общих друзей DESC. Исключи самого себя, уже существующих друзей и заблокированных пользователей (таблица blocks). Ограничь TOP 10.' },
    ],
    '08-03': [
      { level:'medium', uk:'Напиши аналітичні запити для e-commerce: 1) COUNT замовлень і SUM(total_amount) по кожному статусу; 2) AVG, MIN, MAX ціни товарів по категоріях із фільтром HAVING COUNT(products) >= 3; 3) COUNT(DISTINCT user_id) покупців за кожен місяць поточного року через DATE_TRUNC. Поясни у коментарях чому HAVING, а не WHERE для агрегатних умов.', ru:'Напиши аналитические запросы для e-commerce: 1) COUNT заказов и SUM(total_amount) по каждому статусу; 2) AVG, MIN, MAX цены товаров по категориям с фильтром HAVING COUNT(products) >= 3; 3) COUNT(DISTINCT user_id) покупателей за каждый месяц текущего года через DATE_TRUNC. Объясни в комментариях почему HAVING, а не WHERE для агрегатных условий.' },
      { level:'hard',   uk:'Реалізуй звіт "Топ-10 продуктів за виручкою": GROUP BY product_id із HAVING total_revenue > 1000 AND order_count >= 5, упорядкуй DESC. Додай оконну функцію RANK() OVER (ORDER BY total_revenue DESC). Включи: product_name, category_name, order_count, total_revenue, revenue_percent (від загальної виручки), rank. Виконай одним запитом через CTE.', ru:'Реализуй отчёт "Топ-10 продуктов по выручке": GROUP BY product_id с HAVING total_revenue > 1000 AND order_count >= 5, упорядочь DESC. Добавь оконную функцию RANK() OVER (ORDER BY total_revenue DESC). Включи: product_name, category_name, order_count, total_revenue, revenue_percent (от общей выручки), rank. Выполни одним запросом через CTE.' },
      { level:'hard',   uk:'Напиши запит когортного аналізу утримання клієнтів: згрупуй users за місяцем реєстрації (cohort_month = DATE_TRUNC(\'month\', created_at)). Для кожної когорти підрахуй, скільки users зробили хоча б одну покупку у місяці 0, 1, 2, 3 після реєстрації. Виведи: cohort_month, month_0_count, month_1_count, month_2_count, month_3_count.', ru:'Напиши запрос когортного анализа удержания клиентов: сгруппируй users по месяцу регистрации (cohort_month = DATE_TRUNC(\'month\', created_at)). Для каждой когорты подсчитай, сколько users сделали хотя бы одну покупку в месяце 0, 1, 2, 3 после регистрации. Выведи: cohort_month, month_0_count, month_1_count, month_2_count, month_3_count.' },
      { level:'extra',  uk:'Реалізуй аналітичний дашборд через оконні функції (одним запитом через CTE): LAG(revenue, 1) OVER (ORDER BY month) для MoM-порівняння, SUM(revenue) OVER (ORDER BY month ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) для накопиченої виручки, NTILE(4) OVER (ORDER BY total_spent) для розподілу клієнтів на квартилі. Поверни всі метрики за останні 12 місяців.', ru:'Реализуй аналитический дашборд через оконные функции (одним запросом через CTE): LAG(revenue, 1) OVER (ORDER BY month) для MoM-сравнения, SUM(revenue) OVER (ORDER BY month ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) для накопленной выручки, NTILE(4) OVER (ORDER BY total_spent) для распределения клиентов на квартили. Верни все метрики за последние 12 месяцев.' },
    ],
    '08-04': [
      { level:'medium', uk:'Перепиши один і той самий запит трьома способами: знайди users, що зробили замовлення на суму більшу за середню суму всіх замовлень. Реалізуй: 1) correlated subquery у WHERE; 2) scalar subquery у WHERE (SELECT AVG...); 3) CTE: WITH avg_order AS (SELECT AVG(total) FROM orders). Порівняй EXPLAIN ANALYZE для кожного варіанту.', ru:'Перепиши один и тот же запрос тремя способами: найди users, которые сделали заказ на сумму большую, чем средняя сумма всех заказов. Реализуй: 1) correlated subquery в WHERE; 2) scalar subquery в WHERE (SELECT AVG...); 3) CTE: WITH avg_order AS (SELECT AVG(total) FROM orders). Сравни EXPLAIN ANALYZE для каждого варианта.' },
      { level:'hard',   uk:'Реалізуй рекурсивний CTE для обходу ієрархії категорій: WITH RECURSIVE category_tree AS (anchor — коренева категорія WHERE parent_id IS NULL, recursive — JOIN categories ON parent_id = category_tree.id). Виведи: id, name, level (глибина вкладеності), path (рядок "Electronics > Phones > Android"). Обмеж MAXRECURSION 5 рівнями через WHERE level < 5.', ru:'Реализуй рекурсивный CTE для обхода иерархии категорий: WITH RECURSIVE category_tree AS (anchor — корневая категория WHERE parent_id IS NULL, recursive — JOIN categories ON parent_id = category_tree.id). Выведи: id, name, level (глубина вложенности), path (строка "Electronics > Phones > Android"). Ограничь MAXRECURSION 5 уровнями через WHERE level < 5.' },
      { level:'hard',   uk:'Напиши CTE для виявлення неактивних облікових записів: users, що зареєструвались > 6 місяців тому, не входили в систему > 90 днів і не мають жодного замовлення за останній рік. Реалізуй через 3 іменовані CTE (registered_early, inactive_login, no_recent_orders) і об\'єднай через JOIN або INTERSECT. Поверни user_id, email, days_since_login.', ru:'Напиши CTE для выявления неактивных аккаунтов: users, зарегистрировавшиеся > 6 месяцев назад, не входившие в систему > 90 дней и не имеющие ни одного заказа за последний год. Реализуй через 3 именованных CTE (registered_early, inactive_login, no_recent_orders) и объедини через JOIN или INTERSECT. Верни user_id, email, days_since_login.' },
      { level:'extra',  uk:'Реалізуй пошук найкоротшого шляху у графі через рекурсивний CTE (спрощений Dijkstra): таблиця edges (from_node INT, to_node INT, weight NUMERIC). WITH RECURSIVE path_finder шукає шляхи від start_id до end_id, накопичує total_weight і масив visited_nodes (для уникнення циклів через NOT (to_node = ANY(visited))). Поверни найкоротший шлях і його загальну вагу.', ru:'Реализуй поиск кратчайшего пути в графе через рекурсивный CTE (упрощённый Dijkstra): таблица edges (from_node INT, to_node INT, weight NUMERIC). WITH RECURSIVE path_finder ищет пути от start_id до end_id, накапливает total_weight и массив visited_nodes (для избежания циклов через NOT (to_node = ANY(visited))). Верни кратчайший путь и его общий вес.' },
    ],
    '08-05': [
      { level:'medium', uk:'Проаналізуй і оптимізуй запити для таблиці articles (100k рядків): 1) CREATE INDEX idx_articles_status_created ON articles (status, created_at DESC) для фільтрації published за датою; 2) CREATE INDEX idx_users_email_lower ON users (lower(email)) для пошуку без урахування регістру. Порівняй EXPLAIN ANALYZE до і після: seq scan vs index scan. Зафіксуй різницю у мілісекундах.', ru:'Проанализируй и оптимизируй запросы для таблицы articles (100k строк): 1) CREATE INDEX idx_articles_status_created ON articles (status, created_at DESC) для фильтрации published по дате; 2) CREATE INDEX idx_users_email_lower ON users (lower(email)) для поиска без учёта регистра. Сравни EXPLAIN ANALYZE до и после: seq scan vs index scan. Зафиксируй разницу в миллисекундах.' },
      { level:'hard',   uk:'Реалізуй повнотекстовий пошук у PostgreSQL через GIN-індекс: додай стовпець search_vector tsvector GENERATED ALWAYS AS (to_tsvector(\'russian\', coalesce(title,\'\') || \' \' || coalesce(body,\'\'))) STORED до articles. Створи CREATE INDEX idx_articles_fts ON articles USING GIN(search_vector). Напиши пошуковий запит через to_tsquery із ts_headline для підсвічування збігів у результатах.', ru:'Реализуй полнотекстовый поиск в PostgreSQL через GIN-индекс: добавь столбец search_vector tsvector GENERATED ALWAYS AS (to_tsvector(\'russian\', coalesce(title,\'\') || \' \' || coalesce(body,\'\'))) STORED к articles. Создай CREATE INDEX idx_articles_fts ON articles USING GIN(search_vector). Напиши поисковый запрос через to_tsquery с ts_headline для подсветки совпадений в результатах.' },
      { level:'hard',   uk:'Дослідж і виправ index bloat: 1) CREATE UNIQUE INDEX CONCURRENTLY idx_products_sku ON products(sku) без блокування; 2) REINDEX CONCURRENTLY idx_orders_user_id для перебудови фрагментованого індексу; 3) CREATE INDEX CONCURRENTLY idx_orders_active ON orders(user_id, created_at) WHERE status != \'cancelled\' (partial index). Порівняй розміри через pg_relation_size() і pg_indexes_size().', ru:'Исследуй и исправь index bloat: 1) CREATE UNIQUE INDEX CONCURRENTLY idx_products_sku ON products(sku) без блокировки; 2) REINDEX CONCURRENTLY idx_orders_user_id для перестройки фрагментированного индекса; 3) CREATE INDEX CONCURRENTLY idx_orders_active ON orders(user_id, created_at) WHERE status != \'cancelled\' (partial index). Сравни размеры через pg_relation_size() и pg_indexes_size().' },
      { level:'extra',  uk:'Збудуй систему моніторингу індексів через системні каталоги PostgreSQL: 1) невикористовувані індекси (pg_stat_user_indexes WHERE idx_scan = 0 AND schemaname = \'public\'); 2) дублюючі індекси (pg_indexes де стовпці перетинаються); 3) таблиці без індексів на FK (pg_constraint JOIN pg_class). Оформи як збережені функції і заплануй через pg_cron для щотижневого звіту.', ru:'Построй систему мониторинга индексов через системные каталоги PostgreSQL: 1) неиспользуемые индексы (pg_stat_user_indexes WHERE idx_scan = 0 AND schemaname = \'public\'); 2) дублирующие индексы (pg_indexes где столбцы пересекаются); 3) таблицы без индексов на FK (pg_constraint JOIN pg_class). Оформи как хранимые функции и запланируй через pg_cron для еженедельного отчёта.' },
    ],
    '08-06': [
      { level:'medium', uk:'Реалізуй транзакцію переказу коштів: PL/pgSQL функція transfer_money(from_id INT, to_id INT, amount NUMERIC). BEGIN; перевір баланс — якщо from_account.balance < amount, RAISE EXCEPTION \'Insufficient funds\'; UPDATE accounts SET balance = balance - amount WHERE id = from_id; UPDATE accounts SET balance = balance + amount WHERE id = to_id; COMMIT. Тест: перевір що загальна сума не змінилась.', ru:'Реализуй транзакцию перевода средств: PL/pgSQL функция transfer_money(from_id INT, to_id INT, amount NUMERIC). BEGIN; проверь баланс — если from_account.balance < amount, RAISE EXCEPTION \'Insufficient funds\'; UPDATE accounts SET balance = balance - amount WHERE id = from_id; UPDATE accounts SET balance = balance + amount WHERE id = to_id; COMMIT. Тест: проверь что общая сумма не изменилась.' },
      { level:'hard',   uk:'Досліджи рівні ізоляції транзакцій у PostgreSQL: відтвори аномалії "non-repeatable read" (READ COMMITTED) і "phantom read" через дві паралельні сесії psql. Для кожної: покажи SQL двох сесій, поясни аномалію. Продемонструй що SERIALIZABLE запобігає обом аномаліям, але генерує serialization error — покажи як обробляти його у коді через retry.', ru:'Исследуй уровни изоляции транзакций в PostgreSQL: воспроизведи аномалии "non-repeatable read" (READ COMMITTED) и "phantom read" через две параллельные сессии psql. Для каждой: покажи SQL двух сессий, объясни аномалию. Продемонстрируй что SERIALIZABLE предотвращает обе аномалии, но генерирует serialization error — покажи как обрабатывать его в коде через retry.' },
      { level:'hard',   uk:'Реалізуй SAVEPOINT для часткового відкоту при обробці замовлення: PL/pgSQL процедура process_order(order_id). SAVEPOINT after_order_insert; вставка у orders → SAVEPOINT after_items_insert; вставка order_items → SAVEPOINT after_stock_update; UPDATE products SET stock = stock - qty. При помилці на будь-якому кроці — ROLLBACK TO відповідного SAVEPOINT і запис у таблицю order_errors.', ru:'Реализуй SAVEPOINT для частичного отката при обработке заказа: PL/pgSQL процедура process_order(order_id). SAVEPOINT after_order_insert; вставка в orders → SAVEPOINT after_items_insert; вставка order_items → SAVEPOINT after_stock_update; UPDATE products SET stock = stock - qty. При ошибке на любом шаге — ROLLBACK TO соответствующего SAVEPOINT и запись в таблицу order_errors.' },
      { level:'extra',  uk:'Реалізуй і порівняй два підходи до конкурентних оновлень: 1) оптимістичне блокування — поле version INT у products, UPDATE ... WHERE id=? AND version=?, якщо 0 рядків — ConflictError; 2) песимістичне — SELECT ... FOR UPDATE (або FOR UPDATE SKIP LOCKED для черги). Виміряй throughput обох підходів через pgbench із 50 паралельними з\'єднаннями при 100 оновленнях одного рядку.', ru:'Реализуй и сравни два подхода к конкурентным обновлениям: 1) оптимистичная блокировка — поле version INT в products, UPDATE ... WHERE id=? AND version=?, если 0 строк — ConflictError; 2) пессимистичная — SELECT ... FOR UPDATE (или FOR UPDATE SKIP LOCKED для очереди). Измерь throughput обоих подходов через pgbench с 50 параллельными соединениями при 100 обновлениях одной строки.' },
    ],
    '08-07': [
      { level:'medium', uk:'Встанови PostgreSQL 16, створи роль app_user з паролем і базу blog_db. Через psql виконай: \\ l (список БД), \\du (ролі), \\dt (таблиці), \\d articles (структура). Напиши bash-скрипт db_backup.sh: pg_dump -Fc blog_db > backup_$(date +%Y%m%d_%H%M%S).dump і відновлення pg_restore -d blog_db backup.dump. Перевір автоматизацію через cron.', ru:'Установи PostgreSQL 16, создай роль app_user с паролем и базу blog_db. Через psql выполни: \\l (список БД), \\du (роли), \\dt (таблицы), \\d articles (структура). Напиши bash-скрипт db_backup.sh: pg_dump -Fc blog_db > backup_$(date +%Y%m%d_%H%M%S).dump и восстановление pg_restore -d blog_db backup.dump. Проверь автоматизацию через cron.' },
      { level:'hard',   uk:'Налаштуй пул з\'єднань через pgBouncer: встанови, налаштуй pgbouncer.ini (pool_mode=transaction, max_client_conn=100, default_pool_size=10, server_idle_timeout=600). Порівняй throughput прямого з\'єднання vs pgBouncer через pgbench -c 50 -j 4 -T 30 -S. Налаштуй log_min_duration_statement=100 у postgresql.conf і проаналізуй повільні запити через pg_stat_statements.', ru:'Настрой пул соединений через pgBouncer: установи, настрой pgbouncer.ini (pool_mode=transaction, max_client_conn=100, default_pool_size=10, server_idle_timeout=600). Сравни throughput прямого соединения vs pgBouncer через pgbench -c 50 -j 4 -T 30 -S. Настрой log_min_duration_statement=100 в postgresql.conf и проанализируй медленные запросы через pg_stat_statements.' },
      { level:'hard',   uk:'Налаштуй PostgreSQL для production: 1) postgresql.conf — shared_buffers=\'256MB\', effective_cache_size=\'768MB\', work_mem=\'16MB\', max_connections=100; 2) pg_hba.conf — md5 авторизація тільки з 10.0.0.0/8; 3) CREATE SCHEMA audit із таблицею audit_log (table_name, operation, old_data JSONB, new_data JSONB, changed_by, changed_at); 4) увімкни pg_stat_statements і відстеж топ-10 повільних запитів.', ru:'Настрой PostgreSQL для production: 1) postgresql.conf — shared_buffers=\'256MB\', effective_cache_size=\'768MB\', work_mem=\'16MB\', max_connections=100; 2) pg_hba.conf — md5 авторизация только с 10.0.0.0/8; 3) CREATE SCHEMA audit с таблицей audit_log (table_name, operation, old_data JSONB, new_data JSONB, changed_by, changed_at); 4) включи pg_stat_statements и отслеживай топ-10 медленных запросов.' },
      { level:'extra',  uk:'Реалізуй streaming replication PostgreSQL: primary (wal_level=replica, max_wal_senders=3, wal_keep_size=\'64MB\') і replica (primary_conninfo у postgresql.auto.conf). Перевір лаг через pg_stat_replication на primary і pg_is_in_recovery() на replica. Реалізуй failover-скрипт promote_replica.sh, що викликає pg_promote() і оновлює конфіг застосунку. Виміряй replication lag під навантаженням.', ru:'Реализуй streaming replication PostgreSQL: primary (wal_level=replica, max_wal_senders=3, wal_keep_size=\'64MB\') и replica (primary_conninfo в postgresql.auto.conf). Проверь лаг через pg_stat_replication на primary и pg_is_in_recovery() на replica. Реализуй failover-скрипт promote_replica.sh, который вызывает pg_promote() и обновляет конфиг приложения. Измерь replication lag под нагрузкой.' },
    ],
    '08-08': [
      { level:'medium', uk:'Реалізуй SQLAlchemy ORM для блогу: моделі User, Article (ForeignKey до User), Category, та M2M через association table article_categories. Налаштуй engine і SessionLocal. Напиши CRUD-функції: create_article(db, title, body, user_id), get_articles_by_author(db, user_id), update_article_status(db, article_id, status), delete_article(db, article_id). Перевір через скрипт із seed-даними.', ru:'Реализуй SQLAlchemy ORM для блога: модели User, Article (ForeignKey к User), Category и M2M через association table article_categories. Настрой engine и SessionLocal. Напиши CRUD-функции: create_article(db, title, body, user_id), get_articles_by_author(db, user_id), update_article_status(db, article_id, status), delete_article(db, article_id). Проверь через скрипт с seed-данными.' },
      { level:'hard',   uk:'Порівняй стратегії завантаження у SQLAlchemy: увімкни echo=True і виміряй кількість SQL-запитів при отриманні 10 статей із авторами і тегами для: 1) lazy (default); 2) selectin; 3) joined; 4) subquery. Зафіксуй результати. Реалізуй оптимальний запит "статті з автором і тегами" через joinedload(Article.author) + selectinload(Article.tags) уникаючи N+1.', ru:'Сравни стратегии загрузки в SQLAlchemy: включи echo=True и измерь количество SQL-запросов при получении 10 статей с авторами и тегами для: 1) lazy (default); 2) selectin; 3) joined; 4) subquery. Зафиксируй результаты. Реализуй оптимальный запрос "статьи с автором и тегами" через joinedload(Article.author) + selectinload(Article.tags) избегая N+1.' },
      { level:'hard',   uk:'Реалізуй repository pattern із SQLAlchemy 2.x: базовий Generic[ModelT] клас BaseRepository із методами get(id) -> ModelT | None, list(skip, limit, **filters) -> list[ModelT], create(data: dict) -> ModelT, update(id, data: dict) -> ModelT, delete(id) -> bool. Успадкуй ArticleRepository із методами get_published(limit) і search(query: str). Застосуй dependency injection через FastAPI або Django view.', ru:'Реализуй repository pattern с SQLAlchemy 2.x: базовый Generic[ModelT] класс BaseRepository с методами get(id) -> ModelT | None, list(skip, limit, **filters) -> list[ModelT], create(data: dict) -> ModelT, update(id, data: dict) -> ModelT, delete(id) -> bool. Унаследуй ArticleRepository с методами get_published(limit) и search(query: str). Примени dependency injection через FastAPI или Django view.' },
      { level:'extra',  uk:'Збудуй event sourcing із SQLAlchemy: замість прямих UPDATE/DELETE зберігай незмінні події у таблиці domain_events (id UUID, aggregate_id, aggregate_type, event_type, payload JSONB, version INT, created_at). Реалізуй rebuild_state(aggregate_id) через replay подій. Додай snapshot кожні 10 подій (таблиця snapshots: aggregate_id, state JSONB, version). Напиши тести: rebuild_state після 25 подій повертає коректний стан.', ru:'Построй event sourcing с SQLAlchemy: вместо прямых UPDATE/DELETE сохраняй неизменяемые события в таблице domain_events (id UUID, aggregate_id, aggregate_type, event_type, payload JSONB, version INT, created_at). Реализуй rebuild_state(aggregate_id) через replay событий. Добавь snapshot каждые 10 событий (таблица snapshots: aggregate_id, state JSONB, version). Напиши тесты: rebuild_state после 25 событий возвращает корректное состояние.' },
    ],
    '08-09': [
      { level:'medium', uk:'Реалізуй декоратор @redis_cache(ttl=300, prefix="cache") через redis-py, що кешує результат функції у Redis. Ключ: f"{prefix}:{func.__name__}:{hash(str(args)+str(kwargs))}". При cache-hit — поверни десеріалізований JSON, при miss — виконай функцію, серіалізуй і збережи через setex(). Реалізуй також cache.invalidate_prefix(prefix) через scan_iter і delete.', ru:'Реализуй декоратор @redis_cache(ttl=300, prefix="cache") через redis-py, который кеширует результат функции в Redis. Ключ: f"{prefix}:{func.__name__}:{hash(str(args)+str(kwargs))}". При cache-hit — верни десериализованный JSON, при miss — выполни функцию, сериализуй и сохрани через setex(). Реализуй также cache.invalidate_prefix(prefix) через scan_iter и delete.' },
      { level:'hard',   uk:'Побудуй систему pub/sub для подій між мікросервісами: EventPublisher публікує у канали article:created, order:placed, user:registered через redis.publish(channel, json.dumps(event)). EventSubscriber слухає через pubsub.subscribe() у окремому потоці і маршрутизує до зареєстрованих обробників (handlers dict). Тест: publisher у процесі A → subscriber у процесі B отримує повідомлення.', ru:'Построй систему pub/sub для событий между микросервисами: EventPublisher публикует в каналы article:created, order:placed, user:registered через redis.publish(channel, json.dumps(event)). EventSubscriber слушает через pubsub.subscribe() в отдельном потоке и маршрутизирует к зарегистрированным обработчикам (handlers dict). Тест: publisher в процессе A → subscriber в процессе B получает сообщение.' },
      { level:'hard',   uk:'Реалізуй лідерборд через Redis sorted sets: zadd("lb:weekly", {user_id: score}) для оновлення балів, zincrby для додавання очок, zrevrange("lb:weekly", 0, 9, withscores=True) для топ-10, zrevrank для позиції гравця, zcard для загальної кількості учасників. Побудуй REST API: GET /api/leaderboard/top10/ і GET /api/leaderboard/me/ (позиція і бали поточного user).', ru:'Реализуй лидерборд через Redis sorted sets: zadd("lb:weekly", {user_id: score}) для обновления баллов, zincrby для добавления очков, zrevrange("lb:weekly", 0, 9, withscores=True) для топ-10, zrevrank для позиции игрока, zcard для общего количества участников. Построй REST API: GET /api/leaderboard/top10/ и GET /api/leaderboard/me/ (позиция и баллы текущего user).' },
      { level:'extra',  uk:'Реалізуй три просунуті Redis-патерни: 1) HyperLogLog — pfadd(f"uv:{page_id}:{date}", user_id) і pfcount() для унікальних переглядів без дублів (порівняй точність і пам\'ять vs SET); 2) Sliding window rate limiter — zadd/zremrangebyscore/zcard для 100 req/60s на user; 3) Distributed lock — SET key NX EX 30 для атомарного виконання критичної секції з auto-release і retry. Покрий тестами кожен патерн.', ru:'Реализуй три продвинутых Redis-паттерна: 1) HyperLogLog — pfadd(f"uv:{page_id}:{date}", user_id) и pfcount() для уникальных просмотров без дублей (сравни точность и память vs SET); 2) Sliding window rate limiter — zadd/zremrangebyscore/zcard для 100 req/60s на user; 3) Distributed lock — SET key NX EX 30 для атомарного выполнения критической секции с auto-release и retry. Покрой тестами каждый паттерн.' },
    ],
    '08-10': [
      { level:'medium', uk:'Спроєктуй ER-діаграму і реалізуй DDL для Gym Tracker: users (id, name, email, goal ENUM, fitness_level ENUM), exercises (id, name, muscle_group, equipment, type ENUM: strength/cardio), workouts (id, user_id FK, date, duration_min, notes), workout_exercises (workout_id FK, exercise_id FK, sets INT, reps INT, weight_kg NUMERIC, duration_sec INT). Додай всі FK, UNIQUE (workout_id, exercise_id) і CHECK (sets > 0).', ru:'Спроектируй ER-диаграмму и реализуй DDL для Gym Tracker: users (id, name, email, goal ENUM, fitness_level ENUM), exercises (id, name, muscle_group, equipment, type ENUM: strength/cardio), workouts (id, user_id FK, date, duration_min, notes), workout_exercises (workout_id FK, exercise_id FK, sets INT, reps INT, weight_kg NUMERIC, duration_sec INT). Добавь все FK, UNIQUE (workout_id, exercise_id) и CHECK (sets > 0).' },
      { level:'hard',   uk:'Виконай нормалізацію схеми Gym Tracker від ненормалізованої до 3NF: починай з плоскої таблиці gym_data (user_name, user_email, workout_date, exercise_name, muscle_group, sets, reps, weight_kg). Крок 1NF → 2NF → 3NF: для кожного кроку напиши CREATE TABLE, перелік порушень і INSERT ... SELECT для перенесення даних зі старої таблиці.', ru:'Выполни нормализацию схемы Gym Tracker от ненормализованной до 3NF: начинай с плоской таблицы gym_data (user_name, user_email, workout_date, exercise_name, muscle_group, sets, reps, weight_kg). Шаг 1NF → 2NF → 3NF: для каждого шага напиши CREATE TABLE, список нарушений и INSERT ... SELECT для переноса данных из старой таблицы.' },
      { level:'hard',   uk:'Реалізуй аналітичні запити для Gym Tracker: 1) прогрес по максимальній вазі для конкретного exercises за 3 місяці із LAG() OVER (PARTITION BY exercise_id ORDER BY date) для порівняння з попереднім тренуванням; 2) топ-5 найчастіших вправ користувача за останні 30 днів; 3) streak — кількість послідовних днів тренувань через рекурсивний CTE або оконні функції. Додай потрібні індекси.', ru:'Реализуй аналитические запросы для Gym Tracker: 1) прогресс по максимальному весу для конкретного exercises за 3 месяца с LAG() OVER (PARTITION BY exercise_id ORDER BY date) для сравнения с предыдущей тренировкой; 2) топ-5 самых частых упражнений пользователя за последние 30 дней; 3) streak — количество последовательных дней тренировок через рекурсивный CTE или оконные функции. Добавь нужные индексы.' },
      { level:'extra',  uk:'Реалізуй аналітичну схему (star schema) для Gym Tracker: fact_workout_exercises (денормалізована, для OLAP) і виміри dim_user, dim_exercise (із muscle_group_category), dim_date (day, week_num, month, quarter, year, is_weekend, is_holiday). Напиши ETL-скрипт Python (SQLAlchemy), що переносить дані з OLTP у DWH щоночі. Реалізуй аналітичний запит: обсяг навантаження (sets * reps * weight) по місяцях із rolling 3-місячним середнім.', ru:'Реализуй аналитическую схему (star schema) для Gym Tracker: fact_workout_exercises (денормализованная, для OLAP) и измерения dim_user, dim_exercise (с muscle_group_category), dim_date (day, week_num, month, quarter, year, is_weekend, is_holiday). Напиши ETL-скрипт Python (SQLAlchemy), который переносит данные из OLTP в DWH каждую ночь. Реализуй аналитический запрос: объём нагрузки (sets * reps * weight) по месяцам с rolling 3-месячным средним.' },
    ],
  };
  Object.keys(EXT).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.tasks = (l.tasks || []).concat(EXT[id]);
  });
})();
