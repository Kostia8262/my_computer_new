/* Питання самоконтролю · Модуль 06 · 14-18 Фулстек-Про */
(function () {
  'use strict';
  var QUIZ = {
    '06-01': {
      uk: [
        { q: "Що таке application context у Flask і навіщо він потрібен?", a: "Application context — це об'єкт, що зберігає стан додатку під час обробки запиту або команди CLI (g, current_app). Flask автоматично проштовхує app context при кожному запиті. Поза запитом (наприклад у скриптах) його створюють вручну через with app.app_context(). Без нього звернення до current_app або g викликає RuntimeError." },
        { q: "Яка різниця між декоратором @app.route() та методом add_url_rule() у Flask?", a: "@app.route('/path', methods=['GET','POST']) — синтаксичний цукор, що під капотом викликає app.add_url_rule('/path', view_func=func, methods=[...]). add_url_rule() корисний при програматичній реєстрації маршрутів або у розширеннях. Обидва підходи реєструють URL-правило в URL map Flask." },
        { q: "Що таке WSGI і чим Flask-development сервер відрізняється від production WSGI-серверів (Gunicorn, uWSGI)?", a: "WSGI (Web Server Gateway Interface) — стандарт Python для взаємодії між вебсервером та застосунком: сервер викликає callable(environ, start_response). Flask-dev сервер однопотоковий, не підходить для навантаження і не має продакшн-надійності. Gunicorn запускає кілька worker-процесів/потоків, обробляє тисячі запитів одночасно і підходить для продакшену." },
      ],
      ru: [
        { q: "Что такое application context во Flask и зачем он нужен?", a: "Application context — объект, хранящий состояние приложения во время обработки запроса или команды CLI (g, current_app). Flask автоматически помещает app context при каждом запросе. Вне запроса (например в скриптах) его создают вручную через with app.app_context(). Без него обращение к current_app или g вызывает RuntimeError." },
        { q: "В чём разница между декоратором @app.route() и методом add_url_rule() во Flask?", a: "@app.route('/path', methods=['GET','POST']) — синтаксический сахар, под капотом вызывающий app.add_url_rule('/path', view_func=func, methods=[...]). add_url_rule() полезен при программатической регистрации маршрутов или в расширениях. Оба подхода регистрируют URL-правило в URL map Flask." },
        { q: "Что такое WSGI и чем Flask-development сервер отличается от production WSGI-серверов (Gunicorn, uWSGI)?", a: "WSGI (Web Server Gateway Interface) — стандарт Python для взаимодействия между веб-сервером и приложением: сервер вызывает callable(environ, start_response). Flask-dev сервер однопоточный, не подходит для нагрузки и не имеет продакшн-надёжности. Gunicorn запускает несколько worker-процессов/потоков, обрабатывает тысячи запросов одновременно и подходит для продакшена." },
      ]
    },
    '06-02': {
      uk: [
        { q: "Як {% extends %} та {% block %} реалізують механізм наслідування шаблонів у Jinja2?", a: "{% extends 'base.html' %} вказує, що поточний шаблон є дочірнім. base.html визначає {% block content %}{% endblock %} — іменовані зони. Дочірній шаблон перевизначає ці блоки: {% block content %}...{% endblock %}. Jinja2 рендерить базовий шаблон, підставляючи вміст блоків з дочірнього. {{ super() }} вставляє вміст батьківського блоку." },
        { q: "Що таке макрос у Jinja2 і яка його перевага над простим include?", a: "Макрос — це функція у шаблоні: {% macro input(name, type='text') %}<input name=\"{{name}}\" type=\"{{type}}\">{% endmacro %}. На відміну від include, макрос приймає аргументи і дозволяє передавати різні параметри при кожному виклику: {{ input('email', 'email') }}. Це усуває дублювання HTML-фрагментів з різними значеннями." },
        { q: "Як фільтри Jinja2 застосовуються до змінних і як написати кастомний фільтр у Flask?", a: "Фільтр застосовується через pipe: {{ name|upper }}, {{ price|round(2) }}. Ланцюжок: {{ text|trim|lower }}. Кастомний фільтр реєструється через: @app.template_filter('myfilter') def my_filter(value): return ... або app.jinja_env.filters['myfilter'] = my_filter. Тоді у шаблоні: {{ value|myfilter }}." },
      ],
      ru: [
        { q: "Как {% extends %} и {% block %} реализуют механизм наследования шаблонов в Jinja2?", a: "{% extends 'base.html' %} указывает, что текущий шаблон является дочерним. base.html определяет {% block content %}{% endblock %} — именованные зоны. Дочерний шаблон переопределяет эти блоки: {% block content %}...{% endblock %}. Jinja2 рендерит базовый шаблон, подставляя содержимое блоков из дочернего. {{ super() }} вставляет содержимое родительского блока." },
        { q: "Что такое макрос в Jinja2 и в чём его преимущество над обычным include?", a: "Макрос — это функция в шаблоне: {% macro input(name, type='text') %}<input name=\"{{name}}\" type=\"{{type}}\">{% endmacro %}. В отличие от include, макрос принимает аргументы и позволяет передавать разные параметры при каждом вызове: {{ input('email', 'email') }}. Это устраняет дублирование HTML-фрагментов с разными значениями." },
        { q: "Как фильтры Jinja2 применяются к переменным и как написать кастомный фильтр во Flask?", a: "Фильтр применяется через pipe: {{ name|upper }}, {{ price|round(2) }}. Цепочка: {{ text|trim|lower }}. Кастомный фильтр регистрируется через: @app.template_filter('myfilter') def my_filter(value): return ... или app.jinja_env.filters['myfilter'] = my_filter. Затем в шаблоне: {{ value|myfilter }}." },
      ]
    },
    '06-03': {
      uk: [
        { q: "Що таке CSRF-атака і як Flask-WTF захищає від неї?", a: "CSRF (Cross-Site Request Forgery) — зловмисник змушує браузер жертви надіслати підроблений запит від її імені. Flask-WTF генерує унікальний CSRF-токен, вбудований у форму як прихований input. При POST Flask-WTF перевіряє токен з сесії — підроблений запит без токену буде відхилено з помилкою 400. SECRET_KEY Flask використовується для підпису токену." },
        { q: "Як WTForms виконує валідацію на рівні поля і що таке кастомний валідатор?", a: "Кожне поле WTForms приймає validators=[...]: DataRequired(), Email(), Length(min=3, max=50). При form.validate_on_submit() або form.validate() перевіряються всі поля по порядку. Кастомний валідатор — метод у класі форми: def validate_username(self, field): if User.query.filter_by(username=field.data).first(): raise ValidationError('Username taken')." },
        { q: "Як у Flask правильно обробити форму WTForms у маршруті, що обробляє і GET, і POST?", a: "Маршрут реєструється з methods=['GET', 'POST']. При GET: form = MyForm(), render_template('page.html', form=form). При POST: form = MyForm() (WTF автоматично ініціалізує з request.form), if form.validate_on_submit(): обробка -> redirect. Якщо не валідна — render_template знову з помилками у form.errors або field.errors." },
      ],
      ru: [
        { q: "Что такое CSRF-атака и как Flask-WTF защищает от неё?", a: "CSRF (Cross-Site Request Forgery) — злоумышленник вынуждает браузер жертвы отправить поддельный запрос от её имени. Flask-WTF генерирует уникальный CSRF-токен, встроенный в форму как скрытый input. При POST Flask-WTF проверяет токен из сессии — поддельный запрос без токена будет отклонён с ошибкой 400. SECRET_KEY Flask используется для подписи токена." },
        { q: "Как WTForms выполняет валидацию на уровне поля и что такое кастомный валидатор?", a: "Каждое поле WTForms принимает validators=[...]: DataRequired(), Email(), Length(min=3, max=50). При form.validate_on_submit() или form.validate() проверяются все поля по порядку. Кастомный валидатор — метод в классе формы: def validate_username(self, field): if User.query.filter_by(username=field.data).first(): raise ValidationError('Username taken')." },
        { q: "Как во Flask правильно обработать форму WTForms в маршруте, обрабатывающем и GET, и POST?", a: "Маршрут регистрируется с methods=['GET', 'POST']. При GET: form = MyForm(), render_template('page.html', form=form). При POST: form = MyForm() (WTF автоматически инициализируется из request.form), if form.validate_on_submit(): обработка -> redirect. Если не валидна — render_template снова с ошибками в form.errors или field.errors." },
      ]
    },
    '06-04': {
      uk: [
        { q: "Як оголосити відношення один-до-багатьох у Flask-SQLAlchemy і що таке backref?", a: "У моделі один-до-багатьох: у дочірній моделі — ForeignKey: post_id = db.Column(db.Integer, db.ForeignKey('post.id')). У батьківській моделі — relationship: comments = db.relationship('Comment', backref='post', lazy=True). backref автоматично додає атрибут post до Comment-об'єктів, через який можна дістатись батьківського Post." },
        { q: "Що таке lazy loading у SQLAlchemy і яка різниця між lazy='select', lazy='joined' та lazy='dynamic'?", a: "lazy='select' (за замовчуванням) — пов'язані об'єкти завантажуються окремим SELECT-запитом при першому зверненні (N+1 проблема). lazy='joined' — використовує JOIN і завантажує пов'язані об'єкти у тому ж запиті. lazy='dynamic' — повертає BaseQuery замість списку, дозволяючи додаткову фільтрацію: user.posts.filter_by(published=True).all()." },
        { q: "Як виконати складний запит з фільтрацією, сортуванням та пагінацією у Flask-SQLAlchemy?", a: "posts = Post.query.filter(Post.published==True, Post.author_id==user_id).order_by(Post.created_at.desc()).paginate(page=page, per_page=10, error_out=False). paginate() повертає об'єкт Pagination з атрибутами items (список), pages, has_next, has_prev, next_num — зручно для побудови навігації у шаблоні." },
      ],
      ru: [
        { q: "Как объявить отношение один-ко-многим в Flask-SQLAlchemy и что такое backref?", a: "В модели один-ко-многим: в дочерней модели — ForeignKey: post_id = db.Column(db.Integer, db.ForeignKey('post.id')). В родительской модели — relationship: comments = db.relationship('Comment', backref='post', lazy=True). backref автоматически добавляет атрибут post к объектам Comment, через который можно добраться до родительского Post." },
        { q: "Что такое lazy loading в SQLAlchemy и в чём разница между lazy='select', lazy='joined' и lazy='dynamic'?", a: "lazy='select' (по умолчанию) — связанные объекты загружаются отдельным SELECT-запросом при первом обращении (N+1 проблема). lazy='joined' — использует JOIN и загружает связанные объекты в том же запросе. lazy='dynamic' — возвращает BaseQuery вместо списка, позволяя дополнительную фильтрацию: user.posts.filter_by(published=True).all()." },
        { q: "Как выполнить сложный запрос с фильтрацией, сортировкой и пагинацией в Flask-SQLAlchemy?", a: "posts = Post.query.filter(Post.published==True, Post.author_id==user_id).order_by(Post.created_at.desc()).paginate(page=page, per_page=10, error_out=False). paginate() возвращает объект Pagination с атрибутами items (список), pages, has_next, has_prev, next_num — удобно для построения навигации в шаблоне." },
      ]
    },
    '06-05': {
      uk: [
        { q: "Що таке міграція бази даних і яку проблему вирішують Flask-Migrate та Alembic?", a: "Міграція — версійований набір змін схеми БД (ALTER TABLE, CREATE INDEX тощо). Flask-Migrate (обгортка над Alembic) відстежує поточний стан моделей SQLAlchemy і генерує скрипти міграцій, що послідовно трансформують схему. Це дозволяє оновлювати продакшн БД без ручного SQL і відкочуватись при необхідності (flask db downgrade)." },
        { q: "Яка послідовність команд Flask-Migrate для додавання нового поля до існуючої моделі?", a: "1) Додати поле до моделі SQLAlchemy у коді. 2) flask db migrate -m 'add bio to user' — Alembic порівнює поточну схему БД з моделями і генерує файл міграції у migrations/versions/. 3) Переглянути згенерований файл (upgrade/downgrade функції). 4) flask db upgrade — застосовує міграцію до БД. Flask db history показує список міграцій." },
        { q: "Чому Alembic не завжди автоматично виявляє всі зміни моделей і що потребує ручного редагування міграції?", a: "Alembic виявляє зміни колонок, таблиць та індексів, але не завжди виявляє: зміну обмежень (CHECK constraints) у деяких БД, зміну серверних default-значень, зміну CollationSequence, та складні трансформації даних. Такі зміни потребують ручного додавання SQL у функції upgrade()/downgrade() файлу міграції або використання op.execute()." },
      ],
      ru: [
        { q: "Что такое миграция базы данных и какую проблему решают Flask-Migrate и Alembic?", a: "Миграция — версионированный набор изменений схемы БД (ALTER TABLE, CREATE INDEX и т.д.). Flask-Migrate (обёртка над Alembic) отслеживает текущее состояние моделей SQLAlchemy и генерирует скрипты миграций, последовательно трансформирующие схему. Это позволяет обновлять продакшн БД без ручного SQL и откатываться при необходимости (flask db downgrade)." },
        { q: "Какова последовательность команд Flask-Migrate для добавления нового поля к существующей модели?", a: "1) Добавить поле в модель SQLAlchemy в коде. 2) flask db migrate -m 'add bio to user' — Alembic сравнивает текущую схему БД с моделями и генерирует файл миграции в migrations/versions/. 3) Просмотреть сгенерированный файл (функции upgrade/downgrade). 4) flask db upgrade — применяет миграцию к БД. flask db history показывает список миграций." },
        { q: "Почему Alembic не всегда автоматически обнаруживает все изменения моделей и что требует ручного редактирования миграции?", a: "Alembic обнаруживает изменения колонок, таблиц и индексов, но не всегда обнаруживает: изменение ограничений (CHECK constraints) в некоторых БД, изменение серверных default-значений, изменение CollationSequence, и сложные трансформации данных. Такие изменения требуют ручного добавления SQL в функции upgrade()/downgrade() файла миграции или использования op.execute()." },
      ]
    },
    '06-06': {
      uk: [
        { q: "Як jsonify відрізняється від json.dumps() у контексті Flask REST API?", a: "json.dumps() повертає рядок. jsonify() повертає об'єкт Response Flask з тілом у форматі JSON та заголовком Content-Type: application/json. jsonify також підтримує спеціальні типи (datetime, UUID через кастомні encoder'и). Ніколи не повертайте json.dumps() напряму — Flask не встановить правильний Content-Type." },
        { q: "Що таке Blueprint у Flask і як він допомагає структурувати REST API?", a: "Blueprint — це об'єкт, що дозволяє реєструвати маршрути, шаблони та статику незалежно, а потім реєструвати у додатку: app.register_blueprint(users_bp, url_prefix='/api/users'). Blueprint групує маршрути за функціональністю (users, posts, auth), спрощує розбиття на модулі, дозволяє окреме тестування та повторне використання у різних додатках." },
        { q: "Як правильно повертати відповіді з різними HTTP-статус кодами у Flask-маршрутах?", a: "Flask дозволяє кілька варіантів: return jsonify(data), 201 — кортеж (body, status). return make_response(jsonify(data), 201) — явний об'єкт Response, зручний для встановлення заголовків. При помилці: abort(404) або return jsonify({'error': 'Not found'}), 404. Обробник помилок: @app.errorhandler(404) def not_found(e): return jsonify(error=str(e)), 404." },
      ],
      ru: [
        { q: "Как jsonify отличается от json.dumps() в контексте Flask REST API?", a: "json.dumps() возвращает строку. jsonify() возвращает объект Response Flask с телом в формате JSON и заголовком Content-Type: application/json. jsonify также поддерживает специальные типы (datetime, UUID через кастомные encoder'ы). Никогда не возвращайте json.dumps() напрямую — Flask не установит правильный Content-Type." },
        { q: "Что такое Blueprint во Flask и как он помогает структурировать REST API?", a: "Blueprint — объект, позволяющий регистрировать маршруты, шаблоны и статику независимо, а затем регистрировать в приложении: app.register_blueprint(users_bp, url_prefix='/api/users'). Blueprint группирует маршруты по функциональности (users, posts, auth), упрощает разбиение на модули, позволяет отдельное тестирование и повторное использование в разных приложениях." },
        { q: "Как правильно возвращать ответы с разными HTTP-статус кодами в Flask-маршрутах?", a: "Flask допускает несколько вариантов: return jsonify(data), 201 — кортеж (body, status). return make_response(jsonify(data), 201) — явный объект Response, удобный для установки заголовков. При ошибке: abort(404) или return jsonify({'error': 'Not found'}), 404. Обработчик ошибок: @app.errorhandler(404) def not_found(e): return jsonify(error=str(e)), 404." },
      ]
    },
    '06-07': {
      uk: [
        { q: "Що таке UserMixin у Flask-Login і які методи він надає моделі користувача?", a: "UserMixin — міксин, що надає реалізацію за замовчуванням для чотирьох методів, необхідних Flask-Login: is_authenticated (True якщо залогінений), is_active (True якщо акаунт активний), is_anonymous (False), get_id() (повертає рядковий ідентифікатор). Без UserMixin ці методи потрібно реалізувати вручну в моделі User." },
        { q: "Як decorator @login_required захищає маршрут і що відбувається при спробі неавторизованого доступу?", a: "@login_required перевіряє current_user.is_authenticated. Якщо False — перенаправляє на login_manager.login_view з параметром next (URL оригінальної сторінки): redirect(url_for('auth.login', next=request.url)). Після логіну redirect(request.args.get('next') or url_for('main.index')) повертає користувача на початкову сторінку." },
        { q: "Як Flask-Login зберігає стан аутентифікації між запитами і що зберігається у сесії?", a: "Flask-Login зберігає user_id у зашифрованій Cookie-сесії (підписана SECRET_KEY). При кожному запиті Flask-Login читає user_id з сесії і викликає user_loader: @login_manager.user_loader def load_user(id): return User.query.get(int(id)). Завантажений об'єкт стає current_user, доступним у маршрутах та шаблонах." },
      ],
      ru: [
        { q: "Что такое UserMixin во Flask-Login и какие методы он предоставляет модели пользователя?", a: "UserMixin — миксин, предоставляющий реализацию по умолчанию для четырёх методов, необходимых Flask-Login: is_authenticated (True если залогинен), is_active (True если аккаунт активен), is_anonymous (False), get_id() (возвращает строковый идентификатор). Без UserMixin эти методы нужно реализовывать вручную в модели User." },
        { q: "Как декоратор @login_required защищает маршрут и что происходит при попытке несанкционированного доступа?", a: "@login_required проверяет current_user.is_authenticated. Если False — перенаправляет на login_manager.login_view с параметром next (URL исходной страницы): redirect(url_for('auth.login', next=request.url)). После логина redirect(request.args.get('next') or url_for('main.index')) возвращает пользователя на исходную страницу." },
        { q: "Как Flask-Login хранит состояние аутентификации между запросами и что хранится в сессии?", a: "Flask-Login хранит user_id в зашифрованной Cookie-сессии (подписана SECRET_KEY). При каждом запросе Flask-Login читает user_id из сессии и вызывает user_loader: @login_manager.user_loader def load_user(id): return User.query.get(int(id)). Загруженный объект становится current_user, доступным в маршрутах и шаблонах." },
      ]
    },
    '06-08': {
      uk: [
        { q: "Яка ключова перевага Flask-JWT-Extended над Flask-Login для API-аутентифікації?", a: "Flask-JWT-Extended реалізує stateless-аутентифікацію через JWT: токен містить всю необхідну інформацію (user_id, roles), сервер не зберігає сесії. Це ідеально для REST API, де клієнти — мобільні додатки або SPA. Flask-Login використовує серверні сесії (cookie), підходить для server-rendered веб-сайтів." },
        { q: "Як Flask-JWT-Extended реалізує пару access + refresh token і як відбувається оновлення токенів?", a: "При логіні: access_token = create_access_token(identity=user_id), refresh_token = create_refresh_token(identity=user_id). Endpoint /refresh захищений @jwt_required(refresh=True) — приймає лише refresh token. Він видає новий access token: create_access_token(identity=get_jwt_identity()). Refresh token живе довше (дні), access token коротко (хвилини)." },
        { q: "Як захистити маршрут Flask-JWT-Extended і як отримати identity поточного користувача?", a: "Маршрут захищається декоратором @jwt_required(). Декоратор перевіряє заголовок Authorization: Bearer <token>. Після перевірки identity отримують через get_jwt_identity() (повертає значення передане в create_access_token(identity=...)). Додаткові claims JWT доступні через get_jwt() — повертає словник з усіма claims токену." },
      ],
      ru: [
        { q: "Какое ключевое преимущество Flask-JWT-Extended над Flask-Login для API-аутентификации?", a: "Flask-JWT-Extended реализует stateless-аутентификацию через JWT: токен содержит всю необходимую информацию (user_id, roles), сервер не хранит сессии. Это идеально для REST API, где клиенты — мобильные приложения или SPA. Flask-Login использует серверные сессии (cookie), подходит для server-rendered веб-сайтов." },
        { q: "Как Flask-JWT-Extended реализует пару access + refresh token и как происходит обновление токенов?", a: "При логине: access_token = create_access_token(identity=user_id), refresh_token = create_refresh_token(identity=user_id). Endpoint /refresh защищён @jwt_required(refresh=True) — принимает только refresh token. Он выдаёт новый access token: create_access_token(identity=get_jwt_identity()). Refresh token живёт дольше (дни), access token коротко (минуты)." },
        { q: "Как защитить маршрут Flask-JWT-Extended и как получить identity текущего пользователя?", a: "Маршрут защищается декоратором @jwt_required(). Декоратор проверяет заголовок Authorization: Bearer <token>. После проверки identity получают через get_jwt_identity() (возвращает значение переданное в create_access_token(identity=...)). Дополнительные claims JWT доступны через get_jwt() — возвращает словарь со всеми claims токена." },
      ]
    },
    '06-09': {
      uk: [
        { q: "Як Blueprint дозволяє уникнути циклічних імпортів у великих Flask-проектах?", a: "Циклічний імпорт виникає коли app.py імпортує models.py, а models.py намагається імпортувати db з app.py. Blueprint вирішує це через розбиття: db = SQLAlchemy() ініціалізується окремо в extensions.py, моделі імпортують db звідти, Blueprint реєструється в create_app() factory. Жоден модуль не імпортує app напряму." },
        { q: "Як application factory pattern (create_app) покращує тестованість Flask-додатку?", a: "create_app(config=None) — функція, що створює і конфігурує Flask-додаток і повертає його. Замість глобального app = Flask(__name__), тести можуть викликати create_app({'TESTING': True, 'DATABASE_URI': 'sqlite:///:memory:'}) і отримувати ізольований екземпляр з тестовою конфігурацією. Кожен тест отримує чистий застосунок без глобального стану." },
        { q: "Як зареєструвати кілька Blueprint з вкладеними URL-префіксами у Flask?", a: "app.register_blueprint(api_bp, url_prefix='/api'). api_bp може містити вкладені blueprint: api_bp.register_blueprint(users_bp, url_prefix='/users'). Результат: маршрути users_bp доступні як /api/users/.... Flask 2.x підтримує вкладені Blueprint нативно. Кожен Blueprint може мати власний url_prefix, помилки та static_folder." },
      ],
      ru: [
        { q: "Как Blueprint позволяет избежать циклических импортов в больших Flask-проектах?", a: "Циклический импорт возникает когда app.py импортирует models.py, а models.py пытается импортировать db из app.py. Blueprint решает это через разбиение: db = SQLAlchemy() инициализируется отдельно в extensions.py, модели импортируют db оттуда, Blueprint регистрируется в create_app() factory. Ни один модуль не импортирует app напрямую." },
        { q: "Как паттерн application factory (create_app) улучшает тестируемость Flask-приложения?", a: "create_app(config=None) — функция, создающая и конфигурирующая Flask-приложение и возвращающая его. Вместо глобального app = Flask(__name__), тесты могут вызывать create_app({'TESTING': True, 'DATABASE_URI': 'sqlite:///:memory:'}) и получать изолированный экземпляр с тестовой конфигурацией. Каждый тест получает чистое приложение без глобального состояния." },
        { q: "Как зарегистрировать несколько Blueprint с вложенными URL-префиксами во Flask?", a: "app.register_blueprint(api_bp, url_prefix='/api'). api_bp может содержать вложенные blueprint: api_bp.register_blueprint(users_bp, url_prefix='/users'). Результат: маршруты users_bp доступны как /api/users/.... Flask 2.x поддерживает вложенные Blueprint нативно. Каждый Blueprint может иметь собственный url_prefix, обработчики ошибок и static_folder." },
      ]
    },
    '06-10': {
      uk: [
        { q: "Що таке Celery і яку архітектурну проблему він вирішує у веб-застосунках?", a: "Celery — розподілена черга задач. Вебсервер не може виконувати тривалі операції (відправка email, генерація PDF, ML-інференс) у межах HTTP-запиту — користувач чекатиме або отримає timeout. Celery виносить такі задачі у фонові worker-процеси через брокер повідомлень (Redis або RabbitMQ), а вебсервер відразу повертає відповідь." },
        { q: "Яка роль брокера (Redis) та бекенду результатів у архітектурі Celery?", a: "Брокер (Redis) — черга повідомлень: вебсервер кладе задачу у чергу, worker забирає і виконує. Бекенд результатів (також Redis або БД) — сховище результатів виконання задач. Клієнт може перевірити статус: task.state, або отримати результат: task.result. Без бекенду результати не зберігаються і task.get() не працює." },
        { q: "Як визначити Celery-задачу і як відкласти її з Flask-маршруту?", a: "@celery.task def send_email(to, subject, body): ... — декоратор реєструє функцію як задачу. У маршруті виклик .delay() або .apply_async() ставить задачу у чергу та негайно повертає AsyncResult: task = send_email.delay('user@example.com', 'Hi', 'Body'). .delay() — скорочення .apply_async(args=...). Задача виконується worker'ом асинхронно." },
      ],
      ru: [
        { q: "Что такое Celery и какую архитектурную проблему он решает в веб-приложениях?", a: "Celery — распределённая очередь задач. Веб-сервер не может выполнять длительные операции (отправка email, генерация PDF, ML-инференс) в рамках HTTP-запроса — пользователь будет ждать или получит timeout. Celery выносит такие задачи в фоновые worker-процессы через брокер сообщений (Redis или RabbitMQ), а веб-сервер сразу возвращает ответ." },
        { q: "Какова роль брокера (Redis) и бекенда результатов в архитектуре Celery?", a: "Брокер (Redis) — очередь сообщений: веб-сервер помещает задачу в очередь, worker забирает и выполняет. Бекенд результатов (также Redis или БД) — хранилище результатов выполнения задач. Клиент может проверить статус: task.state, или получить результат: task.result. Без бекенда результаты не сохраняются и task.get() не работает." },
        { q: "Как определить Celery-задачу и как отложить её из Flask-маршрута?", a: "@celery.task def send_email(to, subject, body): ... — декоратор регистрирует функцию как задачу. В маршруте вызов .delay() или .apply_async() ставит задачу в очередь и немедленно возвращает AsyncResult: task = send_email.delay('user@example.com', 'Hi', 'Body'). .delay() — сокращение .apply_async(args=...). Задача выполняется worker'ом асинхронно." },
      ]
    },
    '06-11': {
      uk: [
        { q: "Як налаштувати Flask-Mail для відправки email через SMTP і які параметри конфігурації є обов'язковими?", a: "Обов'язкові параметри: MAIL_SERVER (напр. 'smtp.gmail.com'), MAIL_PORT (587 для TLS), MAIL_USE_TLS=True, MAIL_USERNAME та MAIL_PASSWORD. Опціонально: MAIL_DEFAULT_SENDER. Ніколи не зберігайте MAIL_PASSWORD у коді — лише через змінні оточення. Для Gmail також потрібен App Password (якщо увімкнено 2FA)." },
        { q: "Як відправити email з HTML-вмістом та альтернативним текстовим варіантом через Flask-Mail?", a: "msg = Message('Subject', recipients=['user@example.com']). msg.body = 'Plain text fallback'. msg.html = '<h1>HTML version</h1>'. mail.send(msg). Надсилання обох частин — best practice: поштові клієнти без HTML-підтримки відображають text/plain, інші — text/html. Flask-Mail формує multipart/alternative MIME-повідомлення автоматично." },
        { q: "Чому відправку email не варто робити синхронно у HTTP-запиті і як це вирішується у продакшені?", a: "SMTP-з'єднання та відправка можуть тривати 1-5 секунд, що збільшує час відповіді API. При проблемах SMTP-сервера запит може зависнути. Рішення: виносити відправку у Celery-задачу (mail.send(msg) викликається у worker'і). Альтернатива — використовувати transactional email API (SendGrid, Mailgun) через HTTP — швидше і надійніше." },
      ],
      ru: [
        { q: "Как настроить Flask-Mail для отправки email через SMTP и какие параметры конфигурации являются обязательными?", a: "Обязательные параметры: MAIL_SERVER (напр. 'smtp.gmail.com'), MAIL_PORT (587 для TLS), MAIL_USE_TLS=True, MAIL_USERNAME и MAIL_PASSWORD. Опционально: MAIL_DEFAULT_SENDER. Никогда не храните MAIL_PASSWORD в коде — только через переменные окружения. Для Gmail также нужен App Password (если включена 2FA)." },
        { q: "Как отправить email с HTML-содержимым и альтернативным текстовым вариантом через Flask-Mail?", a: "msg = Message('Subject', recipients=['user@example.com']). msg.body = 'Plain text fallback'. msg.html = '<h1>HTML version</h1>'. mail.send(msg). Отправка обеих частей — best practice: почтовые клиенты без HTML-поддержки отображают text/plain, другие — text/html. Flask-Mail формирует multipart/alternative MIME-сообщение автоматически." },
        { q: "Почему отправку email не стоит делать синхронно в HTTP-запросе и как это решается в продакшене?", a: "SMTP-соединение и отправка могут занимать 1-5 секунд, увеличивая время ответа API. При проблемах SMTP-сервера запрос может зависнуть. Решение: выносить отправку в Celery-задачу (mail.send(msg) вызывается в worker'е). Альтернатива — использовать transactional email API (SendGrid, Mailgun) через HTTP — быстрее и надёжнее." },
      ]
    },
    '06-12': {
      uk: [
        { q: "Як fixture client у pytest для Flask дозволяє тестувати маршрути без запуску реального сервера?", a: "Fixture: @pytest.fixture def client(): app.config['TESTING'] = True; with app.test_client() as client: yield client. app.test_client() повертає Werkzeug TestClient, який симулює HTTP-запити безпосередньо через WSGI-інтерфейс без мережевого рівня. client.get('/users'), client.post('/login', json={...}) — зручний API для тестів без запуску сервера." },
        { q: "Як у pytest ізолювати тести БД щоб зміни одного тесту не впливали на інший?", a: "Варіанти: 1) Використовувати in-memory SQLite для кожного тесту: DATABASE_URI='sqlite:///:memory:', перестворювати таблиці у fixture через db.create_all()/db.drop_all(). 2) Транзакційна ізоляція: обгортати кожен тест у транзакцію і робити rollback замість commit — зміни ніколи не фіксуються в БД." },
        { q: "Що таке monkeypatch у pytest і як його використовують для мокування зовнішніх сервісів у Flask-тестах?", a: "monkeypatch — вбудований pytest-fixture для тимчасової заміни атрибутів, функцій або змінних оточення. Для мокування email: monkeypatch.setattr('flask_mail.Mail.send', lambda self, msg: None) — замінює реальну відправку пустою функцією. Після тесту monkeypatch автоматично відновлює оригінальні значення. Альтернатива — unittest.mock.patch як декоратор." },
      ],
      ru: [
        { q: "Как fixture client в pytest для Flask позволяет тестировать маршруты без запуска реального сервера?", a: "Fixture: @pytest.fixture def client(): app.config['TESTING'] = True; with app.test_client() as client: yield client. app.test_client() возвращает Werkzeug TestClient, симулирующий HTTP-запросы напрямую через WSGI-интерфейс без сетевого уровня. client.get('/users'), client.post('/login', json={...}) — удобный API для тестов без запуска сервера." },
        { q: "Как в pytest изолировать тесты БД чтобы изменения одного теста не влияли на другой?", a: "Варианты: 1) Использовать in-memory SQLite для каждого теста: DATABASE_URI='sqlite:///:memory:', пересоздавать таблицы в fixture через db.create_all()/db.drop_all(). 2) Транзакционная изоляция: оборачивать каждый тест в транзакцию и делать rollback вместо commit — изменения никогда не фиксируются в БД." },
        { q: "Что такое monkeypatch в pytest и как его используют для мокирования внешних сервисов в Flask-тестах?", a: "monkeypatch — встроенный pytest-fixture для временной замены атрибутов, функций или переменных окружения. Для мокирования email: monkeypatch.setattr('flask_mail.Mail.send', lambda self, msg: None) — заменяет реальную отправку пустой функцией. После теста monkeypatch автоматически восстанавливает оригинальные значения. Альтернатива — unittest.mock.patch как декоратор." },
      ]
    },
    '06-13': {
      uk: [
        { q: "Що таке OpenAPI Specification (OAS) і яку роль відіграє Swagger UI при документуванні Flask API?", a: "OpenAPI Specification — мовно-нейтральний стандарт опису REST API у форматі YAML/JSON: ендпоінти, методи, параметри, схеми запиту/відповіді, аутентифікація. Swagger UI — веб-інтерфейс, що читає OAS-документ і генерує інтерактивну документацію: дозволяє виконувати запити прямо з браузера, що прискорює розробку і тестування." },
        { q: "Як Flask-Smorest або Flasgger автоматично генерують OAS-документ з коду Flask?", a: "Flasgger читає YAML/docstring у маршрутах і будує специфікацію. Flask-Smorest використовує marshmallow-схеми та Blueprint: @blp.arguments(UserSchema) і @blp.response(200, UserSchema) — декоратори автоматично генерують параметри та відповіді в OAS. Клас Api(app) збирає всі Blueprint і генерує /api/openapi.json та /api/swagger-ui." },
        { q: "Як описати аутентифікацію Bearer JWT у OpenAPI-специфікації?", a: "У секції components.securitySchemes: bearerAuth: { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' }. Глобально або для конкретного ендпоінту: security: [{ bearerAuth: [] }]. Swagger UI автоматично додасть кнопку Authorize і буде надсилати Authorization: Bearer <token> заголовок у тестових запитах." },
      ],
      ru: [
        { q: "Что такое OpenAPI Specification (OAS) и какую роль играет Swagger UI при документировании Flask API?", a: "OpenAPI Specification — язык-нейтральный стандарт описания REST API в формате YAML/JSON: эндпоинты, методы, параметры, схемы запроса/ответа, аутентификация. Swagger UI — веб-интерфейс, читающий OAS-документ и генерирующий интерактивную документацию: позволяет выполнять запросы прямо из браузера, что ускоряет разработку и тестирование." },
        { q: "Как Flask-Smorest или Flasgger автоматически генерируют OAS-документ из кода Flask?", a: "Flasgger читает YAML/docstring в маршрутах и строит спецификацию. Flask-Smorest использует marshmallow-схемы и Blueprint: @blp.arguments(UserSchema) и @blp.response(200, UserSchema) — декораторы автоматически генерируют параметры и ответы в OAS. Класс Api(app) собирает все Blueprint и генерирует /api/openapi.json и /api/swagger-ui." },
        { q: "Как описать аутентификацию Bearer JWT в OpenAPI-спецификации?", a: "В секции components.securitySchemes: bearerAuth: { type: 'http', scheme: 'bearer', bearerFormat: 'JWT' }. Глобально или для конкретного эндпоинта: security: [{ bearerAuth: [] }]. Swagger UI автоматически добавит кнопку Authorize и будет отправлять заголовок Authorization: Bearer <token> в тестовых запросах." },
      ]
    },
  };
  Object.keys(QUIZ).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.quiz = QUIZ[id];
  });
})();
