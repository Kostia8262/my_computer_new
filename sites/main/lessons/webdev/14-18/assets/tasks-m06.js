/* Додаткові завдання · Модуль 06 — Flask/Python */
(function () {
  'use strict';
  var EXT = {
    '06-01': [
      { level:'medium', uk:'Створи Flask-застосунок із трьома маршрутами: GET / повертає HTML-сторінку з привітанням, GET /about повертає plain text із описом, GET /user/<name> повертає JSON { "name": name, "message": "Привіт, <name>!" }. Запускай через if __name__ == "__main__": app.run(debug=True, host="0.0.0.0", port=5000).', ru:'Создай Flask-приложение с тремя маршрутами: GET / возвращает HTML-страницу с приветствием, GET /about возвращает plain text с описанием, GET /user/<name> возвращает JSON { "name": name, "message": "Привет, <name>!" }. Запускай через if __name__ == "__main__": app.run(debug=True, host="0.0.0.0", port=5000).' },
      { level:'hard',   uk:'Додай хук before_request, що фіксує час початку запиту (g.start_time = time.time()) і записує рядок "<METHOD> <PATH> <TIMESTAMP>" до файлу logs/requests.log. Додай хук after_request, що обчислює тривалість (time.time() - g.start_time) і вставляє X-Response-Time: <ms>ms до відповіді.', ru:'Добавь хук before_request, фиксирующий время начала запроса (g.start_time = time.time()) и записывающий строку "<METHOD> <PATH> <TIMESTAMP>" в файл logs/requests.log. Добавь хук after_request, вычисляющий длительность (time.time() - g.start_time) и вставляющий X-Response-Time: <ms>ms в ответ.' },
      { level:'hard',   uk:'Створи Blueprint api із маршрутами GET /api/ping (повертає { "pong": true, "timestamp": ... }) і POST /api/echo (повертає request.get_json() без змін). Зареєструй із prefix="/api". Додай обробник 404 на Blueprint, що повертає JSON { "error": "Not Found", "path": request.path } замість HTML.', ru:'Создай Blueprint api с маршрутами GET /api/ping (возвращает { "pong": true, "timestamp": ... }) и POST /api/echo (возвращает request.get_json() без изменений). Зарегистрируй с prefix="/api". Добавь обработчик 404 на Blueprint, возвращающий JSON { "error": "Not Found", "path": request.path } вместо HTML.' },
      { level:'extra',  uk:'Реалізуй систему плагінів Flask: INSTALLED_PLUGINS у config.py — список рядків із шляхами модулів (наприклад "plugins.auth", "plugins.metrics"). Напиши функцію load_plugins(app, plugin_paths), що динамічно імпортує кожен модуль через importlib.import_module, викликає його register(app) і реєструє Blueprint. Кожен плагін має власний маршрут /api/<plugin>/health.', ru:'Реализуй систему плагинов Flask: INSTALLED_PLUGINS в config.py — список строк с путями модулей (например "plugins.auth", "plugins.metrics"). Напиши функцию load_plugins(app, plugin_paths), динамически импортирующую каждый модуль через importlib.import_module, вызывающую его register(app) и регистрирующую Blueprint. Каждый плагин имеет собственный маршрут /api/<plugin>/health.' },
    ],
    '06-02': [
      { level:'medium', uk:'Створи base.html із чотирма блоками: {% block title %}{% endblock %}, {% block nav %}{% endblock %}, {% block content %}{% endblock %}, {% block footer %}{% endblock %}. Напиши index.html і about.html, що розширюють base.html через {% extends "base.html" %} і перевизначають усі чотири блоки унікальним вмістом.', ru:'Создай base.html с четырьмя блоками: {% block title %}{% endblock %}, {% block nav %}{% endblock %}, {% block content %}{% endblock %}, {% block footer %}{% endblock %}. Напиши index.html и about.html, расширяющие base.html через {% extends "base.html" %} и переопределяющие все четыре блока уникальным содержимым.' },
      { level:'hard',   uk:'Побудуй сторінку каталогу товарів у Jinja2: проходь по products у {% for %}, застосовуй {{ p.name | upper }} і {{ p.description | truncate(80) }}, показуй {{ p.stock | default("Немає в наявності") }}. Зареєструй кастомний фільтр price_format через app.jinja_env.filters["price_format"] = lambda v: f"{v:,.2f} ГРН".', ru:'Создай страницу каталога товаров в Jinja2: проходи по products в {% for %}, применяй {{ p.name | upper }} и {{ p.description | truncate(80) }}, показывай {{ p.stock | default("Нет в наличии") }}. Зарегистрируй кастомный фильтр price_format через app.jinja_env.filters["price_format"] = lambda v: f"{v:,.2f} ГРН".' },
      { level:'hard',   uk:'Створи файл macros.html із трьома макросами: form_field(name, label, type="text", value="", error="") — рендерить label + input + span помилки; card(title, body, footer="") — рендерить Bootstrap-картку; pagination(page, total_pages, base_url) — рендерить рядок сторінок із посиланнями. Підключи і використай усі три на сторінці каталогу.', ru:'Создай файл macros.html с тремя макросами: form_field(name, label, type="text", value="", error="") — рендерит label + input + span ошибки; card(title, body, footer="") — рендерит Bootstrap-карточку; pagination(page, total_pages, base_url) — рендерит строку страниц со ссылками. Подключи и используй все три на странице каталога.' },
      { level:'extra',  uk:'Побудуй ланцюжок успадкування шаблонів: base.html > layout.html > page.html. Зареєструй context_processor через @app.context_processor, що вводить current_user (із flask.g або None) і site_config (словник із назвою сайту). У nav-блоці base.html показуй кнопку "Вийти" лише якщо current_user is not None. Напиши тест, що перевіряє вміст контексту у кожному шаблоні.', ru:'Создай цепочку наследования шаблонов: base.html > layout.html > page.html. Зарегистрируй context_processor через @app.context_processor, вводящий current_user (из flask.g или None) и site_config (словарь с названием сайта). В nav-блоке base.html показывай кнопку "Выйти" только если current_user is not None. Напиши тест, проверяющий содержимое контекста в каждом шаблоне.' },
    ],
    '06-03': [
      { level:'medium', uk:'Створи RegistrationForm із Flask-WTF: username (StringField, DataRequired, Length(min=3, max=30)), email (EmailField, DataRequired, Email()), password (PasswordField, DataRequired, Length(min=8)), confirm_password (PasswordField, EqualTo("password")). Рендери у шаблоні registration.html: для кожного поля виводь label, input і {{ field.errors[0] }} при наявності помилок.', ru:'Создай RegistrationForm с Flask-WTF: username (StringField, DataRequired, Length(min=3, max=30)), email (EmailField, DataRequired, Email()), password (PasswordField, DataRequired, Length(min=8)), confirm_password (PasswordField, EqualTo("password")). Рендери в шаблоне registration.html: для каждого поля выводи label, input и {{ field.errors[0] }} при наличии ошибок.' },
      { level:'hard',   uk:'Увімкни CSRF-захист Flask-WTF глобально через CSRFProtect(app). Додай {{ form.hidden_tag() }} до кожного шаблону форми. Напиши тест через Flask test client: POST /register без CSRF-токена повертає 400. POST із валідним токеном проходить успішно. Переконайся, що SECRET_KEY встановлено у config.', ru:'Включи CSRF-защиту Flask-WTF глобально через CSRFProtect(app). Добавь {{ form.hidden_tag() }} к каждому шаблону формы. Напиши тест через Flask test client: POST /register без CSRF-токена возвращает 400. POST с валидным токеном проходит успешно. Убедись, что SECRET_KEY установлен в config.' },
      { level:'hard',   uk:'Побудуй динамічну форму освіти: JavaScript-кнопка "Додати освіту" додає новий рядок (school, year_from, year_to, degree). POST-маршрут обробляє дані як списки: request.form.getlist("school[]"), request.form.getlist("degree[]") тощо. Валідуй кожен запис на стороні сервера і відповідай JSON { valid, errors }.', ru:'Создай динамическую форму образования: JavaScript-кнопка "Добавить образование" добавляет новую строку (school, year_from, year_to, degree). POST-маршрут обрабатывает данные как списки: request.form.getlist("school[]"), request.form.getlist("degree[]") и т.д. Валидируй каждую запись на стороне сервера и отвечай JSON { valid, errors }.' },
      { level:'extra',  uk:'Реалізуй форму завантаження файлів: FileField із Flask-WTF, валідатор FileAllowed(["jpg","png","pdf"]) і кастомний валідатор FileSizeLimit (перевіряй len(field.data.read()) <= 5*1024*1024, скидай seek(0) після перевірки). Зберігай із UUID-ім\'ям у папку uploads/. Показуй посилання на завантажений файл і мініатюру для зображень після успішного збереження.', ru:'Реализуй форму загрузки файлов: FileField с Flask-WTF, валидатор FileAllowed(["jpg","png","pdf"]) и кастомный валидатор FileSizeLimit (проверяй len(field.data.read()) <= 5*1024*1024, сбрасывай seek(0) после проверки). Сохраняй с UUID-именем в папку uploads/. Показывай ссылку на загруженный файл и миниатюру для изображений после успешного сохранения.' },
    ],
    '06-04': [
      { level:'medium', uk:'Визнач моделі User (id, username, email, created_at) і Post (id, title, body, user_id FK, created_at) із Flask-SQLAlchemy і зв\'язком один-до-багатьох через db.relationship. Напиши маршрут GET /demo, що створює тестового користувача, 3 пости, читає їх назад і виводить у JSON. Увімкни SQLALCHEMY_ECHO=True для виведення SQL.', ru:'Определи модели User (id, username, email, created_at) и Post (id, title, body, user_id FK, created_at) с Flask-SQLAlchemy и связью один-ко-многим через db.relationship. Напиши маршрут GET /demo, создающий тестового пользователя, 3 поста, читающий их обратно и выводящий в JSON. Включи SQLALCHEMY_ECHO=True для вывода SQL.' },
      { level:'hard',   uk:'Реалізуй зв\'язок User–Role багато-до-багатьох через таблицю асоціації user_roles (user_id FK, role_id FK, assigned_at). Напиши три функції: assign_role(user_id, role_name) додає роль якщо не існує; get_users_by_role(role_name) повертає список користувачів; remove_role(user_id, role_name) видаляє роль. Перевір через окремий тестовий маршрут.', ru:'Реализуй связь User–Role многие-ко-многим через таблицу ассоциации user_roles (user_id FK, role_id FK, assigned_at). Напиши три функции: assign_role(user_id, role_name) добавляет роль если не существует; get_users_by_role(role_name) возвращает список пользователей; remove_role(user_id, role_name) удаляет роль. Проверь через отдельный тестовый маршрут.' },
      { level:'hard',   uk:'Побудуй маршрут пошуку GET /posts?q=flask&page=1&per_page=10: фільтруй Post.title за допомогою ilike("%q%") і Post.body через ilike, об\'єднай через or_. Використай .paginate(page=page, per_page=per_page). Повертай JSON { items: [...], page, per_page, total, pages }. Додай індекс: db.Index("idx_post_title", Post.title).', ru:'Создай маршрут поиска GET /posts?q=flask&page=1&per_page=10: фильтруй Post.title через ilike("%q%") и Post.body через ilike, объединяй через or_. Используй .paginate(page=page, per_page=per_page). Возвращай JSON { items: [...], page, per_page, total, pages }. Добавь индекс: db.Index("idx_post_title", Post.title).' },
      { level:'extra',  uk:'Створи поліморфну модель контенту через Flask-SQLAlchemy joined-table inheritance: базовий клас Content (id, title, created_at, type) і підкласи Article (body TextField), Video (url, duration_sec) і Podcast (url, episode_number). Напиши GET /content, що повертає всі записи у єдиному форматі з полем "type". Реалізуй метод to_dict() у кожному підкласі.', ru:'Создай полиморфную модель контента через Flask-SQLAlchemy joined-table inheritance: базовый класс Content (id, title, created_at, type) и подклассы Article (body TextField), Video (url, duration_sec) и Podcast (url, episode_number). Напиши GET /content, возвращающий все записи в едином формате с полем "type". Реализуй метод to_dict() в каждом подклассе.' },
    ],
    '06-05': [
      { level:'medium', uk:'Ініціалізуй Flask-Migrate у наявному SQLAlchemy-застосунку: flask db init, flask db migrate -m "initial", flask db upgrade. Додай поле bio = db.Column(db.Text, nullable=True) до моделі User, запусти flask db migrate -m "add_user_bio" і flask db upgrade. Перевір стан через flask db history.', ru:'Инициализируй Flask-Migrate в существующем SQLAlchemy-приложении: flask db init, flask db migrate -m "initial", flask db upgrade. Добавь поле bio = db.Column(db.Text, nullable=True) к модели User, запусти flask db migrate -m "add_user_bio" и flask db upgrade. Проверь состояние через flask db history.' },
      { level:'hard',   uk:'Додай NOT NULL стовпець bio до таблиці users у продакшн-безпечний спосіб: у upgrade() виклич op.add_column("users", sa.Column("bio", sa.Text, server_default="")) щоб уникнути блокування; потім видали server_default через op.alter_column. У downgrade() виклич op.drop_column("users", "bio"). Перевір обидва напрямки.', ru:'Добавь NOT NULL столбец bio к таблице users безопасным для продакшена способом: в upgrade() вызови op.add_column("users", sa.Column("bio", sa.Text, server_default="")) чтобы избежать блокировки; затем удали server_default через op.alter_column. В downgrade() вызови op.drop_column("users", "bio"). Проверь оба направления.' },
      { level:'hard',   uk:'Перейменуй стовпець username на display_name у таблиці users: у upgrade() виклич op.alter_column("users", "username", new_column_name="display_name"). У downgrade() зроби зворотну операцію. Запусти flask db upgrade, перевір через sqlite3 (або psql) що стовпець перейменовано і дані збережено. Потім flask db downgrade і знову перевір.', ru:'Переименуй столбец username в display_name в таблице users: в upgrade() вызови op.alter_column("users", "username", new_column_name="display_name"). В downgrade() выполни обратную операцию. Запусти flask db upgrade, проверь через sqlite3 (или psql) что столбец переименован и данные сохранены. Затем flask db downgrade и проверь снова.' },
      { level:'extra',  uk:'Напиши міграцію даних: у upgrade() спочатку додай стовпець full_name (nullable), потім через op.execute() або Python-цикл populate: UPDATE users SET full_name = first_name || " " || last_name WHERE first_name IS NOT NULL. Після заповнення встанови NOT NULL через op.alter_column. У downgrade() видали стовпець. Тестуй на базі з 1000 рядками.', ru:'Напиши миграцию данных: в upgrade() сначала добавь столбец full_name (nullable), затем через op.execute() или Python-цикл populate: UPDATE users SET full_name = first_name || " " || last_name WHERE first_name IS NOT NULL. После заполнения установи NOT NULL через op.alter_column. В downgrade() удали столбец. Тестируй на базе с 1000 строками.' },
    ],
    '06-06': [
      { level:'medium', uk:'Створи Blueprint api_v1 із GET /api/v1/items, що повертає jsonify(items) і POST /api/v1/items, що перевіряє наявність "name" і "price" у request.get_json() — повертає 400 { "error": "Missing fields" } або 201 { "id": ..., "name": ..., "price": ... }. Зберігай у списку в пам\'яті.', ru:'Создай Blueprint api_v1 с GET /api/v1/items, возвращающим jsonify(items) и POST /api/v1/items, проверяющим наличие "name" и "price" в request.get_json() — возвращай 400 { "error": "Missing fields" } или 201 { "id": ..., "name": ..., "price": ... }. Храни в списке в памяти.' },
      { level:'hard',   uk:'Додай GET /api/v1/items/<int:item_id> (404 якщо не знайдено через abort(404)), PUT /api/v1/items/<int:item_id> (оновлює name/price, повертає 200 із оновленим об\'єктом), DELETE /api/v1/items/<int:item_id> (видаляє, повертає 204 make_response("", 204)). Валідуй тіло PUT через marshmallow або власну функцію validate_item(data).', ru:'Добавь GET /api/v1/items/<int:item_id> (404 если не найден через abort(404)), PUT /api/v1/items/<int:item_id> (обновляет name/price, возвращает 200 с обновлённым объектом), DELETE /api/v1/items/<int:item_id> (удаляет, возвращает 204 make_response("", 204)). Валидируй тело PUT через marshmallow или собственную функцию validate_item(data).' },
      { level:'hard',   uk:'Зареєструй JSON-обробники помилок для кодів 400, 404, 405 і 500 на Blueprint через @blueprint.errorhandler(N). Кожен повертає jsonify({ "error": "...", "message": "...", "status": N }) з правильним кодом. Перевір кожен обробник цільовим запитом: DELETE до неіснуючого маршруту → 404, метод GET до POST-only → 405.', ru:'Зарегистрируй JSON-обработчики ошибок для кодов 400, 404, 405 и 500 на Blueprint через @blueprint.errorhandler(N). Каждый возвращает jsonify({ "error": "...", "message": "...", "status": N }) с правильным кодом. Проверь каждый обработчик целевым запросом: DELETE к несуществующему маршруту → 404, метод GET к POST-only → 405.' },
      { level:'extra',  uk:'Побудуй версіоновані Blueprint: api_v1 (без тегів) і api_v2 (додає поле "tags" і пагінацію { items, page, per_page, total }). api_v2 повертає заголовок Link: <url?page=N>; rel="next", <url?page=1>; rel="first" відповідно до RFC 5988. Напиши адаптер-шар: api_v1 клієнти можуть надсилати запити без змін і отримують відповідь v1-формату.', ru:'Создай версионированные Blueprint: api_v1 (без тегов) и api_v2 (добавляет поле "tags" и пагинацию { items, page, per_page, total }). api_v2 возвращает заголовок Link: <url?page=N>; rel="next", <url?page=1>; rel="first" согласно RFC 5988. Напиши адаптер-слой: api_v1 клиенты могут отправлять запросы без изменений и получать ответ v1-формата.' },
    ],
    '06-07': [
      { level:'medium', uk:'Налаштуй Flask-Login: модель User реалізує UserMixin (is_active=True, is_authenticated, get_id). Ініціалізуй LoginManager(app) із login_view="auth.login" і user_loader. Реалізуй POST /auth/login: перевіряй credentials, викликай login_user(user) і переспрямовуй на /dashboard. GET /auth/logout: logout_user() і переспрямування на /.', ru:'Настрой Flask-Login: модель User реализует UserMixin (is_active=True, is_authenticated, get_id). Инициализируй LoginManager(app) с login_view="auth.login" и user_loader. Реализуй POST /auth/login: проверяй credentials, вызывай login_user(user) и перенаправляй на /dashboard. GET /auth/logout: logout_user() и перенаправление на /.' },
      { level:'hard',   uk:'Захисти усі маршрути /dashboard/* через @login_required. Додай прапорець "Запам\'ятати мене" у форму входу: якщо checked — login_user(user, remember=True, duration=timedelta(days=30)). Переконайся через браузер DevTools, що cookie "remember_token" встановлено із терміном дії 30 днів. Тестуй із Flask test client.', ru:'Защити все маршруты /dashboard/* через @login_required. Добавь флажок "Запомнить меня" в форму входа: если checked — login_user(user, remember=True, duration=timedelta(days=30)). Убедись через браузер DevTools, что cookie "remember_token" установлен со сроком 30 дней. Тестируй с Flask test client.' },
      { level:'hard',   uk:'Побудуй сторінку профілю GET /profile (лише для авторизованих): відображай current_user.display_name і current_user.avatar_url. POST /profile: форма оновлює display_name і avatar_url через db.session.commit(). Неавторизованих перенаправляй на /auth/login?next=/profile; після входу відновлюй next через request.args.get("next").', ru:'Создай страницу профиля GET /profile (только для авторизованных): отображай current_user.display_name и current_user.avatar_url. POST /profile: форма обновляет display_name и avatar_url через db.session.commit(). Неавторизованных перенаправляй на /auth/login?next=/profile; после входа восстанавливай next через request.args.get("next").' },
      { level:'extra',  uk:'Реалізуй декоратор @requires_role(*roles) на основі current_user.roles: повертає abort(403) якщо жодна роль не збігається. Побудуй /admin/users зі списком усіх користувачів і кнопками "Заблокувати"/"Розблокувати". POST /admin/users/<id>/ban встановлює user.is_active = False; Flask-Login при наступному login_user(user) перевіряє is_active і відхиляє вхід. Захисти через @requires_role("admin").', ru:'Реализуй декоратор @requires_role(*roles) на основе current_user.roles: возвращает abort(403) если ни одна роль не совпадает. Создай /admin/users со списком всех пользователей и кнопками "Заблокировать"/"Разблокировать". POST /admin/users/<id>/ban устанавливает user.is_active = False; Flask-Login при следующем login_user(user) проверяет is_active и отклоняет вход. Защити через @requires_role("admin").' },
    ],
    '06-08': [
      { level:'medium', uk:'Налаштуй Flask-JWT-Extended: JWT_SECRET_KEY з os.environ, JWT_ACCESS_TOKEN_EXPIRES = timedelta(minutes=15). POST /auth/login перевіряє credentials і повертає { "access_token": ..., "refresh_token": ... }. GET /api/me захищений @jwt_required() і повертає jsonify({ "id": get_jwt_identity(), "claims": get_jwt() }).', ru:'Настрой Flask-JWT-Extended: JWT_SECRET_KEY из os.environ, JWT_ACCESS_TOKEN_EXPIRES = timedelta(minutes=15). POST /auth/login проверяет credentials и возвращает { "access_token": ..., "refresh_token": ... }. GET /api/me защищён @jwt_required() и возвращает jsonify({ "id": get_jwt_identity(), "claims": get_jwt() }).' },
      { level:'hard',   uk:'Реалізуй повний потік оновлення: POST /auth/refresh із @jwt_required(refresh=True) повертає новий access-токен через create_access_token(identity=get_jwt_identity()). POST /auth/logout вносить get_jwt()["jti"] до множини jwt_blocklist у пам\'яті. Зареєструй @jwt.token_in_blocklist_loader, що перевіряє jti у jwt_blocklist. Тестуй весь цикл.', ru:'Реализуй полный поток обновления: POST /auth/refresh с @jwt_required(refresh=True) возвращает новый access-токен через create_access_token(identity=get_jwt_identity()). POST /auth/logout добавляет get_jwt()["jti"] в множество jwt_blocklist в памяти. Зарегистрируй @jwt.token_in_blocklist_loader, проверяющий jti в jwt_blocklist. Тестируй весь цикл.' },
      { level:'hard',   uk:'Додай кастомні claims до JWT: при логіні передавай additional_claims={"role": user.role, "user_id": user.id} до create_access_token. Напиши get_current_user(), що зчитує claims через get_jwt() і повертає словник. Захисти /admin/* маршрути перевіркою get_jwt()["role"] == "admin" у декораторі або хелпері require_admin().', ru:'Добавь кастомные claims в JWT: при логине передавай additional_claims={"role": user.role, "user_id": user.id} в create_access_token. Напиши get_current_user(), считывающий claims через get_jwt() и возвращающий словарь. Защити /admin/* маршруты проверкой get_jwt()["role"] == "admin" в декораторе или хелпере require_admin().' },
      { level:'extra',  uk:'Налаштуй зберігання JWT у cookie: JWT_TOKEN_LOCATION = ["cookies"], JWT_COOKIE_SECURE = True, JWT_COOKIE_SAMESITE = "Lax". Використовуй set_access_cookies(response, token) при логіні і unset_jwt_cookies(response) при логауті. Увімкни JWT_COOKIE_CSRF_PROTECT = True і передавай X-CSRF-TOKEN у заголовках. Напиши тест, що перевіряє 401 при відсутності CSRF-токена.', ru:'Настрой хранение JWT в cookie: JWT_TOKEN_LOCATION = ["cookies"], JWT_COOKIE_SECURE = True, JWT_COOKIE_SAMESITE = "Lax". Используй set_access_cookies(response, token) при логине и unset_jwt_cookies(response) при логауте. Включи JWT_COOKIE_CSRF_PROTECT = True и передавай X-CSRF-TOKEN в заголовках. Напиши тест, проверяющий 401 при отсутствии CSRF-токена.' },
    ],
    '06-09': [
      { level:'medium', uk:'Розбий монолітний Flask-застосунок на три Blueprint: auth (маршрути /auth/login, /auth/logout, /auth/register), users (/users/me, /users/<id>), posts (/posts, /posts/<id>). Кожен Blueprint реєструється у create_app() із відповідним url_prefix. Перевір усі маршрути через flask routes.', ru:'Разбей монолитное Flask-приложение на три Blueprint: auth (маршруты /auth/login, /auth/logout, /auth/register), users (/users/me, /users/<id>), posts (/posts, /posts/<id>). Каждый Blueprint регистрируется в create_app() с соответствующим url_prefix. Проверь все маршруты через flask routes.' },
      { level:'hard',   uk:'Реалізуй патерн фабрики Blueprint: кожен модуль (auth, users, posts) надає функцію create_blueprint() → Blueprint. Фабрика застосунку create_app(config_name) викликає кожну фабрику і реєструє Blueprint. Кожен Blueprint реєструє власні JSON-обробники 404 і 500 через @bp.errorhandler(N). Тестуй кожну помилку окремо.', ru:'Реализуй паттерн фабрики Blueprint: каждый модуль (auth, users, posts) предоставляет функцию create_blueprint() → Blueprint. Фабрика приложения create_app(config_name) вызывает каждую фабрику и регистрирует Blueprint. Каждый Blueprint регистрирует собственные JSON-обработчики 404 и 500 через @bp.errorhandler(N). Тестируй каждую ошибку отдельно.' },
      { level:'hard',   uk:'Розділи стан між Blueprint через app.extensions: у create_app() реєструй app.extensions["user_service"] = UserService(db) і app.extensions["post_service"] = PostService(db). У Blueprint-маршрутах отримуй сервіси через current_app.extensions["user_service"]. Напиши декоратор inject_service(name), що передає сервіс у параметр функції автоматично.', ru:'Раздели состояние между Blueprint через app.extensions: в create_app() регистрируй app.extensions["user_service"] = UserService(db) и app.extensions["post_service"] = PostService(db). В Blueprint-маршрутах получай сервисы через current_app.extensions["user_service"]. Напиши декоратор inject_service(name), передающий сервис в параметр функции автоматически.' },
      { level:'extra',  uk:'Побудуй реєстр плагінів: INSTALLED_BLUEPRINTS = ["app.blueprints.auth", "app.blueprints.users", "app.blueprints.posts", "app.blueprints.admin"] у config.py. Функція load_blueprints(app, modules) імпортує кожен модуль, викликає create_blueprint() і реєструє. Кожен Blueprint може оголошувати CLI-команди через @bp.cli.command("seed"). Перевір flask --app app seed.', ru:'Создай реестр плагинов: INSTALLED_BLUEPRINTS = ["app.blueprints.auth", "app.blueprints.users", "app.blueprints.posts", "app.blueprints.admin"] в config.py. Функция load_blueprints(app, modules) импортирует каждый модуль, вызывает create_blueprint() и регистрирует. Каждый Blueprint может объявлять CLI-команды через @bp.cli.command("seed"). Проверь flask --app app seed.' },
    ],
    '06-10': [
      { level:'medium', uk:'Налаштуй Celery із брокером Redis: CELERY_BROKER_URL = "redis://localhost:6379/0". Напиши завдання send_welcome_email(user_email) із @celery.task. У маршруті POST /register після збереження користувача виклич send_welcome_email.delay(user.email). Запусти воркер через celery -A app.celery worker --loglevel=info і перевір виконання в логах.', ru:'Настрой Celery с брокером Redis: CELERY_BROKER_URL = "redis://localhost:6379/0". Напиши задачу send_welcome_email(user_email) с @celery.task. В маршруте POST /register после сохранения пользователя вызови send_welcome_email.delay(user.email). Запусти воркер через celery -A app.celery worker --loglevel=info и проверь выполнение в логах.' },
      { level:'hard',   uk:'Налаштуй Celery Beat: у CELERYBEAT_SCHEDULE визнач clean_expired_tokens — виконується щогодини (crontab(minute=0)) і видаляє з БД усі записи TokenBlocklist де expires_at < datetime.utcnow(). Запусти celery -A app.celery beat --loglevel=info разом із воркером. Перевір через logs.', ru:'Настрой Celery Beat: в CELERYBEAT_SCHEDULE определи clean_expired_tokens — выполняется каждый час (crontab(minute=0)) и удаляет из БД все записи TokenBlocklist где expires_at < datetime.utcnow(). Запусти celery -A app.celery beat --loglevel=info вместе с воркером. Проверь через logs.' },
      { level:'hard',   uk:'Побудуй конвеєр завдань як Celery chain: upload_file.s(file_path) | process_image.s() | save_thumbnail.s(). POST /process повертає { "task_id": result.id }. GET /tasks/<task_id> повертає { "state": result.state, "result": result.result } через AsyncResult. Обробляй стани PENDING, STARTED, SUCCESS, FAILURE.', ru:'Создай конвейер задач как Celery chain: upload_file.s(file_path) | process_image.s() | save_thumbnail.s(). POST /process возвращает { "task_id": result.id }. GET /tasks/<task_id> возвращает { "state": result.state, "result": result.result } через AsyncResult. Обрабатывай состояния PENDING, STARTED, SUCCESS, FAILURE.' },
      { level:'extra',  uk:'Реалізуй повторні спроби з експоненційним відкотом для fetch_exchange_rates: autoretry_for=(requests.RequestException, ConnectionError), max_retries=5, retry_backoff=True (Celery автоматично множить countdown на 2^retry). Після успіху зберігай результат у Redis через redis_client.setex("exchange_rates", 3600, json.dumps(rates)). Логуй кожну повторну спробу.', ru:'Реализуй повторные попытки с экспоненциальным откатом для fetch_exchange_rates: autoretry_for=(requests.RequestException, ConnectionError), max_retries=5, retry_backoff=True (Celery автоматически умножает countdown на 2^retry). После успеха сохраняй результат в Redis через redis_client.setex("exchange_rates", 3600, json.dumps(rates)). Логируй каждую повторную попытку.' },
    ],
    '06-11': [
      { level:'medium', uk:'Налаштуй Flask-Mail зі змінних середовища: MAIL_SERVER, MAIL_PORT, MAIL_USE_TLS, MAIL_USERNAME, MAIL_PASSWORD, MAIL_DEFAULT_SENDER. Після успішної реєстрації (POST /register) надсилай plain-text лист через msg = Message("Ласкаво просимо!", recipients=[user.email]); msg.body = "..."; mail.send(msg). Перевір через Mailtrap або MailHog.', ru:'Настрой Flask-Mail из переменных среды: MAIL_SERVER, MAIL_PORT, MAIL_USE_TLS, MAIL_USERNAME, MAIL_PASSWORD, MAIL_DEFAULT_SENDER. После успешной регистрации (POST /register) отправляй plain-text письмо через msg = Message("Добро пожаловать!", recipients=[user.email]); msg.body = "..."; mail.send(msg). Проверь через Mailtrap или MailHog.' },
      { level:'hard',   uk:'Створи HTML-шаблон email/welcome.html: персональне вітання "Привіт, {{ user.name }}!", кнопка-посилання активації із token, plain-text резервний варіант у окремому welcome.txt. Надсилай обидва через msg.body = render_template("email/welcome.txt", user=user) і msg.html = render_template("email/welcome.html", user=user, token=token). Генеруй безпечний token через secrets.token_urlsafe(32).', ru:'Создай HTML-шаблон email/welcome.html: персональное приветствие "Привет, {{ user.name }}!", кнопка-ссылка активации с token, plain-text резервный вариант в отдельном welcome.txt. Отправляй оба через msg.body = render_template("email/welcome.txt", user=user) и msg.html = render_template("email/welcome.html", user=user, token=token). Генерируй безопасный token через secrets.token_urlsafe(32).' },
      { level:'hard',   uk:'Реалізуй чергу надсилання листів через Celery: замість mail.send(msg) передавай { "to": email, "subject": subject, "body": body, "html": html } до завдання send_email_task.delay(...). Celery-воркер будує Message і викликає mail.send(). Налаштуй autoretry_for=(smtplib.SMTPException,), max_retries=3 з вбудованою паузою між спробами.', ru:'Реализуй очередь отправки писем через Celery: вместо mail.send(msg) передавай { "to": email, "subject": subject, "body": body, "html": html } в задачу send_email_task.delay(...). Celery-воркер строит Message и вызывает mail.send(). Настрой autoretry_for=(smtplib.SMTPException,), max_retries=3 со встроенной паузой между попытками.' },
      { level:'extra',  uk:'Побудуй систему масового розсилання: POST /admin/broadcast приймає JSON { "subject": str, "body": str, "filter": { "role": str } }. Вибирай відповідних користувачів із БД. Розбивай на партії по 50 через list(chunks(users, 50)). Кожну партію відправляй як group([send_email_task.s(...) for u in batch]).apply_async(). Зберігай лічильники sent і failed у Redis INCR. Повертай { "queued": N } одразу.', ru:'Создай систему массовой рассылки: POST /admin/broadcast принимает JSON { "subject": str, "body": str, "filter": { "role": str } }. Выбирай подходящих пользователей из БД. Разбивай на партии по 50 через list(chunks(users, 50)). Каждую партию отправляй как group([send_email_task.s(...) for u in batch]).apply_async(). Храни счётчики sent и failed в Redis INCR. Возвращай { "queued": N } сразу.' },
    ],
    '06-12': [
      { level:'medium', uk:'Налаштуй pytest із conftest.py: фікстура app через create_app("testing") (TESTING=True, SQLite :memory:), фікстура client через app.test_client(). Напиши чотири тести: GET / → 200, POST /auth/login із валідними даними → 200, із невалідними → 401, GET /api/me без токена → 401. Запускай через pytest -v.', ru:'Настрой pytest с conftest.py: фикстура app через create_app("testing") (TESTING=True, SQLite :memory:), фикстура client через app.test_client(). Напиши четыре теста: GET / → 200, POST /auth/login с валидными данными → 200, с невалидными → 401, GET /api/me без токена → 401. Запускай через pytest -v.' },
      { level:'hard',   uk:'Створи фікстуру тестової бази даних: @pytest.fixture(scope="function") db_session: перед кожним тестом виконуй db.create_all(), після — db.session.remove() і db.drop_all(). Напиши тести CRUD для User: create → читай за id → оновлюй email → видаляй і перевіряй відсутність. Кожен тест незалежний і не залишає даних.', ru:'Создай фикстуру тестовой базы данных: @pytest.fixture(scope="function") db_session: перед каждым тестом выполняй db.create_all(), после — db.session.remove() и db.drop_all(). Напиши тесты CRUD для User: create → читай по id → обновляй email → удаляй и проверяй отсутствие. Каждый тест независим и не оставляет данных.' },
      { level:'hard',   uk:'Замокай зовнішні сервіси у тестах: через unittest.mock.patch("app.mail.send") перевіряй, що при POST /auth/register mail.send() викликається рівно 1 раз із правильними to і subject. Через mock.patch("app.tasks.send_welcome_email.delay") перевіряй постановку у чергу. Всі моки застосовуй через менеджер контексту with.', ru:'Замокай внешние сервисы в тестах: через unittest.mock.patch("app.mail.send") проверяй, что при POST /auth/register mail.send() вызывается ровно 1 раз с правильными to и subject. Через mock.patch("app.tasks.send_welcome_email.delay") проверяй постановку в очередь. Все моки применяй через менеджер контекста with.' },
      { level:'extra',  uk:'Досягни покриття 90%+ Blueprint автентифікації. Запускай pytest --cov=app.blueprints.auth --cov-report=html. Напиши параметризовані тести: @pytest.mark.parametrize("email,password,expected", [("","",400),("bad-email","pass",400),("a@b.com","short",400),("a@b.com","valid1234",201)]). Додай Makefile-ціль test-coverage, що завершується помилкою raise якщо покриття < 90%.', ru:'Достигни покрытия 90%+ Blueprint аутентификации. Запускай pytest --cov=app.blueprints.auth --cov-report=html. Напиши параметризованные тесты: @pytest.mark.parametrize("email,password,expected", [("","",400),("bad-email","pass",400),("a@b.com","short",400),("a@b.com","valid1234",201)]). Добавь Makefile-цель test-coverage, завершающуюся ошибкой если покрытие < 90%.' },
    ],
    '06-13': [
      { level:'medium', uk:'Підключи flasgger або flask-smorest. Задокументуй GET /api/v1/users через YAML у docstring: опиши query-параметри page (integer, default=1) і per_page (integer, default=20), схему відповіді (масив UserSchema) і приклад відповіді. Перейди за /apidocs або /swagger-ui і переконайся, що endpoint задокументовано коректно.', ru:'Подключи flasgger или flask-smorest. Задокументируй GET /api/v1/users через YAML в docstring: опиши query-параметры page (integer, default=1) и per_page (integer, default=20), схему ответа (массив UserSchema) и пример ответа. Перейди по /apidocs или /swagger-ui и убедись, что endpoint задокументирован корректно.' },
      { level:'hard',   uk:'Визнач marshmallow-схеми: UserSchema (id, username, email, created_at), PostSchema (id, title, body, author), ErrorSchema (error, message, status). Використовуй з декораторами flask-smorest @blp.arguments(PostSchema) для тіла запиту POST і @blp.response(200, PostSchema) або @blp.response(201, PostSchema) для відповідей. Перевір автогенерацію Swagger UI.', ru:'Определи marshmallow-схемы: UserSchema (id, username, email, created_at), PostSchema (id, title, body, author), ErrorSchema (error, message, status). Используй с декораторами flask-smorest @blp.arguments(PostSchema) для тела запроса POST и @blp.response(200, PostSchema) или @blp.response(201, PostSchema) для ответов. Проверь автогенерацию Swagger UI.' },
      { level:'hard',   uk:'Додай схему безпеки Bearer до Swagger: у API_SPEC_OPTIONS визнач securitySchemes: { Bearer: { type: "http", scheme: "bearer", bearerFormat: "JWT" } }. Позначай захищені endpoint-и через @blp.doc(security=[{"Bearer": []}]). Перевір у Swagger UI: захищені маршрути мають замок, кнопка "Authorize" дозволяє ввести токен.', ru:'Добавь схему безопасности Bearer в Swagger: в API_SPEC_OPTIONS определи securitySchemes: { Bearer: { type: "http", scheme: "bearer", bearerFormat: "JWT" } }. Отмечай защищённые endpoint-ы через @blp.doc(security=[{"Bearer": []}]). Проверь в Swagger UI: защищённые маршруты имеют замок, кнопка "Authorize" позволяет ввести токен.' },
      { level:'extra',  uk:'Реалізуй CLI-команду flask export-spec: запускає тимчасовий Flask test client, читає spec через api.spec.to_dict() і записує у openapi.json. Напиши GitHub Actions workflow (.github/workflows/docs.yml): запускає застосунок, виконує flask export-spec, валідує через npx swagger-cli validate openapi.json і публікує файл як артефакт через actions/upload-artifact.', ru:'Реализуй CLI-команду flask export-spec: запускает временный Flask test client, читает spec через api.spec.to_dict() и записывает в openapi.json. Напиши GitHub Actions workflow (.github/workflows/docs.yml): запускает приложение, выполняет flask export-spec, валидирует через npx swagger-cli validate openapi.json и публикует файл как артефакт через actions/upload-artifact.' },
    ],
    '06-14': [
      { level:'medium', uk:'Розгорни проєкт із фабричним патерном: create_app(config_name="development") завантажує клас конфігурації (DevelopmentConfig, TestingConfig, ProductionConfig) з config.py. Додай CLI-команду flask seed-db: створює таблиці і наповнює 5 тестових користувачів і 10 постів. Запускай через flask --app app:create_app seed-db.', ru:'Разверни проект с фабричным паттерном: create_app(config_name="development") загружает класс конфигурации (DevelopmentConfig, TestingConfig, ProductionConfig) из config.py. Добавь CLI-команду flask seed-db: создаёт таблицы и наполняет 5 тестовых пользователей и 10 постов. Запускай через flask --app app:create_app seed-db.' },
      { level:'hard',   uk:'Побудуй REST API Users + Posts: Flask-SQLAlchemy моделі зі зв\'язком один-до-багатьох, Flask-JWT-Extended (access 15 хв, refresh 7 днів), marshmallow-серіалізація (UserSchema, PostSchema), Blueprint-архітектура (auth, users, posts), глобальні JSON-обробники 400/401/403/404/500 у create_app().', ru:'Создай REST API Users + Posts: Flask-SQLAlchemy модели со связью один-ко-многим, Flask-JWT-Extended (access 15 мин, refresh 7 дней), marshmallow-сериализация (UserSchema, PostSchema), Blueprint-архитектура (auth, users, posts), глобальные JSON-обработчики 400/401/403/404/500 в create_app().' },
      { level:'hard',   uk:'Додай: Celery + Redis для асинхронного вітального листа при реєстрації; Flask-Migrate міграції (початкова + seed-data); конфігурацію з .env через python-decouple; GET /health, що повертає { "db": "ok"|"error", "redis": "ok"|"error", "uptime": ... } перевіряючи db.session.execute("SELECT 1") і redis_client.ping().', ru:'Добавь: Celery + Redis для асинхронного приветственного письма при регистрации; Flask-Migrate миграции (начальная + seed-data); конфигурацию из .env через python-decouple; GET /health, возвращающий { "db": "ok"|"error", "redis": "ok"|"error", "uptime": ... } с проверкой db.session.execute("SELECT 1") и redis_client.ping().' },
      { level:'extra',  uk:'Напиши pytest-набір (90%+ покриття): тести auth (register, login, refresh, logout), CRUD users і posts, /health. Замокай mail.send і Celery .delay. Докеризуй через docker-compose.yml: services flask (gunicorn), celery-worker, redis, postgres. Додай Makefile: make dev (flask run), make test (pytest), make migrate (flask db upgrade), make docker-up (docker compose up -d). Генеруй OpenAPI через flask export-spec.', ru:'Напиши pytest-набор (90%+ покрытия): тесты auth (register, login, refresh, logout), CRUD users и posts, /health. Замокай mail.send и Celery .delay. Докеризируй через docker-compose.yml: services flask (gunicorn), celery-worker, redis, postgres. Добавь Makefile: make dev (flask run), make test (pytest), make migrate (flask db upgrade), make docker-up (docker compose up -d). Генерируй OpenAPI через flask export-spec.' },
    ],
  };
  Object.keys(EXT).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.tasks = (l.tasks || []).concat(EXT[id]);
  });
})();
