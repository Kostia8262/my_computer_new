/* Питання самоконтролю · Модуль 04 · 14-18 Фулстек-Про */
(function () {
  'use strict';
  var QUIZ = {
    '04-01': {
      uk: [
        { q: "Що таке «prop drilling» і чому Context API вирішує цю проблему?", a: "Prop drilling — це передача пропсів через багато проміжних компонентів, які самі їх не використовують. Context API дозволяє надати значення будь-якому нащадку напряму, через useContext, минаючи проміжні ланки." },
        { q: "Коли Context Provider ре-рендерить своїх споживачів (consumers)?", a: "Кожного разу, коли змінюється значення value у Provider. Якщо value — це новий об'єкт на кожному рендері батьківського компонента, всі споживачі ре-рендеряться, навіть якщо дані не змінились." },
        { q: "Як уникнути зайвих ре-рендерів при використанні Context API зі складним об'єктом стану?", a: "Розбити контекст на кілька менших (наприклад, окремий контекст для даних і для dispatch/setter), або мемоізувати value через useMemo, щоб посилання на об'єкт змінювалось тільки при реальній зміні даних." },
      ],
      ru: [
        { q: "Что такое «prop drilling» и почему Context API решает эту проблему?", a: "Prop drilling — это передача пропсов через множество промежуточных компонентов, которые сами их не используют. Context API позволяет предоставить значение любому потомку напрямую через useContext, минуя промежуточные звенья." },
        { q: "Когда Context Provider перерендеривает своих потребителей (consumers)?", a: "Каждый раз, когда меняется значение value у Provider. Если value — новый объект при каждом рендере родителя, все потребители перерендеривают-ся, даже если данные не изменились." },
        { q: "Как избежать лишних ре-рендеров при использовании Context API со сложным объектом состояния?", a: "Разбить контекст на несколько меньших (например, отдельный контекст для данных и для dispatch/setter), либо мемоизировать value через useMemo, чтобы ссылка на объект менялась только при реальном изменении данных." },
      ]
    },
    '04-02': {
      uk: [
        { q: "Яка сигнатура useReducer і що повертає цей хук?", a: "useReducer(reducer, initialState) повертає кортеж [state, dispatch]. Reducer — це чиста функція (state, action) => newState. dispatch надсилає action-об'єкт до reducer для оновлення стану." },
        { q: "Коли доцільніше використовувати useReducer замість useState?", a: "Коли логіка переходів складна або взаємозалежна (кілька полів стану змінюються разом), коли наступний стан залежить від попереднього за складною логікою, або коли потрібно передати лише dispatch (стабільне посилання) глибоко по дереву." },
        { q: "Чому reducer обов'язково має бути чистою функцією?", a: "React може викликати reducer двічі (в StrictMode) для виявлення побічних ефектів. Чиста функція гарантує передбачуваність стану, дозволяє тестувати переходи без моків і спрощує відлагодження через time-travel debugging." },
      ],
      ru: [
        { q: "Какова сигнатура useReducer и что возвращает этот хук?", a: "useReducer(reducer, initialState) возвращает кортеж [state, dispatch]. Reducer — чистая функция (state, action) => newState. dispatch отправляет action-объект в reducer для обновления состояния." },
        { q: "Когда предпочтительнее использовать useReducer вместо useState?", a: "Когда логика переходов сложна или взаимозависима (несколько полей состояния меняются вместе), когда следующее состояние зависит от предыдущего по сложной логике, или когда нужно передать только dispatch (стабильная ссылка) глубоко по дереву." },
        { q: "Почему reducer обязательно должен быть чистой функцией?", a: "React может вызвать reducer дважды (в StrictMode) для обнаружения побочных эффектов. Чистая функция гарантирует предсказуемость состояния, позволяет тестировать переходы без моков и упрощает отладку через time-travel debugging." },
      ]
    },
    '04-03': {
      uk: [
        { q: "Як створити стор у Zustand і чим він відрізняється від Redux за обсягом коду?", a: "Стор створюється одним викликом create((set) => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) })). На відміну від Redux, не потрібні action-creators, reducer, combineReducers і налаштування middleware — весь стан і логіка в одній функції." },
        { q: "Що таке селектор у Zustand і навіщо він потрібен?", a: "Селектор — функція, яку передають у хук: useStore(s => s.count). Компонент підписується лише на частину стору і ре-рендериться тільки якщо повернене значення змінилося, що мінімізує зайві рендери." },
        { q: "Як підключити middleware persist до стору Zustand для збереження стану в localStorage?", a: "Обгорнути фабрику стору: create(persist((set) => ({...}), { name: 'my-store' })). Middleware автоматично серіалізує стан у localStorage при кожній зміні та відновлює його при першому монтуванні." },
      ],
      ru: [
        { q: "Как создать стор в Zustand и чем он отличается от Redux по объёму кода?", a: "Стор создаётся одним вызовом create((set) => ({ count: 0, inc: () => set(s => ({ count: s.count + 1 })) })). В отличие от Redux, не нужны action-creators, reducer, combineReducers и настройка middleware — всё состояние и логика в одной функции." },
        { q: "Что такое селектор в Zustand и зачем он нужен?", a: "Селектор — функция, передаваемая в хук: useStore(s => s.count). Компонент подписывается только на часть стора и перерендеривается лишь при изменении возвращаемого значения, минимизируя лишние рендеры." },
        { q: "Как подключить middleware persist к стору Zustand для сохранения состояния в localStorage?", a: "Обернуть фабрику стора: create(persist((set) => ({...}), { name: 'my-store' })). Middleware автоматически сериализует состояние в localStorage при каждом изменении и восстанавливает его при первом монтировании." },
      ]
    },
    '04-04': {
      uk: [
        { q: "Що означають параметри staleTime і gcTime (cacheTime) у TanStack Query і як вони взаємодіють?", a: "staleTime — час, протягом якого дані вважаються свіжими і не рефетчяться автоматично. gcTime — час, через який неактивні (без підписників) кешовані дані видаляються з пам'яті. Дані можуть бути stale, але ще зберігатися в кеші для миттєвого показу." },
        { q: "Як useMutation відрізняється від useQuery і що таке onSuccess / onSettled callbacks?", a: "useQuery для читання даних (автоматичний тригер), useMutation для запитів що змінюють дані (POST/PUT/DELETE, тригер вручну через mutate()). onSuccess викликається лише при успіху, onSettled — завжди після завершення (і успіх, і помилка), що зручно для інвалідації кешу." },
        { q: "Як queryClient.invalidateQueries працює і коли його викликають?", a: "Він позначає відповідні запити як stale і, якщо вони активні (є компоненти-підписники), негайно тригерить рефетч. Зазвичай викликається в onSuccess мутації, щоб UI автоматично відобразив оновлені дані з сервера." },
      ],
      ru: [
        { q: "Что означают параметры staleTime и gcTime (cacheTime) в TanStack Query и как они взаимодействуют?", a: "staleTime — время, в течение которого данные считаются свежими и не рефетчатся автоматически. gcTime — время, через которое неактивные (без подписчиков) кешированные данные удаляются из памяти. Данные могут быть stale, но ещё храниться в кеше для мгновенного показа." },
        { q: "Чем useMutation отличается от useQuery и что такое коллбэки onSuccess / onSettled?", a: "useQuery для чтения данных (автотриггер), useMutation для запросов изменяющих данные (POST/PUT/DELETE, триггер вручную через mutate()). onSuccess вызывается только при успехе, onSettled — всегда после завершения (и успех, и ошибка), что удобно для инвалидации кеша." },
        { q: "Как работает queryClient.invalidateQueries и когда его вызывают?", a: "Он помечает соответствующие запросы как stale и, если они активны (есть компоненты-подписчики), немедленно тригерит рефетч. Обычно вызывается в onSuccess мутации, чтобы UI автоматически отобразил обновлённые данные с сервера." },
      ]
    },
    '04-05': {
      uk: [
        { q: "Що робить React.memo і за яких умов він пропускає ре-рендер компонента?", a: "React.memo — HOC, що мемоізує результат рендеру функціонального компонента. Ре-рендер пропускається, якщо всі пропси порівняні за поверхневою рівністю (shallow equality) і дали той самий результат. Для складних пропсів можна передати власну функцію порівняння другим аргументом." },
        { q: "У чому різниця між useMemo та useCallback, і коли кожен з них застосовують?", a: "useMemo мемоізує результат обчислення: const val = useMemo(() => heavyCalc(x), [x]). useCallback мемоізує саму функцію: const fn = useCallback(() => doSomething(x), [x]). useCallback = useMemo(() => fn, deps). Обидва перераховують значення лише при зміні залежностей." },
        { q: "Чому передача нової функції через пропс може призвести до зайвих ре-рендерів дочірнього React.memo компонента і як це виправити?", a: "При кожному рендері батька створюється нова функція з іншим посиланням. React.memo порівнює пропси за посиланням, тому вважає функцію зміненою і ре-рендерить дочірній компонент. Виправлення: обгорнути функцію у useCallback, щоб посилання залишалось стабільним між рендерами." },
      ],
      ru: [
        { q: "Что делает React.memo и при каких условиях он пропускает ре-рендер компонента?", a: "React.memo — HOC, мемоизирующий результат рендера функционального компонента. Ре-рендер пропускается, если все пропсы совпали при поверхностном сравнении (shallow equality). Для сложных пропсов можно передать собственную функцию сравнения вторым аргументом." },
        { q: "В чём разница между useMemo и useCallback, и когда каждый из них применяют?", a: "useMemo мемоизирует результат вычисления: const val = useMemo(() => heavyCalc(x), [x]). useCallback мемоизирует саму функцию: const fn = useCallback(() => doSomething(x), [x]). useCallback = useMemo(() => fn, deps). Оба пересчитывают значение только при изменении зависимостей." },
        { q: "Почему передача новой функции через пропс может вызывать лишние ре-рендеры дочернего React.memo компонента и как это исправить?", a: "При каждом рендере родителя создаётся новая функция с другой ссылкой. React.memo сравнивает пропсы по ссылке, поэтому считает функцию изменившейся и перерендеривает дочерний компонент. Исправление: обернуть функцию в useCallback, чтобы ссылка оставалась стабильной между рендерами." },
      ]
    },
    '04-06': {
      uk: [
        { q: "Як React.lazy та Suspense реалізують code splitting на рівні компонентів?", a: "React.lazy(() => import('./Component')) повертає компонент, який завантажується асинхронно при першому рендері. Suspense перехоплює стан завантаження та показує fallback UI доти, доки чанк не завантажиться. Webpack або Vite автоматично виносять імпортований модуль в окремий чанк." },
        { q: "Яка різниця між статичним import і динамічним import() і як це впливає на розмір бандлу?", a: "Статичний import аналізується під час збирання і потрапляє в основний бандл. Динамічний import() — це вираз, що повертає Promise, і збирач виносить цей модуль в окремий чанк, який завантажується лише тоді, коли код реально виконується." },
        { q: "Як організувати route-based code splitting у React Router з React.lazy?", a: "Обгорнути кожен page-компонент у React.lazy: const Page = lazy(() => import('./Page')). У конфігурації роутів обгорнути <Routes> або окремий <Route element> у <Suspense fallback={<Spinner/>}>. Тоді JavaScript для кожної сторінки завантажується лише при навігації на неї." },
      ],
      ru: [
        { q: "Как React.lazy и Suspense реализуют code splitting на уровне компонентов?", a: "React.lazy(() => import('./Component')) возвращает компонент, загружаемый асинхронно при первом рендере. Suspense перехватывает состояние загрузки и показывает fallback UI до тех пор, пока чанк не загрузится. Webpack или Vite автоматически выносят импортируемый модуль в отдельный чанк." },
        { q: "В чём разница между статичным import и динамическим import() и как это влияет на размер бандла?", a: "Статичный import анализируется при сборке и попадает в основной бандл. Динамический import() — это выражение, возвращающее Promise, и сборщик выносит этот модуль в отдельный чанк, загружаемый только при реальном выполнении кода." },
        { q: "Как организовать route-based code splitting в React Router с React.lazy?", a: "Обернуть каждый page-компонент в React.lazy: const Page = lazy(() => import('./Page')). В конфигурации роутов обернуть <Routes> или отдельный <Route element> в <Suspense fallback={<Spinner/>}>. Тогда JavaScript для каждой страницы загружается только при навигации на неё." },
      ]
    },
    '04-07': {
      uk: [
        { q: "Що таке «windowing» (virtualization) і яку проблему вона вирішує при рендері великих списків?", a: "Windowing — техніка, за якої в DOM одночасно знаходяться лише елементи, видимі у вікні перегляду (плюс невеликий overscan). Замість рендеру 10 000 вузлів одночасно, рендеряться 20-50. Це різко скорочує час першого рендеру, використання пам'яті та кількість DOM-вузлів." },
        { q: "Яка різниця між FixedSizeList та VariableSizeList у react-window?", a: "FixedSizeList — всі елементи мають однакову висоту, задану через prop itemSize. VariableSizeList — висота кожного елемента динамічна і задається функцією itemSize(index). VariableSizeList потребує відомих розмірів заздалегідь; для невідомих (контент залежний) використовують react-virtual з авто-вимірюванням." },
        { q: "Як у react-window передати дані до кожного рядка без prop drilling через List?", a: "Використати prop itemData у компонента списку: <FixedSizeList itemData={items} ...>. У функції рендеру рядка отримати дані через деструктуризацію: ({ index, data, style }) => <div style={style}>{data[index].name}</div>. Це дозволяє уникнути closures, що захоплюють весь масив." },
      ],
      ru: [
        { q: "Что такое «windowing» (virtualization) и какую проблему она решает при рендере больших списков?", a: "Windowing — техника, при которой в DOM одновременно находятся только элементы, видимые в окне просмотра (плюс небольшой overscan). Вместо рендера 10 000 узлов, рендерится 20-50. Это резко сокращает время первого рендера, использование памяти и количество DOM-узлов." },
        { q: "В чём разница между FixedSizeList и VariableSizeList в react-window?", a: "FixedSizeList — все элементы имеют одинаковую высоту, заданную через prop itemSize. VariableSizeList — высота каждого элемента динамическая и задаётся функцией itemSize(index). VariableSizeList требует заранее известных размеров; для неизвестных (контент-зависимых) используют react-virtual с авто-измерением." },
        { q: "Как в react-window передать данные каждой строке без prop drilling через List?", a: "Использовать prop itemData у компонента списка: <FixedSizeList itemData={items} ...>. В функции рендера строки получить данные деструктуризацией: ({ index, data, style }) => <div style={style}>{data[index].name}</div>. Это позволяет избежать closures, захватывающих весь массив." },
      ]
    },
    '04-08': {
      uk: [
        { q: "Що таке сенсори (sensors) у dnd-kit і навіщо їх налаштовують?", a: "Sensors — це абстракції, які визначають спосіб ініціювання перетягування: PointerSensor (миша/дотик), KeyboardSensor (клавіатура для доступності), TouchSensor. Їх налаштовують через useSensors, задаючи пороги активації (наприклад, мінімальну дистанцію або затримку), щоб відрізнити drag від кліку." },
        { q: "Яку роль відіграє DndContext і які події він надає?", a: "DndContext — кореневий провайдер, що координує всі draggable і droppable елементи в межах однієї зони. Він надає події onDragStart, onDragOver, onDragEnd (та інші), де отримують активний елемент (active) і ціль (over), що дозволяє оновити порядок елементів у стані." },
        { q: "Як реалізувати сортований список з dnd-kit за допомогою SortableContext?", a: "Обгорнути список у <SortableContext items={ids} strategy={verticalListSortingStrategy}>. Кожен елемент списку використовує хук useSortable(id), який повертає attributes, listeners і setNodeRef. В onDragEnd застосовують arrayMove(items, oldIndex, newIndex) для перебудови масиву і оновлення стану." },
      ],
      ru: [
        { q: "Что такое сенсоры (sensors) в dnd-kit и зачем их настраивают?", a: "Sensors — абстракции, определяющие способ инициации перетаскивания: PointerSensor (мышь/касание), KeyboardSensor (клавиатура для доступности), TouchSensor. Их настраивают через useSensors, задавая пороги активации (минимальное расстояние или задержку), чтобы отличить drag от клика." },
        { q: "Какую роль играет DndContext и какие события он предоставляет?", a: "DndContext — корневой провайдер, координирующий все draggable и droppable элементы в пределах одной зоны. Он предоставляет события onDragStart, onDragOver, onDragEnd (и другие), где получают активный элемент (active) и цель (over), что позволяет обновить порядок элементов в состоянии." },
        { q: "Как реализовать сортируемый список с dnd-kit с помощью SortableContext?", a: "Обернуть список в <SortableContext items={ids} strategy={verticalListSortingStrategy}>. Каждый элемент списка использует хук useSortable(id), возвращающий attributes, listeners и setNodeRef. В onDragEnd применяют arrayMove(items, oldIndex, newIndex) для перестройки массива и обновления состояния." },
      ]
    },
    '04-09': {
      uk: [
        { q: "Що робить функція register() у react-hook-form і чому підхід з неконтрольованими полями є продуктивнішим?", a: "register(name) повертає ref, onChange, onBlur, name, які прив'язують нативний input до форми. Форма читає значення через ref, а не через стан React, тому введення даних не викликає ре-рендерів компонента. Це значно підвищує продуктивність форм з багатьма полями." },
        { q: "Як інтегрувати Zod-схему з react-hook-form для типобезпечної валідації?", a: "Встановити @hookform/resolvers і передати zodResolver(schema) в useForm({ resolver: zodResolver(schema) }). RHF при submit або при зміні поля викликає schema.safeParse() і заповнює formState.errors повідомленнями з Zod. TypeScript-тип форми виводять через z.infer<typeof schema>." },
        { q: "Як useForm обробляє вкладені поля та масиви і що таке useFieldArray?", a: "Вкладені поля реєструють через крапкову нотацію: register('address.city'). Для динамічних масивів полів useFieldArray({ control, name: 'items' }) надає fields (масив з id), append, remove, move — вони змінюють масив і синхронізуються з RHF без ручного управління станом." },
      ],
      ru: [
        { q: "Что делает функция register() в react-hook-form и почему подход с неконтролируемыми полями производительнее?", a: "register(name) возвращает ref, onChange, onBlur, name, которые привязывают нативный input к форме. Форма читает значения через ref, а не через состояние React, поэтому ввод данных не вызывает ре-рендеры компонента. Это значительно повышает производительность форм со многими полями." },
        { q: "Как интегрировать Zod-схему с react-hook-form для типобезопасной валидации?", a: "Установить @hookform/resolvers и передать zodResolver(schema) в useForm({ resolver: zodResolver(schema) }). RHF при submit или изменении поля вызывает schema.safeParse() и заполняет formState.errors сообщениями из Zod. TypeScript-тип формы выводят через z.infer<typeof schema>." },
        { q: "Как useForm обрабатывает вложенные поля и массивы и что такое useFieldArray?", a: "Вложенные поля регистрируют через точечную нотацию: register('address.city'). Для динамических массивов полей useFieldArray({ control, name: 'items' }) предоставляет fields (массив с id), append, remove, move — они изменяют массив и синхронизируются с RHF без ручного управления состоянием." },
      ]
    },
    '04-10': {
      uk: [
        { q: "Що роблять пропси initial, animate та exit у Framer Motion і в якому порядку вони застосовуються?", a: "initial — стан компонента до монтування (або початкова позиція анімації). animate — цільовий стан, до якого компонент анімується після монтування. exit — стан, до якого компонент анімується перед розмонтуванням. Порядок: initial -> animate при mount, animate -> exit при unmount." },
        { q: "Навіщо потрібен AnimatePresence і яку проблему він вирішує?", a: "React видаляє компонент з DOM миттєво при зміні умови рендеру. AnimatePresence дозволяє компонентам з exit-анімацією завершити анімацію виходу перед фактичним видаленням. Він відстежує нащадків і затримує видалення до завершення exit-переходу." },
        { q: "Що таке layout animation у Framer Motion і як увімкнути автоматичний перехід при зміні позиції елемента?", a: "Layout animation автоматично анімує зміну розміру або позиції елемента (FLIP-техніка). Достатньо додати пропс layout до motion-елемента: <motion.div layout>. Framer Motion вимірює позицію до і після зміни DOM та анімує між ними через CSS transform для продуктивного переходу." },
      ],
      ru: [
        { q: "Что делают пропсы initial, animate и exit во Framer Motion и в каком порядке они применяются?", a: "initial — состояние компонента до монтирования (начальная позиция анимации). animate — целевое состояние, к которому компонент анимируется после монтирования. exit — состояние, к которому компонент анимируется перед размонтированием. Порядок: initial -> animate при mount, animate -> exit при unmount." },
        { q: "Зачем нужен AnimatePresence и какую проблему он решает?", a: "React удаляет компонент из DOM мгновенно при изменении условия рендера. AnimatePresence позволяет компонентам с exit-анимацией завершить анимацию выхода перед фактическим удалением. Он отслеживает потомков и задерживает удаление до завершения exit-перехода." },
        { q: "Что такое layout animation во Framer Motion и как включить автоматический переход при изменении позиции элемента?", a: "Layout animation автоматически анимирует изменение размера или позиции элемента (FLIP-техника). Достаточно добавить проп layout к motion-элементу: <motion.div layout>. Framer Motion измеряет позицию до и после изменения DOM и анимирует между ними через CSS transform для производительного перехода." },
      ]
    },
    '04-11': {
      uk: [
        { q: "Яка різниця між getByRole, queryByRole та findByRole у React Testing Library і коли який використовувати?", a: "getBy — синхронний, кидає помилку якщо елемент не знайдено; для елементів, що мають бути присутні. queryBy — синхронний, повертає null якщо не знайдено; для перевірки відсутності елемента. findBy — асинхронний (повертає Promise), чекає появи елемента; для асинхронних операцій (fetch, таймери)." },
        { q: "Чим userEvent відрізняється від fireEvent і чому RTL рекомендує userEvent?", a: "fireEvent — низькорівневий: безпосередньо генерує один DOM-подію. userEvent симулює повну взаємодію користувача: клік включає mousedown, mouseup, click; введення тексту — послідовність keydown, keypress, input, keyup для кожного символу. userEvent точніше відтворює реальну поведінку браузера." },
        { q: "Що таке Mock Service Worker (MSW) і як він вписується у тестування компонентів, що виконують fetch-запити?", a: "MSW перехоплює мережеві запити на рівні Service Worker (браузер) або через node-interceptors (Jest/Node). Замість мокування fetch чи axios напряму, тести взаємодіють з реальним кодом отримання даних, а MSW повертає задані відповіді. Це дозволяє тестувати компоненти в умовах, близьких до реальних." },
      ],
      ru: [
        { q: "В чём разница между getByRole, queryByRole и findByRole в React Testing Library и когда какой использовать?", a: "getBy — синхронный, бросает ошибку если элемент не найден; для элементов, которые должны присутствовать. queryBy — синхронный, возвращает null если не найден; для проверки отсутствия элемента. findBy — асинхронный (возвращает Promise), ждёт появления элемента; для асинхронных операций (fetch, таймеры)." },
        { q: "Чем userEvent отличается от fireEvent и почему RTL рекомендует userEvent?", a: "fireEvent — низкоуровневый: непосредственно генерирует одно DOM-событие. userEvent симулирует полное взаимодействие пользователя: клик включает mousedown, mouseup, click; ввод текста — последовательность keydown, keypress, input, keyup для каждого символа. userEvent точнее воспроизводит реальное поведение браузера." },
        { q: "Что такое Mock Service Worker (MSW) и как он вписывается в тестирование компонентов, выполняющих fetch-запросы?", a: "MSW перехватывает сетевые запросы на уровне Service Worker (браузер) или через node-interceptors (Jest/Node). Вместо мокирования fetch или axios напрямую, тесты взаимодействуют с реальным кодом получения данных, а MSW возвращает заданные ответы. Это позволяет тестировать компоненты в условиях, близких к реальным." },
      ]
    },
    '04-12': {
      uk: [
        { q: "Що таке Story у Storybook і яка мінімальна структура файлу *.stories.tsx?", a: "Story — це іменований експорт функції, що повертає конкретний стан компонента з певними аргументами. Файл має default export з мета-об'єктом (title, component) і named exports — самі stories. Наприклад: export const Primary: Story = { args: { label: 'Button' } }." },
        { q: "Що таке Args (аргументи) у Storybook і як вони пов'язані з Controls панеллю?", a: "Args — об'єкт значень пропсів, що передається компоненту в конкретній story. Controls панель у UI автоматично генерується з ArgTypes компонента і дозволяє змінювати args у реальному часі без редагування коду, що робить Storybook інтерактивним середовищем для дизайнерів і розробників." },
        { q: "Як Storybook допомагає виявляти проблеми компонентів, які важко відтворити в реальному застосунку?", a: "Storybook рендерить компоненти ізольовано, без глобального стану, роутингу або даних. Це дозволяє легко тестувати крайні стани (пусте значення, довгий текст, помилка, завантаження), які важко відтворити в реальному застосунку. Також дозволяє проводити візуальне регресійне тестування через Chromatic." },
      ],
      ru: [
        { q: "Что такое Story в Storybook и какова минимальная структура файла *.stories.tsx?", a: "Story — именованный экспорт функции, возвращающей конкретное состояние компонента с определёнными аргументами. Файл имеет default export с мета-объектом (title, component) и named exports — сами stories. Например: export const Primary: Story = { args: { label: 'Button' } }." },
        { q: "Что такое Args (аргументы) в Storybook и как они связаны с панелью Controls?", a: "Args — объект значений пропсов, передаваемый компоненту в конкретной story. Панель Controls в UI автоматически генерируется из ArgTypes компонента и позволяет менять args в реальном времени без редактирования кода, делая Storybook интерактивной средой для дизайнеров и разработчиков." },
        { q: "Как Storybook помогает выявлять проблемы компонентов, которые сложно воспроизвести в реальном приложении?", a: "Storybook рендерит компоненты изолированно, без глобального состояния, роутинга или данных. Это позволяет легко тестировать крайние состояния (пустое значение, длинный текст, ошибка, загрузка), которые сложно воспроизвести в реальном приложении. Также позволяет проводить визуальное регрессионное тестирование через Chromatic." },
      ]
    },
    '04-13': {
      uk: [
        { q: "Як правильно типізувати пропси функціонального React-компонента і яка різниця між FC<Props> та явним типом аргументу?", a: "Явна типізація аргументу (function Comp({ name }: Props)) є кращою практикою: вона не включає неявний children (як React.FC) і точніше виводить тип повернення. React.FC<Props> неявно додає children до Props (у старих версіях), що може спричинити неточності у типах." },
        { q: "Як типізувати useRef для DOM-елемента і чому важливо правильно задати початкове значення?", a: "const ref = useRef<HTMLInputElement>(null). Тип параметра визначає тип ref.current. Початкове значення null необхідне для рефів, що прив'язуються до DOM-елементів (вони заповнюються React після mount). Якщо передати undefined, TypeScript не звузить тип ref.current до HTMLInputElement." },
        { q: "Як типізувати createContext щоб уникнути перевірки на undefined при використанні useContext?", a: "Один підхід — надати значення за замовчуванням: createContext<Theme>(defaultTheme). Інший — createContext<Theme | null>(null) і в хуку-обгортці перевіряти: if (!ctx) throw new Error('Missing Provider'). Це гарантує, що споживачі завжди отримують валідний контекст або отримують зрозумілу помилку." },
      ],
      ru: [
        { q: "Как правильно типизировать пропсы функционального React-компонента и в чём разница между FC<Props> и явным типом аргумента?", a: "Явная типизация аргумента (function Comp({ name }: Props)) является лучшей практикой: она не включает неявный children (как React.FC) и точнее выводит тип возвращаемого значения. React.FC<Props> неявно добавляет children в Props (в старых версиях), что может вызывать неточности в типах." },
        { q: "Как типизировать useRef для DOM-элемента и почему важно правильно задать начальное значение?", a: "const ref = useRef<HTMLInputElement>(null). Тип параметра определяет тип ref.current. Начальное значение null необходимо для рефов, привязываемых к DOM-элементам (они заполняются React после mount). Если передать undefined, TypeScript не сузит тип ref.current до HTMLInputElement." },
        { q: "Как типизировать createContext чтобы избежать проверки на undefined при использовании useContext?", a: "Один подход — предоставить значение по умолчанию: createContext<Theme>(defaultTheme). Другой — createContext<Theme | null>(null) и в хуке-обёртке проверять: if (!ctx) throw new Error('Missing Provider'). Это гарантирует, что потребители всегда получают валидный контекст или получают понятную ошибку." },
      ]
    },
  };
  Object.keys(QUIZ).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.quiz = QUIZ[id];
  });
})();
