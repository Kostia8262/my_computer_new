/* Питання самоконтролю · Модуль 05 · 14-18 Фулстек-Про */
(function () {
  'use strict';
  var QUIZ = {
    '05-01': {
      uk: [
        { q: "Що таке Event Loop у Node.js і як він дозволяє обробляти тисячі запитів в одному потоці?", a: "Event Loop — це механізм, що безперервно перевіряє чергу подій (callbacks) і виконує їх, поки call stack порожній. Node.js не блокує виконання на I/O-операціях: файл або мережевий запит передається до libuv, а callback ставиться у чергу після завершення. Це дозволяє обробляти багато запитів конкурентно без многопотоковості." },
        { q: "Яка різниця між CommonJS (require) та ES Modules (import/export) у Node.js і як їх переключати?", a: "CommonJS — стандартна система модулів Node.js: require() синхронний, module.exports. ESM — стандарт JavaScript: import/export, асинхронне завантаження. Для ESM у Node.js використовують розширення .mjs або поле \"type\": \"module\" у package.json. ESM не підтримує __dirname/__filename безпосередньо — замінюються через import.meta.url." },
        { q: "Що таке package.json поле engines і як npm/yarn використовують package-lock.json для детермінованих залежностей?", a: "Поле engines задає мінімальну версію Node.js/npm, необхідну для проекту: { \"engines\": { \"node\": \">=18\" } }. package-lock.json фіксує точні версії всіх залежностей (включно з транзитивними) та їхні checksums. Це гарантує, що npm install на будь-якій машині встановить ідентичне дерево залежностей." },
      ],
      ru: [
        { q: "Что такое Event Loop в Node.js и как он позволяет обрабатывать тысячи запросов в одном потоке?", a: "Event Loop — механизм, непрерывно проверяющий очередь событий (callbacks) и выполняющий их, пока call stack пуст. Node.js не блокирует выполнение на I/O-операциях: файл или сетевой запрос передаётся в libuv, а callback ставится в очередь после завершения. Это позволяет обрабатывать много запросов конкурентно без многопоточности." },
        { q: "В чём разница между CommonJS (require) и ES Modules (import/export) в Node.js и как их переключать?", a: "CommonJS — стандартная система модулей Node.js: require() синхронный, module.exports. ESM — стандарт JavaScript: import/export, асинхронная загрузка. Для ESM в Node.js используют расширение .mjs или поле \"type\": \"module\" в package.json. ESM не поддерживает __dirname/__filename напрямую — заменяются через import.meta.url." },
        { q: "Что такое поле engines в package.json и как npm/yarn используют package-lock.json для детерминированных зависимостей?", a: "Поле engines задаёт минимальную версию Node.js/npm, необходимую для проекта: { \"engines\": { \"node\": \">=18\" } }. package-lock.json фиксирует точные версии всех зависимостей (включая транзитивные) и их checksums. Это гарантирует, что npm install на любой машине установит идентичное дерево зависимостей." },
      ]
    },
    '05-02': {
      uk: [
        { q: "Що таке middleware у Express і в якому порядку вони виконуються?", a: "Middleware — функції з сигнатурою (req, res, next) або (err, req, res, next). Express виконує їх у порядку реєстрації через app.use() або на конкретних маршрутах. Кожен middleware може змінити req/res, завершити відповідь або передати управління далі через next(). При виклику next(err) управління переходить до error-handling middleware." },
        { q: "Як Express визначає, чи є функція error-handling middleware?", a: "Express визначає error middleware за кількістю параметрів: функція з чотирма аргументами (err, req, res, next) є error handler. Вона отримує контроль лише коли попередній middleware викликає next(err) або next(new Error(...)). Error middleware реєструють після всіх звичайних маршрутів." },
        { q: "Як router.use() відрізняється від app.use() і навіщо виносити маршрути в окремі Router-файли?", a: "app.use() реєструє middleware на рівні всього додатку. router.use() реєструє middleware лише в межах конкретного Router. Виокремлення маршрутів у Router дозволяє монтувати їх з префіксом: app.use('/api/users', usersRouter), що спрощує масштабування, тестування і підтримку великих API." },
      ],
      ru: [
        { q: "Что такое middleware в Express и в каком порядке они выполняются?", a: "Middleware — функции с сигнатурой (req, res, next) или (err, req, res, next). Express выполняет их в порядке регистрации через app.use() или на конкретных маршрутах. Каждый middleware может изменить req/res, завершить ответ или передать управление дальше через next(). При вызове next(err) управление переходит к error-handling middleware." },
        { q: "Как Express определяет, является ли функция error-handling middleware?", a: "Express определяет error middleware по количеству параметров: функция с четырьмя аргументами (err, req, res, next) является error handler. Она получает управление только когда предыдущий middleware вызывает next(err) или next(new Error(...)). Error middleware регистрируют после всех обычных маршрутов." },
        { q: "Как router.use() отличается от app.use() и зачем выносить маршруты в отдельные Router-файлы?", a: "app.use() регистрирует middleware на уровне всего приложения. router.use() регистрирует middleware только в пределах конкретного Router. Вынесение маршрутов в Router позволяет монтировать их с префиксом: app.use('/api/users', usersRouter), что упрощает масштабирование, тестирование и поддержку больших API." },
      ]
    },
    '05-03': {
      uk: [
        { q: "Як в Express маршрут отримує динамічний параметр (наприклад :id) і де він доступний?", a: "Динамічний сегмент визначається двокрапкою: router.get('/users/:id'). Значення доступне в req.params.id. Можна задати кілька параметрів: /posts/:postId/comments/:commentId — вони будуть у req.params.postId та req.params.commentId. Необов'язковий параметр позначається знаком питання: :id?." },
        { q: "Яка семантика HTTP-методів PUT та PATCH і як вони відрізняються за REST-конвенцією?", a: "PUT замінює ресурс цілком: клієнт надсилає повний об'єкт і сервер повністю перезаписує запис. PATCH вносить часткове оновлення: клієнт надсилає лише змінені поля. У REST PUT є ідемпотентним (повторний запит дає той самий результат), PATCH може бути як ідемпотентним, так і ні, залежно від реалізації." },
        { q: "Як Express обробляє query-параметри (рядок запиту) і що робить middleware express.urlencoded()?", a: "Query-параметри після ? у URL автоматично розбираються Express і доступні через req.query як об'єкт. express.urlencoded({ extended: true }) парсить тіло запитів з Content-Type: application/x-www-form-urlencoded (HTML-форми) і розміщує результат у req.body. Без цього middleware req.body буде undefined для form-запитів." },
      ],
      ru: [
        { q: "Как в Express маршрут получает динамический параметр (например :id) и где он доступен?", a: "Динамический сегмент определяется двоеточием: router.get('/users/:id'). Значение доступно в req.params.id. Можно задать несколько параметров: /posts/:postId/comments/:commentId — они будут в req.params.postId и req.params.commentId. Необязательный параметр обозначается знаком вопроса: :id?." },
        { q: "Какова семантика HTTP-методов PUT и PATCH и как они отличаются по REST-конвенции?", a: "PUT заменяет ресурс целиком: клиент отправляет полный объект и сервер полностью перезаписывает запись. PATCH вносит частичное обновление: клиент отправляет только изменённые поля. В REST PUT является идемпотентным (повторный запрос даёт тот же результат), PATCH может быть как идемпотентным, так и нет, в зависимости от реализации." },
        { q: "Как Express обрабатывает query-параметры (строку запроса) и что делает middleware express.urlencoded()?", a: "Query-параметры после ? в URL автоматически разбираются Express и доступны через req.query как объект. express.urlencoded({ extended: true }) парсит тело запросов с Content-Type: application/x-www-form-urlencoded (HTML-формы) и помещает результат в req.body. Без этого middleware req.body будет undefined для form-запросов." },
      ]
    },
    '05-04': {
      uk: [
        { q: "Які заголовки відповіді критично важливі для безпеки REST API і як їх встановити в Express?", a: "Content-Type: application/json (гарантує правильну інтерпретацію клієнтом), X-Content-Type-Options: nosniff (блокує MIME-sniffing), Cache-Control: no-store для чутливих ендпоінтів. Встановлюються через res.set() або пакет helmet, що додає десяток security-заголовків автоматично. Статус коди встановлюються через res.status(code).json(data)." },
        { q: "Які HTTP-статус коди відповідають ситуаціям: ресурс не знайдено, немає прав доступу, конфлікт даних, помилка валідації?", a: "404 Not Found — ресурс не знайдено. 403 Forbidden — автентифікований, але немає прав (на відміну від 401 Unauthorized — не автентифікований). 409 Conflict — конфлікт стану (наприклад, email вже існує). 422 Unprocessable Entity — дані синтаксично коректні, але семантично неправильні (помилки валідації)." },
        { q: "Як правильно зчитати тіло (body) POST-запиту з JSON-даними в Express і що необхідно підключити?", a: "Необхідно підключити middleware express.json(): app.use(express.json()). Після цього Express парсить тіло запитів з Content-Type: application/json і розміщує розпарсений об'єкт у req.body. Без цього middleware req.body буде undefined або порожнім. Для обмеження розміру тіла передають опцію limit: express.json({ limit: '10kb' })." },
      ],
      ru: [
        { q: "Какие заголовки ответа критически важны для безопасности REST API и как их установить в Express?", a: "Content-Type: application/json (гарантирует правильную интерпретацию клиентом), X-Content-Type-Options: nosniff (блокирует MIME-sniffing), Cache-Control: no-store для чувствительных эндпоинтов. Устанавливаются через res.set() или пакет helmet, добавляющий десяток security-заголовков автоматически. Статус коды устанавливаются через res.status(code).json(data)." },
        { q: "Какие HTTP-статус коды соответствуют ситуациям: ресурс не найден, нет прав доступа, конфликт данных, ошибка валидации?", a: "404 Not Found — ресурс не найден. 403 Forbidden — аутентифицирован, но нет прав (в отличие от 401 Unauthorized — не аутентифицирован). 409 Conflict — конфликт состояния (например, email уже существует). 422 Unprocessable Entity — данные синтаксически корректны, но семантически неверны (ошибки валидации)." },
        { q: "Как правильно прочитать тело (body) POST-запроса с JSON-данными в Express и что нужно подключить?", a: "Необходимо подключить middleware express.json(): app.use(express.json()). После этого Express парсит тело запросов с Content-Type: application/json и помещает распарсенный объект в req.body. Без этого middleware req.body будет undefined или пустым. Для ограничения размера тела передают опцию limit: express.json({ limit: '10kb' })." },
      ]
    },
    '05-05': {
      uk: [
        { q: "Що робить пакет helmet і які саме заголовки він встановлює за замовчуванням?", a: "helmet() — колекція middleware, що встановлює security-заголовки: Content-Security-Policy, X-DNS-Prefetch-Control, X-Frame-Options (захист від clickjacking), X-Content-Type-Options: nosniff, Referrer-Policy, Strict-Transport-Security (HSTS) та інші. Викликається одним рядком app.use(helmet()) і значно покращує security posture API." },
        { q: "Яка різниця між Simple та Preflight CORS-запитами і як cors() middleware обробляє їх?", a: "Simple requests (GET, HEAD, POST з безпечними заголовками) браузер надсилає напряму. Preflight — браузер спочатку надсилає OPTIONS-запит, щоб перевірити чи дозволяє сервер фактичний запит. cors() автоматично відповідає на OPTIONS і встановлює заголовки Access-Control-Allow-Origin, Methods, Headers згідно з конфігурацією." },
        { q: "Що логує morgan у форматі 'combined' і чим він відрізняється від формату 'dev'?", a: "'combined' — стандартний Apache-формат з IP, датою, методом, URL, статусом, розміром відповіді, Referer і User-Agent. Корисний для продакшену та аналізу логів. 'dev' — скорочений кольоровий формат: метод, URL, статус (з кольором за кодом) і час відповіді. Зручний для розробки, не підходить для парсингу логів." },
      ],
      ru: [
        { q: "Что делает пакет helmet и какие именно заголовки он устанавливает по умолчанию?", a: "helmet() — коллекция middleware, устанавливающая security-заголовки: Content-Security-Policy, X-DNS-Prefetch-Control, X-Frame-Options (защита от clickjacking), X-Content-Type-Options: nosniff, Referrer-Policy, Strict-Transport-Security (HSTS) и другие. Вызывается одной строкой app.use(helmet()) и значительно улучшает security posture API." },
        { q: "В чём разница между Simple и Preflight CORS-запросами и как cors() middleware обрабатывает их?", a: "Simple requests (GET, HEAD, POST с безопасными заголовками) браузер отправляет напрямую. Preflight — браузер сначала отправляет OPTIONS-запрос, чтобы проверить разрешает ли сервер фактический запрос. cors() автоматически отвечает на OPTIONS и устанавливает заголовки Access-Control-Allow-Origin, Methods, Headers согласно конфигурации." },
        { q: "Что логирует morgan в формате 'combined' и чем он отличается от формата 'dev'?", a: "'combined' — стандартный Apache-формат с IP, датой, методом, URL, статусом, размером ответа, Referer и User-Agent. Полезен для продакшена и анализа логов. 'dev' — сокращённый цветной формат: метод, URL, статус (с цветом по коду) и время ответа. Удобен для разработки, не подходит для парсинга логов." },
      ]
    },
    '05-06': {
      uk: [
        { q: "Назвіть ключові принципи REST і поясніть, що означає принцип statelessness.", a: "REST-принципи: client-server, statelessness, cacheability, uniform interface, layered system, code on demand (опціональний). Statelessness означає, що сервер не зберігає стан сесії між запитами — кожен запит містить всю необхідну інформацію (токен, параметри). Це спрощує масштабування, оскільки будь-який сервер може обробити будь-який запит." },
        { q: "Як правильно структурувати URL REST API для колекцій та окремих ресурсів?", a: "Використовують іменники у множині для колекцій: GET /users, POST /users. Окремий ресурс через ідентифікатор: GET /users/:id, PUT /users/:id, DELETE /users/:id. Вкладені ресурси: GET /users/:id/posts. URL не повинен містити дієслова (/getUser — неправильно). Версіонування: /api/v1/users." },
        { q: "Коли використовувати статус 201 Created замість 200 OK і що повинна повертати відповідь на POST-запит?", a: "201 Created повертається при успішному створенні нового ресурсу (POST). Відповідь має містити: 1) тіло з даними створеного ресурсу (включаючи id та server-generated поля); 2) заголовок Location зі шляхом до нового ресурсу: Location: /api/users/123. 200 OK — для запитів, що не створюють новий ресурс." },
      ],
      ru: [
        { q: "Назовите ключевые принципы REST и объясните, что означает принцип statelessness.", a: "REST-принципы: client-server, statelessness, cacheability, uniform interface, layered system, code on demand (опциональный). Statelessness означает, что сервер не хранит состояние сессии между запросами — каждый запрос содержит всю необходимую информацию (токен, параметры). Это упрощает масштабирование, так как любой сервер может обработать любой запрос." },
        { q: "Как правильно структурировать URL REST API для коллекций и отдельных ресурсов?", a: "Используют существительные во множественном числе для коллекций: GET /users, POST /users. Отдельный ресурс через идентификатор: GET /users/:id, PUT /users/:id, DELETE /users/:id. Вложенные ресурсы: GET /users/:id/posts. URL не должен содержать глаголы (/getUser — неправильно). Версионирование: /api/v1/users." },
        { q: "Когда использовать статус 201 Created вместо 200 OK и что должен возвращать ответ на POST-запрос?", a: "201 Created возвращается при успешном создании нового ресурса (POST). Ответ должен содержать: 1) тело с данными созданного ресурса (включая id и server-generated поля); 2) заголовок Location с путём к новому ресурсу: Location: /api/users/123. 200 OK — для запросов, не создающих новый ресурс." },
      ]
    },
    '05-07': {
      uk: [
        { q: "Яка різниця між access token та refresh token у JWT-аутентифікації?", a: "Access token — короткоживучий (5-15 хвилин), передається у кожному запиті в заголовку Authorization: Bearer. Refresh token — довгоживучий (7-30 днів), зберігається безпечно (httpOnly cookie) і використовується лише для отримання нового access token. Якщо access token викрадено, атакуючий має обмежений час доступу." },
        { q: "Яка структура JWT токена і що означає термін «підпис» (signature)?", a: "JWT складається з трьох base64url-кодованих частин, розділених крапкою: Header (алгоритм та тип), Payload (claims: дані та метадані), Signature. Підпис обчислюється: HMACSHA256(base64(header) + '.' + base64(payload), secretKey). Сервер верифікує токен, перераховуючи підпис і порівнюючи — підробити токен без секрету неможливо." },
        { q: "Як реалізувати logout при використанні stateless JWT і які підходи існують для інвалідації токенів?", a: "Оскільки JWT stateless, сервер не зберігає список активних токенів. Підходи до інвалідації: 1) Короткий TTL access token — обмежує вікно атаки. 2) Token blacklist у Redis — зберігати jti (JWT ID) інвалідованих токенів до їх expiry. 3) Ротація секрету — інвалідує всі токени, але впливає на всіх користувачів. Найбільш практичний: комбінація короткого TTL + blacklist." },
      ],
      ru: [
        { q: "В чём разница между access token и refresh token в JWT-аутентификации?", a: "Access token — короткоживущий (5-15 минут), передаётся в каждом запросе в заголовке Authorization: Bearer. Refresh token — долгоживущий (7-30 дней), хранится безопасно (httpOnly cookie) и используется только для получения нового access token. Если access token украден, атакующий имеет ограниченное время доступа." },
        { q: "Какова структура JWT токена и что означает термин «подпись» (signature)?", a: "JWT состоит из трёх base64url-закодированных частей, разделённых точкой: Header (алгоритм и тип), Payload (claims: данные и метаданные), Signature. Подпись вычисляется: HMACSHA256(base64(header) + '.' + base64(payload), secretKey). Сервер верифицирует токен, пересчитывая подпись и сравнивая — подделать токен без секрета невозможно." },
        { q: "Как реализовать logout при использовании stateless JWT и какие подходы существуют для инвалидации токенов?", a: "Поскольку JWT stateless, сервер не хранит список активных токенов. Подходы к инвалидации: 1) Короткий TTL access token — ограничивает окно атаки. 2) Token blacklist в Redis — хранить jti (JWT ID) инвалидированных токенов до их expiry. 3) Ротация секрета — инвалидирует все токены, но влияет на всех пользователей. Наиболее практичный: комбинация короткого TTL + blacklist." },
      ]
    },
    '05-08': {
      uk: [
        { q: "Що таке Multer і яка різниця між diskStorage та memoryStorage?", a: "Multer — middleware для Express, що обробляє multipart/form-data (завантаження файлів). diskStorage зберігає файли безпосередньо на диск у задану директорію з можливістю налаштування імені файлу через filename. memoryStorage тримає файл у пам'яті як Buffer у req.file.buffer — зручно для подальшої обробки або передачі у хмарне сховище (S3) без запису на диск." },
        { q: "Як обмежити тип та розмір файлів, що завантажуються через Multer?", a: "Розмір обмежується через опцію limits: multer({ limits: { fileSize: 5 * 1024 * 1024 } }) — максимум 5 МБ. Тип файлу перевіряється через fileFilter: функція отримує req, file та cb. Перевіряють file.mimetype або розширення file.originalname. Для дозволу: cb(null, true), для відхилення: cb(new Error('Wrong type'), false)." },
        { q: "Яка різниця між single(), array() та fields() методами Multer і коли застосовують кожен?", a: "upload.single('avatar') — один файл з поля avatar, доступний у req.file. upload.array('photos', 5) — до 5 файлів з одного поля, доступні у req.files. upload.fields([{ name: 'avatar', maxCount: 1 }, { name: 'docs', maxCount: 3 }]) — кілька різних полів, доступні у req.files як об'єкт з ключами за іменем поля." },
      ],
      ru: [
        { q: "Что такое Multer и в чём разница между diskStorage и memoryStorage?", a: "Multer — middleware для Express, обрабатывающий multipart/form-data (загрузку файлов). diskStorage сохраняет файлы непосредственно на диск в заданную директорию с возможностью настройки имени через filename. memoryStorage держит файл в памяти как Buffer в req.file.buffer — удобно для дальнейшей обработки или передачи в облачное хранилище (S3) без записи на диск." },
        { q: "Как ограничить тип и размер файлов, загружаемых через Multer?", a: "Размер ограничивается через опцию limits: multer({ limits: { fileSize: 5 * 1024 * 1024 } }) — максимум 5 МБ. Тип файла проверяется через fileFilter: функция получает req, file и cb. Проверяют file.mimetype или расширение file.originalname. Для разрешения: cb(null, true), для отклонения: cb(new Error('Wrong type'), false)." },
        { q: "В чём разница между методами single(), array() и fields() Multer и когда применяют каждый?", a: "upload.single('avatar') — один файл из поля avatar, доступен в req.file. upload.array('photos', 5) — до 5 файлов из одного поля, доступны в req.files. upload.fields([{ name: 'avatar', maxCount: 1 }, { name: 'docs', maxCount: 3 }]) — несколько разных полей, доступны в req.files как объект с ключами по имени поля." },
      ]
    },
    '05-09': {
      uk: [
        { q: "Як інтегрувати Zod-схему у Express middleware для валідації тіла запиту?", a: "Створюють middleware: (req, res, next) => { const result = schema.safeParse(req.body); if (!result.success) { return res.status(422).json({ errors: result.error.flatten() }); } req.body = result.data; next(); }. Передають цей middleware до route-обробника. result.data містить дані після парсингу та відповідно до схеми (типізовані)." },
        { q: "Яка різниця між schema.parse() та schema.safeParse() у Zod?", a: "parse() кидає ZodError при невалідних даних — підходить для випадків, де помилка означає баг (не очікувані вхідні дані). safeParse() повертає { success: true, data } або { success: false, error } без кидання виключення — ідеально для валідації user input, де помилка є очікуваним сценарієм і потребує повернення 422 клієнту." },
        { q: "Як Zod дозволяє валідувати query-параметри, враховуючи що вони завжди є рядками?", a: "Query-параметри у req.query завжди рядки (або масиви рядків). Zod-схема для query має перетворювати типи через coerce або preprocess: z.object({ page: z.coerce.number().int().positive(), limit: z.coerce.number().int().max(100) }). z.coerce.number() автоматично конвертує '10' у 10 перед валідацією." },
      ],
      ru: [
        { q: "Как интегрировать Zod-схему в Express middleware для валидации тела запроса?", a: "Создают middleware: (req, res, next) => { const result = schema.safeParse(req.body); if (!result.success) { return res.status(422).json({ errors: result.error.flatten() }); } req.body = result.data; next(); }. Передают этот middleware обработчику маршрута. result.data содержит данные после парсинга согласно схеме (типизированные)." },
        { q: "В чём разница между schema.parse() и schema.safeParse() в Zod?", a: "parse() бросает ZodError при невалидных данных — подходит для случаев, где ошибка означает баг (неожидаемые входные данные). safeParse() возвращает { success: true, data } или { success: false, error } без бросания исключения — идеально для валидации user input, где ошибка является ожидаемым сценарием и требует возврата 422 клиенту." },
        { q: "Как Zod позволяет валидировать query-параметры, учитывая что они всегда являются строками?", a: "Query-параметры в req.query всегда строки (или массивы строк). Zod-схема для query должна преобразовывать типы через coerce или preprocess: z.object({ page: z.coerce.number().int().positive(), limit: z.coerce.number().int().max(100) }). z.coerce.number() автоматически конвертирует '10' в 10 перед валидацией." },
      ]
    },
    '05-10': {
      uk: [
        { q: "Як Express розрізняє звичайний middleware та error-handling middleware і де саме потрібно реєструвати error handler?", a: "Express визначає error handler за кількістю параметрів: функція з чотирма параметрами (err, req, res, next) є error handler. Вона реєструється після всіх маршрутів і звичайних middleware: app.use((err, req, res, next) => {...}). Звичайний middleware з трьома параметрами не отримає контроль при виклику next(err)." },
        { q: "Як створити кастомний клас помилки, що передає HTTP-статус-код та ідентифікатор помилки у централізований обробник?", a: "class AppError extends Error { constructor(message, statusCode, code) { super(message); this.statusCode = statusCode; this.code = code; } }. У маршруті: throw new AppError('User not found', 404, 'USER_NOT_FOUND'). Error middleware перевіряє instanceof AppError і повертає err.statusCode, або 500 для неочікуваних помилок." },
        { q: "Як правильно обробити помилки у async route-обробниках Express, щоб вони потрапляли до error middleware?", a: "Express 5 автоматично ловить відхилені Promise у async handlers. У Express 4 потрібно або обгорнути: const asyncHandler = fn => (req, res, next) => fn(req, res, next).catch(next), або використовувати try/catch з next(err). Без цього відхилений Promise призведе до UnhandledPromiseRejection, а не до error middleware." },
      ],
      ru: [
        { q: "Как Express различает обычный middleware и error-handling middleware и где именно нужно регистрировать error handler?", a: "Express определяет error handler по количеству параметров: функция с четырьмя параметрами (err, req, res, next) является error handler. Она регистрируется после всех маршрутов и обычных middleware: app.use((err, req, res, next) => {...}). Обычный middleware с тремя параметрами не получит управление при вызове next(err)." },
        { q: "Как создать кастомный класс ошибки, передающий HTTP-статус-код и идентификатор ошибки в централизованный обработчик?", a: "class AppError extends Error { constructor(message, statusCode, code) { super(message); this.statusCode = statusCode; this.code = code; } }. В маршруте: throw new AppError('User not found', 404, 'USER_NOT_FOUND'). Error middleware проверяет instanceof AppError и возвращает err.statusCode, или 500 для неожиданных ошибок." },
        { q: "Как правильно обработать ошибки в async route-обработчиках Express, чтобы они попадали в error middleware?", a: "Express 5 автоматически ловит отклонённые Promise в async handlers. В Express 4 нужно либо обернуть: const asyncHandler = fn => (req, res, next) => fn(req, res, next).catch(next), либо использовать try/catch с next(err). Без этого отклонённый Promise приведёт к UnhandledPromiseRejection, а не в error middleware." },
      ]
    },
    '05-11': {
      uk: [
        { q: "Навіщо зберігати конфіденційні дані у .env-файлі і чому цей файл не можна комітити у git?", a: ".env зберігає секрети (паролі БД, API-ключі, JWT-секрет) окремо від коду, щоб вони не потрапили у репозиторій. У git комітять .env.example з порожніми або фіктивними значеннями як зразок. Якщо .env потрапить у публічний репозиторій, зловмисники отримають повний доступ до сервісів. .env додають у .gitignore з першого коміту." },
        { q: "Як dotenv.config() завантажує змінні оточення і де потрібно його викликати у застосунку?", a: "dotenv.config() читає файл .env у поточній директорії і встановлює кожну змінну у process.env, якщо вона ще не встановлена. Викликати якомога раніше — перший рядок точки входу (index.js/server.js), до будь-яких import/require конфіг-файлів або ініціалізації БД, бо вони вже можуть звертатися до process.env." },
        { q: "Яка перевага використання конфіг-модуля (config.js) що re-export значення з process.env замість прямого звертання до process.env по всьому коду?", a: "Конфіг-модуль централізує всі змінні оточення: export const DB_URL = process.env.DATABASE_URL || throwMissing('DATABASE_URL'). Переваги: 1) Можна перевірити наявність обов'язкових змінних при старті, а не при першому використанні. 2) Типізація (TypeScript). 3) Єдине місце для зміни назв змінних. 4) Легше мокувати у тестах." },
      ],
      ru: [
        { q: "Зачем хранить конфиденциальные данные в .env-файле и почему этот файл нельзя коммитить в git?", a: ".env хранит секреты (пароли БД, API-ключи, JWT-секрет) отдельно от кода, чтобы они не попали в репозиторий. В git коммитят .env.example с пустыми или фиктивными значениями как образец. Если .env попадёт в публичный репозиторий, злоумышленники получат полный доступ к сервисам. .env добавляют в .gitignore с первого коммита." },
        { q: "Как dotenv.config() загружает переменные окружения и где нужно его вызывать в приложении?", a: "dotenv.config() читает файл .env в текущей директории и устанавливает каждую переменную в process.env, если она ещё не установлена. Вызывать как можно раньше — первая строка точки входа (index.js/server.js), до любых import/require конфиг-файлов или инициализации БД, так как они уже могут обращаться к process.env." },
        { q: "Какое преимущество использования конфиг-модуля (config.js) реэкспортирующего значения из process.env вместо прямого обращения к process.env по всему коду?", a: "Конфиг-модуль централизует все переменные окружения: export const DB_URL = process.env.DATABASE_URL || throwMissing('DATABASE_URL'). Преимущества: 1) Можно проверить наличие обязательных переменных при старте, а не при первом использовании. 2) Типизация (TypeScript). 3) Единое место для изменения названий переменных. 4) Легче мокировать в тестах." },
      ]
    },
  };
  Object.keys(QUIZ).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.quiz = QUIZ[id];
  });
})();
