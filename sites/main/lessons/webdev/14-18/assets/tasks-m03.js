/* Додаткові завдання · Модуль 03 — React basics */
(function () {
  'use strict';
  var EXT = {
    '03-01': [
      { level:'medium', uk:'Поясни в коді чому Virtual DOM ефективніший за пряму маніпуляцію DOM: створи два рядки коду — один через document.createElement і другий через JSX. Заміряй час рендеру 1000 елементів для обох підходів.', ru:'Объясни в коде почему Virtual DOM эффективнее прямой манипуляции DOM: создай две строки кода — одну через document.createElement и вторую через JSX. Замерь время рендера 1000 элементов для обоих подходов.' },
      { level:'hard',   uk:'Реалізуй мікро-реакт: createElement(type, props, ...children) що будує VDOM-дерево і render(vnode, container) що створює реальний DOM. Підтримай вкладеність і атрибути className, onClick.', ru:'Реализуй микро-реакт: createElement(type, props, ...children) строящий VDOM-дерево и render(vnode, container) создающий реальный DOM. Поддержи вложенность и атрибуты className, onClick.' },
      { level:'hard',   uk:'Створи компонент-дерево з трьох рівнів: App -> Section -> Card. Кожен компонент рендерить список дочірніх елементів. Виведи повну структуру VDOM у консоль через console.log(React.createElement(...)).', ru:'Создай дерево компонентов из трёх уровней: App -> Section -> Card. Каждый компонент рендерит список дочерних элементов. Выведи полную структуру VDOM в консоль через console.log(React.createElement(...)).' },
      { level:'extra',  uk:'Реалізуй власний reconciler: diff(oldVdom, newVdom) що повертає мінімальний список патчів (INSERT, UPDATE, DELETE), і patch(dom, patches) що застосовує їх до реального DOM. Продемонструй на динамічному списку.', ru:'Реализуй собственный reconciler: diff(oldVdom, newVdom) возвращающий минимальный список патчей (INSERT, UPDATE, DELETE), и patch(dom, patches) применяющий их к реальному DOM. Продемонстрируй на динамическом списке.' },
    ],
    '03-02': [
      { level:'medium', uk:'Створи компонент UserCard що приймає ім\'я і email і рендерить картку. Виведи список з 5 різних користувачів через масив і map. Застосуй базові CSS-стилі через className.', ru:'Создай компонент UserCard принимающий имя и email и рендерящий карточку. Выведи список из 5 разных пользователей через массив и map. Примени базовые CSS-стили через className.' },
      { level:'hard',   uk:'Реалізуй компонент SearchableList що рендерить список елементів з полем пошуку. Фільтрація відбувається на клієнті. Переконайся що компонент повністю незалежний і приймає лише items через props.', ru:'Реализуй компонент SearchableList рендерящий список элементов с полем поиска. Фильтрация происходит на клиенте. Убедись что компонент полностью независимый и принимает только items через props.' },
      { level:'hard',   uk:'Напиши компонент Avatar що показує зображення користувача або його ініціали якщо зображення відсутнє. Обробляй onError зображення і плавно переходь до fallback-версії.', ru:'Напиши компонент Avatar показывающий изображение пользователя или его инициалы если изображение отсутствует. Обрабатывай onError изображения и плавно переходи к fallback-версии.' },
      { level:'extra',  uk:'Побудуй систему компонентів для data table: Table, TableHead, TableBody, TableRow, TableCell. Підтримай сортування по колонці, пагінацію і вибір рядків. Всі стани — у батьківському компоненті.', ru:'Построй систему компонентов для data table: Table, TableHead, TableBody, TableRow, TableCell. Поддержи сортировку по колонке, пагинацию и выбор строк. Все состояния — в родительском компоненте.' },
    ],
    '03-03': [
      { level:'medium', uk:'Напиши компонент Button з пропсами variant (\'primary\'|\'secondary\'|\'danger\'), size (\'sm\'|\'md\'|\'lg\'), disabled і onClick. Встанови defaultProps і задокументуй PropTypes.', ru:'Напиши компонент Button с пропсами variant (\'primary\'|\'secondary\'|\'danger\'), size (\'sm\'|\'md\'|\'lg\'), disabled и onClick. Установи defaultProps и задокументируй PropTypes.' },
      { level:'hard',   uk:'Реалізуй компонент DataGrid<T> що приймає columns: {key: keyof T, label: string}[] і rows: T[]. Він має автоматично рендерити правильні колонки і підтримувати custom renderCell для кожної колонки.', ru:'Реализуй компонент DataGrid<T> принимающий columns: {key: keyof T, label: string}[] и rows: T[]. Он должен автоматически рендерить правильные колонки и поддерживать custom renderCell для каждой колонки.' },
      { level:'hard',   uk:'Напиши компонент Alert з props: type (\'info\'|\'warning\'|\'error\'|\'success\'), title, children і onClose. Реалізуй анімацію появи і зникнення через CSS transitions і управляй нею через props.', ru:'Напиши компонент Alert с props: type (\'info\'|\'warning\'|\'error\'|\'success\'), title, children и onClose. Реализуй анимацию появления и исчезновения через CSS transitions и управляй ею через props.' },
      { level:'extra',  uk:'Побудуй повноцінний компонент Form з конфігураційним API: fields: FieldConfig[] де кожен field описує тип інпуту, валідацію, залежності. Form автоматично рендерить поля і збирає валідований результат.', ru:'Построй полноценный компонент Form с конфигурационным API: fields: FieldConfig[] где каждый field описывает тип инпута, валидацию, зависимости. Form автоматически рендерит поля и собирает валидированный результат.' },
    ],
    '03-04': [
      { level:'medium', uk:'Реалізуй компонент Counter з кнопками + і - і відображенням значення. Додай кнопку Reset. Потім реалізуй MultiCounter що підтримує кілька незалежних лічильників через масив у state.', ru:'Реализуй компонент Counter с кнопками + и - и отображением значения. Добавь кнопку Reset. Потом реализуй MultiCounter поддерживающий несколько независимых счётчиков через массив в state.' },
      { level:'hard',   uk:'Побудуй компонент Accordion з кількома секціями. Стан відкритої секції зберігається в useState. Реалізуй два режими: single (лише одна відкрита) і multi (можуть бути відкриті кілька одночасно).', ru:'Построй компонент Accordion с несколькими секциями. Состояние открытой секции хранится в useState. Реализуй два режима: single (только одна открытая) и multi (могут быть открыты несколько одновременно).' },
      { level:'hard',   uk:'Реалізуй компонент PixelCanvas: сітка 20x20 комірок де клік на комірку перемикає її колір. Стан всієї сітки у одному useState. Додай кнопки Clear і Export (скачати як PNG через canvas API).', ru:'Реализуй компонент PixelCanvas: сетка 20x20 ячеек где клик на ячейку переключает её цвет. Состояние всей сетки в одном useState. Добавь кнопки Clear и Export (скачать как PNG через canvas API).' },
      { level:'extra',  uk:'Реалізуй компонент Kanban Board: колонки To Do, In Progress, Done. Картки переміщуються між колонками через drag-and-drop (або кнопки). Увесь стан у useState, підтримай додавання і видалення карток.', ru:'Реализуй компонент Kanban Board: колонки To Do, In Progress, Done. Карточки перемещаются между колонками через drag-and-drop (или кнопки). Весь стейт в useState, поддержи добавление и удаление карточек.' },
    ],
    '03-05': [
      { level:'medium', uk:'Напиши компонент що завантажує список постів з JSONPlaceholder API через useEffect і показує spinner під час завантаження. Реалізуй обробку помилок і retry кнопку.', ru:'Напиши компонент загружающий список постов из JSONPlaceholder API через useEffect и показывающий spinner во время загрузки. Реализуй обработку ошибок и кнопку retry.' },
      { level:'hard',   uk:'Реалізуй компонент Timer з useEffect: показує час, що минув з моменту монтування. Переконайся що інтервал очищається при unmount. Додай кнопки Pause/Resume що керують intervalRef через useRef.', ru:'Реализуй компонент Timer с useEffect: показывает прошедшее время с момента монтирования. Убедись что интервал очищается при unmount. Добавь кнопки Pause/Resume управляющие intervalRef через useRef.' },
      { level:'hard',   uk:'Напиши компонент що синхронізує стан з URL через useEffect і window.history.pushState. При зміні фільтра URL оновлюється, при завантаженні сторінки стан відновлюється з URL.', ru:'Напиши компонент синхронизирующий состояние с URL через useEffect и window.history.pushState. При изменении фильтра URL обновляется, при загрузке страницы состояние восстанавливается из URL.' },
      { level:'extra',  uk:'Реалізуй WebSocket чат з useEffect: підключення при mount, відписка при unmount, авто-реконект з exponential backoff. Показуй статус з\'єднання, чергу не надісланих повідомлень і індикатор \'typing\'.', ru:'Реализуй WebSocket чат с useEffect: подключение при mount, отписка при unmount, авто-реконнект с exponential backoff. Показывай статус соединения, очередь не отправленных сообщений и индикатор \'typing\'.' },
    ],
    '03-06': [
      { level:'medium', uk:'Реалізуй компонент що відображає різний контент в залежності від ролі користувача: admin, editor, viewer. Використай всі три підходи — &&, тернарний оператор і окрему функцію renderContent(role).', ru:'Реализуй компонент отображающий разный контент в зависимости от роли пользователя: admin, editor, viewer. Используй все три подхода — &&, тернарный оператор и отдельную функцию renderContent(role).' },
      { level:'hard',   uk:'Побудуй компонент-wizard (майстер налаштувань) з 4 кроками. Умовний рендеринг показує лише поточний крок. Навігація вперед доступна лише якщо поточний крок валідний.', ru:'Построй компонент-wizard (мастер настроек) с 4 шагами. Условный рендеринг показывает только текущий шаг. Навигация вперёд доступна только если текущий шаг валиден.' },
      { level:'hard',   uk:'Реалізуй компонент FeatureFlag: <FeatureFlag name="new-ui" fallback={<OldUI/>}><NewUI/></FeatureFlag> що перевіряє список увімкнених фіч і рендерить відповідний варіант. Фічі зберігаються в localStorage.', ru:'Реализуй компонент FeatureFlag: <FeatureFlag name="new-ui" fallback={<OldUI/>}><NewUI/></FeatureFlag> проверяющий список включённых фич и рендерящий соответствующий вариант. Фичи хранятся в localStorage.' },
      { level:'extra',  uk:'Реалізуй компонент Dashboard що рендерить різні layouts (grid, list, kanban) і різні empty states залежно від даних, прав доступу і поточного режиму. Уникни вкладених тернарних операторів через early return.', ru:'Реализуй компонент Dashboard рендерящий разные layouts (grid, list, kanban) и разные empty states в зависимости от данных, прав доступа и текущего режима. Избегай вложенных тернарных операторов через early return.' },
    ],
    '03-07': [
      { level:'medium', uk:'Реалізуй компонент TodoList: масив завдань рендериться через map, кожне завдання має унікальний key. Реалізуй додавання, видалення і позначення виконаним. Не мутуй масив — використовуй spread і filter.', ru:'Реализуй компонент TodoList: массив задач рендерится через map, каждая задача имеет уникальный key. Реализуй добавление, удаление и пометку выполненным. Не мутируй массив — используй spread и filter.' },
      { level:'hard',   uk:'Реалізуй компонент VirtualList що рендерить лише видимі елементи зі списку 10 000 записів. Обчислюй які елементи в viewport через scrollTop і висоту елемента. Використай React.Fragment для обгортки.', ru:'Реализуй компонент VirtualList рендерящий только видимые элементы из списка 10 000 записей. Вычисляй какие элементы в viewport через scrollTop и высоту элемента. Используй React.Fragment для обёртки.' },
      { level:'hard',   uk:'Напиши компонент TreeView що рекурсивно рендерить дерево категорій (node: {id, name, children?}). Підтримай розгортання/згортання вузлів і множинне виділення через checkbox. Унікальні key для кожного вузла.', ru:'Напиши компонент TreeView рекурсивно рендерящий дерево категорий (node: {id, name, children?}). Поддержи разворачивание/сворачивание узлов и множественное выделение через checkbox. Уникальные key для каждого узла.' },
      { level:'extra',  uk:'Реалізуй компонент таблиці з безмежним скролом (infinite scroll): завантажуй наступну сторінку коли користувач досягає кінця. Використай IntersectionObserver для детекції і React.Fragment для рядків.', ru:'Реализуй компонент таблицы с бесконечной прокруткой (infinite scroll): загружай следующую страницу когда пользователь достигает конца. Используй IntersectionObserver для детекции и React.Fragment для строк.' },
    ],
    '03-08': [
      { level:'medium', uk:'Реалізуй форму реєстрації з полями: ім\'я, email, пароль, підтвердження паролю. Всі поля — controlled components. Показуй inline помилки валідації при blur і блокуй submit якщо форма невалідна.', ru:'Реализуй форму регистрации с полями: имя, email, пароль, подтверждение пароля. Все поля — controlled components. Показывай inline ошибки валидации при blur и блокируй submit если форма невалидна.' },
      { level:'hard',   uk:'Реалізуй dynamic form builder: конфігурація fields: FieldConfig[] описує поля, їх тип (text, select, checkbox, textarea) і правила валідації. Компонент DynamicForm рендерить і валідує форму автоматично.', ru:'Реализуй dynamic form builder: конфигурация fields: FieldConfig[] описывает поля, их тип (text, select, checkbox, textarea) и правила валидации. Компонент DynamicForm рендерит и валидирует форму автоматически.' },
      { level:'hard',   uk:'Реалізуй uncontrolled форму через useRef для максимальної продуктивності при великій кількості полів. Порівняй re-render count з controlled версією. Додай custom валідацію при submit.', ru:'Реализуй uncontrolled форму через useRef для максимальной производительности при большом количестве полей. Сравни re-render count с controlled версией. Добавь кастомную валидацию при submit.' },
      { level:'extra',  uk:'Реалізуй multi-step форму з persisted state: кожен крок зберігається в sessionStorage. При перезавантаженні сторінки форма відновлюється з того ж кроку. Реалізуй валідацію кожного кроку і progress bar.', ru:'Реализуй multi-step форму с persisted state: каждый шаг сохраняется в sessionStorage. При перезагрузке страницы форма восстанавливается с того же шага. Реализуй валидацию каждого шага и progress bar.' },
    ],
    '03-09': [
      { level:'medium', uk:'Реалізуй компонент FilteredList де фільтр і список — дочірні компоненти, а спільний стан (searchQuery) піднятий у батьківський компонент. Обидва дочірніх компоненти отримують state і setState через props.', ru:'Реализуй компонент FilteredList где фильтр и список — дочерние компоненты, а общий стейт (searchQuery) поднят в родительский. Оба дочерних компонента получают state и setState через props.' },
      { level:'hard',   uk:'Реалізуй компонент ShoppingCart: ProductList і Cart — брати, батько App тримає items[]. Вибір товару в ProductList оновлює кошик в Cart через спільний стейт в App. Уникни prop drilling більше ніж 2 рівні.', ru:'Реализуй компонент ShoppingCart: ProductList и Cart — братья, родитель App хранит items[]. Выбор товара в ProductList обновляет корзину в Cart через общий стейт в App. Избегай prop drilling глубже 2 уровней.' },
      { level:'hard',   uk:'Побудуй компонент DateRangePicker: два DateInput (start і end) — дочірні компоненти, батько валідує що start <= end і показує помилку. При виборі start, кінцева дата автоматично обнуляється якщо вона раніша.', ru:'Построй компонент DateRangePicker: два DateInput (start и end) — дочерние компоненты, родитель валидирует что start <= end и показывает ошибку. При выборе start, конечная дата автоматически сбрасывается если она раньше.' },
      { level:'extra',  uk:'Реалізуй компонент Spreadsheet (таблиця як Excel): стан всіх комірок у батьківському компоненті як 2D масив. Дочірній Cell може бути в режимі перегляду чи редагування. Підтримай Tab/Enter навігацію і формули виду =A1+B2.', ru:'Реализуй компонент Spreadsheet (таблица как Excel): состояние всех ячеек в родительском компоненте как 2D массив. Дочерний Cell может быть в режиме просмотра или редактирования. Поддержи Tab/Enter навигацию и формулы вида =A1+B2.' },
    ],
    '03-10': [
      { level:'medium', uk:'Реалізуй компонент Card що приймає children і рендерить їх усередині. Потім реалізуй компоненти CardHeader, CardBody, CardFooter і продемонструй їх композицію у складній картці.', ru:'Реализуй компонент Card принимающий children и рендерящий их внутри. Потом реализуй компоненты CardHeader, CardBody, CardFooter и продемонстрируй их композицию в сложной карточке.' },
      { level:'hard',   uk:'Реалізуй render prop паттерн: компонент DataFetcher<T> що завантажує дані і викликає render({ data, loading, error }) — children як функція. Використай для рендеру двох різних UI з одними даними.', ru:'Реализуй render prop паттерн: компонент DataFetcher<T> загружающий данные и вызывающий render({ data, loading, error }) — children как функция. Используй для рендера двух разных UI с одними данными.' },
      { level:'hard',   uk:'Реалізуй систему slot\'ів: компонент Layout приймає пропси header, sidebar, footer і children — кожен є React Node. Це дозволяє замінити будь-яку зону без зміни компонента Layout.', ru:'Реализуй систему слотов: компонент Layout принимает пропсы header, sidebar, footer и children — каждый является React Node. Это позволяет заменить любую зону без изменения компонента Layout.' },
      { level:'extra',  uk:'Реалізуй повноцінну компонентну бібліотеку UI-kit: Button, Input, Modal, Dropdown, Tabs — кожен з variants, sizes і a11y атрибутами. Компоненти компонуються між собою. Підключи Storybook для документації.', ru:'Реализуй полноценную компонентную библиотеку UI-kit: Button, Input, Modal, Dropdown, Tabs — каждый с variants, sizes и a11y атрибутами. Компоненты компонуются между собой. Подключи Storybook для документации.' },
    ],
    '03-11': [
      { level:'medium', uk:'Налаштуй React Router 6 і створи 4 маршрути: /, /about, /users, /users/:id. Реалізуй навігаційне меню з <Link> де активний маршрут підсвічується через NavLink і його className.', ru:'Настрой React Router 6 и создай 4 маршрута: /, /about, /users, /users/:id. Реализуй навигационное меню с <Link> где активный маршрут подсвечивается через NavLink и его className.' },
      { level:'hard',   uk:'Реалізуй захищені маршрути: компонент ProtectedRoute що перевіряє isAuthenticated і редіректить на /login через useNavigate якщо не авторизований. Зберігай originalPath і редіректь назад після логіну.', ru:'Реализуй защищённые маршруты: компонент ProtectedRoute проверяющий isAuthenticated и редиректящий на /login через useNavigate если не авторизован. Сохраняй originalPath и редиректи обратно после логина.' },
      { level:'hard',   uk:'Реалізуй breadcrumbs компонент що автоматично будується з поточного маршруту. Використай useLocation і useParams для отримання шляху і параметрів. Кожен сегмент шляху є клікабельним посиланням.', ru:'Реализуй компонент breadcrumbs автоматически строящийся из текущего маршрута. Используй useLocation и useParams для получения пути и параметров. Каждый сегмент пути является кликабельной ссылкой.' },
      { level:'extra',  uk:'Побудуй повну систему маршрутизації: вкладені маршрути, lazy loading для кожного модуля, перехід між маршрутами зі збереженням scroll position, анімований перехід між сторінками і 404 сторінка.', ru:'Построй полную систему маршрутизации: вложенные маршруты, lazy loading для каждого модуля, переход между маршрутами с сохранением scroll position, анимированный переход между страницами и 404 страница.' },
    ],
    '03-12': [
      { level:'medium', uk:'Реалізуй сторінку пошуку що зберігає query, page і sort у URL через useSearchParams. При перезавантаженні сторінки стан відновлюється. Кнопка \'Поділитись\' копіює URL до буфера обміну.', ru:'Реализуй страницу поиска сохраняющую query, page и sort в URL через useSearchParams. При перезагрузке страницы состояние восстанавливается. Кнопка \'Поделиться\' копирует URL в буфер обмена.' },
      { level:'hard',   uk:'Реалізуй компонент DataTable з пагінацією де поточна сторінка, розмір сторінки і сортування синхронізовані з URL через useSearchParams. Навігація кнопками \'Назад\' у браузері коректно відновлює попередній стан.', ru:'Реализуй компонент DataTable с пагинацией где текущая страница, размер страницы и сортировка синхронизированы с URL через useSearchParams. Навигация кнопками \'Назад\' в браузере корректно восстанавливает предыдущее состояние.' },
      { level:'hard',   uk:'Реалізуй маршрут деталей з state: з карточки товару переходь на /products/:id і передай повний об\'єкт товару через state (useNavigate). На сторінці деталей використай useLocation().state як початкові дані до завантаження з API.', ru:'Реализуй маршрут деталей со state: с карточки товара переходи на /products/:id и передай полный объект товара через state (useNavigate). На странице деталей используй useLocation().state как начальные данные до загрузки из API.' },
      { level:'extra',  uk:'Реалізуй складну URL-based фільтрацію: масив фільтрів зберігається в searchParams як JSON або через multiple values (?tag=js&tag=ts). Синхронізуй з Redux store або useReducer. Підтримай глибоке посилання з будь-якого стану фільтра.', ru:'Реализуй сложную URL-based фильтрацию: массив фильтров хранится в searchParams как JSON или через multiple values (?tag=js&tag=ts). Синхронизируй с Redux store или useReducer. Поддержи глубокую ссылку из любого состояния фильтра.' },
    ],
    '03-13': [
      { level:'medium', uk:'Розбий SPA на lazy chunks: кожна сторінка (Home, About, Dashboard) завантажується окремо через React.lazy() і Suspense з fallback spinner. Перевір у DevTools Network що чанки завантажуються лише при переході.', ru:'Раздели SPA на lazy chunks: каждая страница (Home, About, Dashboard) загружается отдельно через React.lazy() и Suspense с fallback spinner. Проверь в DevTools Network что чанки загружаются только при переходе.' },
      { level:'hard',   uk:'Реалізуй preloading компонентів: при hover над посиланням у навбарі починай завантажувати відповідний chunk. Це зменшує perceived latency. Використай import() безпосередньо в onMouseEnter обробнику.', ru:'Реализуй preloading компонентов: при hover над ссылкой в навбаре начинай загружать соответствующий chunk. Это уменьшает perceived latency. Используй import() непосредственно в обработчике onMouseEnter.' },
      { level:'hard',   uk:'Реалізуй retry механізм для lazy компонентів: якщо chunk не завантажився (мережева помилка) — кнопка \'Retry\' перезапускає завантаження. Використай Error Boundary з можливістю reset.', ru:'Реализуй retry механизм для lazy компонентов: если chunk не загрузился (сетевая ошибка) — кнопка \'Retry\' перезапускает загрузку. Используй Error Boundary с возможностью reset.' },
      { level:'extra',  uk:'Реалізуй розумну систему prefetch: аналізуй маршрути які найчастіше відвідуються після поточного (через аналітику) і prefetch їх chunk\'и у фоні після idle. Використай requestIdleCallback і динамічний import().', ru:'Реализуй умную систему prefetch: анализируй маршруты которые чаще всего посещаются после текущего (через аналитику) и prefetch их чанки в фоне после idle. Используй requestIdleCallback и динамический import().' },
    ],
    '03-14': [
      { level:'medium', uk:'Реалізуй хук useFetch<T>(url: string): { data: T | null, loading: boolean, error: Error | null } що скасовує fetch при unmount через AbortController. Перевір що при зміні url старий запит скасовується.', ru:'Реализуй хук useFetch<T>(url: string): { data: T | null, loading: boolean, error: Error | null } отменяющий fetch при unmount через AbortController. Проверь что при изменении url старый запрос отменяется.' },
    ],
    '03-14-b': [
      { level:'hard',   uk:'Реалізуй хук useLocalStorage<T>(key: string, initialValue: T) що синхронізує стан з localStorage. При зміні значення в іншій вкладці (storage event) стан автоматично оновлюється.', ru:'Реализуй хук useLocalStorage<T>(key: string, initialValue: T) синхронизирующий состояние с localStorage. При изменении значения в другой вкладке (storage event) состояние автоматически обновляется.' },
      { level:'hard',   uk:'Реалізуй хук useDebounce<T>(value: T, delay: number): T що повертає debounced значення. Також реалізуй useThrottle<T>(value: T, limit: number): T. Продемонструй на пошуковому полі.', ru:'Реализуй хук useDebounce<T>(value: T, delay: number): T возвращающий debounced значение. Также реализуй useThrottle<T>(value: T, limit: number): T. Продемонстрируй на поисковом поле.' },
      { level:'extra',  uk:'Реалізуй хук useAsync<T>(asyncFn, deps) що автоматично запускає асинхронну функцію при зміні deps, керує loading/error/data станами, підтримує скасування і retry з exponential backoff.', ru:'Реализуй хук useAsync<T>(asyncFn, deps) автоматически запускающий асинхронную функцию при изменении deps, управляющий loading/error/data состояниями, поддерживающий отмену и retry с exponential backoff.' },
    ],
    '03-15': [
      { level:'medium', uk:'Реалізуй компонент AutofocusInput що автоматично фокусується при монтуванні через useRef. Потім реалізуй компонент ScrollToTop: кнопка що плавно скролить сторінку вгору через ref на div.', ru:'Реализуй компонент AutofocusInput автоматически фокусирующийся при монтировании через useRef. Потом реализуй компонент ScrollToTop: кнопка плавно скролящая страницу вверх через ref на div.' },
      { level:'hard',   uk:'Реалізуй хук usePrevious<T>(value: T): T | undefined через useRef що зберігає попереднє значення між рендерами без тригерингу ре-рендеру. Продемонструй на компоненті що показує diff між поточним і попереднім значенням.', ru:'Реализуй хук usePrevious<T>(value: T): T | undefined через useRef хранящий предыдущее значение между рендерами без тригеринга ре-рендера. Продемонстрируй на компоненте показывающем diff между текущим и предыдущим значением.' },
      { level:'hard',   uk:'Реалізуй компонент StopwatchWithLaps: useRef зберігає intervalId, useState — час і масив відмінних часів. Кнопки Start/Stop/Lap/Reset. Переконайся що Stop не робить зайвих ре-рендерів.', ru:'Реализуй компонент StopwatchWithLaps: useRef хранит intervalId, useState — время и массив отсечек. Кнопки Start/Stop/Lap/Reset. Убедись что Stop не вызывает лишних ре-рендеров.' },
      { level:'extra',  uk:'Реалізуй хук useClickOutside(ref, callback) що викликає callback коли клік стався поза елементом. Потім реалізуй Dropdown компонент що закривається при кліку поза ним. Обробляй і mousedown і touchstart.', ru:'Реализуй хук useClickOutside(ref, callback) вызывающий callback когда клик произошёл за пределами элемента. Потом реализуй компонент Dropdown закрывающийся при клике вне него. Обрабатывай и mousedown и touchstart.' },
    ],
    '03-16': [
      { level:'medium', uk:'Реалізуй компонент CustomInput з forwardRef щоб батьківський компонент міг викликати .focus() на ньому напряму. Продемонструй: при кліку кнопки у батьку — фокус переходить на input.', ru:'Реализуй компонент CustomInput с forwardRef чтобы родительский компонент мміг вызывать .focus() напрямую. Продемонстрируй: при клике кнопки у родителя — фокус переходит на input.' },
      { level:'hard',   uk:'Реалізуй компонент VideoPlayer з useImperativeHandle що виставляє API: { play, pause, seek(time), getCurrentTime() }. Батьківський компонент керує відтворенням через ref без доступу до внутрішнього video елементу.', ru:'Реализуй компонент VideoPlayer с useImperativeHandle выставляющий API: { play, pause, seek(time), getCurrentTime() }. Родительский компонент управляет воспроизведением через ref без доступа к внутреннему video элементу.' },
      { level:'hard',   uk:'Реалізуй компонент Modal з forwardRef і useImperativeHandle що виставляє { open(), close(), isOpen() }. Батьківський компонент відкриває модальне вікно через modalRef.current.open() без передачі isOpen через props.', ru:'Реализуй компонент Modal с forwardRef и useImperativeHandle выставляющий { open(), close(), isOpen() }. Родительский компонент открывает модальное окно через modalRef.current.open() без передачи isOpen через props.' },
      { level:'extra',  uk:'Реалізуй компонентну бібліотеку де всі інпути (TextInput, Select, DatePicker) приймають ref і виставляють уніфікований API: { focus(), blur(), getValue(), setValue(v), validate() }. Реалізуй компонент Form що автоматично збирає їх значення через refs.', ru:'Реализуй компонентную библиотеку где все инпуты (TextInput, Select, DatePicker) принимают ref и выставляют унифицированный API: { focus(), blur(), getValue(), setValue(v), validate() }. Реализуй компонент Form автоматически собирающий их значения через refs.' },
    ],
    '03-17': [
      { level:'medium', uk:'Реалізуй компонент Toast що рендериться поза основним деревом через ReactDOM.createPortal у div#toast-root. Покажи 3 повідомлення зі затримкою і переконайся що вони не впливають на layout основного контенту.', ru:'Реализуй компонент Toast рендерящийся вне основного дерева через ReactDOM.createPortal в div#toast-root. Покажи 3 сообщения с задержкой и убедись что они не влияют на layout основного контента.' },
      { level:'hard',   uk:'Реалізуй компонент Tooltip через Portal: при hover над будь-яким елементом показуй tooltip у body без проблем z-index і overflow:hidden батьківських контейнерів. Позиціонуй tooltip через getBoundingClientRect.', ru:'Реализуй компонент Tooltip через Portal: при hover над любым элементом показывай tooltip в body без проблем z-index и overflow:hidden родительских контейнеров. Позиционируй tooltip через getBoundingClientRect.' },
      { level:'hard',   uk:'Реалізуй систему модальних вікон через Portal: ModalManager підтримує стек модалів, ESC закриває верхній. Кожен модаль рендериться в окремий div через createPortal. Перший модаль блокує scroll.', ru:'Реализуй систему модальных окон через Portal: ModalManager поддерживает стек модалей, ESC закрывает верхний. Каждый модаль рендерится в отдельный div через createPortal. Первый модаль блокирует scroll.' },
      { level:'extra',  uk:'Реалізуй drag-and-drop між різними контейнерами використовуючи Portals: під час drag — копія елемента рендериться у body через Portal і слідує за курсором. При drop — анімовано вставляється у цільовий контейнер.', ru:'Реализуй drag-and-drop между разными контейнерами используя Portals: во время drag — копия элемента рендерится в body через Portal и следует за курсором. При drop — анимированно вставляется в целевой контейнер.' },
    ],
    '03-18': [
      { level:'medium', uk:'Реалізуй базовий Error Boundary клас-компонент з componentDidCatch і getDerivedStateFromError. Покажи fallback UI з повідомленням про помилку і кнопкою \'Спробувати знову\' що скидає стан.', ru:'Реализуй базовый Error Boundary класс-компонент с componentDidCatch и getDerivedStateFromError. Покажи fallback UI с сообщением об ошибке и кнопкой \'Попробовать снова\' сбрасывающей состояние.' },
      { level:'hard',   uk:'Реалізуй Error Boundary що логує помилки до зовнішнього сервісу (імітуй через console.error з деталями). Підтримай retry: кнопка \'Retry\' скидає error state і робить повторний рендер дочірніх компонентів.', ru:'Реализуй Error Boundary логирующий ошибки во внешний сервис (имитируй через console.error с деталями). Поддержи retry: кнопка \'Retry\' сбрасывает error state и делает повторный рендер дочерних компонентов.' },
      { level:'hard',   uk:'Реалізуй granular Error Boundaries: кожна секція Dashboard (Header, Sidebar, MainContent, Footer) обгорнута у власний Error Boundary з різними fallback UI. Помилка в одній секції не зупиняє інші.', ru:'Реализуй granular Error Boundaries: каждая секция Dashboard (Header, Sidebar, MainContent, Footer) обёрнута в собственный Error Boundary с разными fallback UI. Ошибка в одной секции не останавливает другие.' },
      { level:'extra',  uk:'Реалізуй систему graceful degradation: при помилці компонент показує спрощену версію себе замість повного fallback. Відстежуй кількість помилок: після 3 помилок показуй повний fallback. Логуй до Sentry (mock API).', ru:'Реализуй систему graceful degradation: при ошибке компонент показывает упрощённую версию себя вместо полного fallback. Отслеживай количество ошибок: после 3 ошибок показывай полный fallback. Логируй в Sentry (mock API).' },
    ],
    '03-19': [
      { level:'medium', uk:'Відкрий React DevTools і знайди компонент що робить зайві ре-рендери. Зафіксуй re-render причину (props, state, context). Оптимізуй через React.memo або useMemo і підтверди у Profiler що re-renders зменшились.', ru:'Открой React DevTools и найди компонент делающий лишние ре-рендеры. Зафиксируй причину re-render (props, state, context). Оптимизируй через React.memo или useMemo и подтверди в Profiler что re-renders уменьшились.' },
      { level:'hard',   uk:'Профілюй компонент зі списком 1000 елементів: знайди slow render через Profiler flamegraph. Оптимізуй через React.memo для дочірніх елементів і useCallback для обробників. Порівняй час рендеру до і після.', ru:'Профилируй компонент со списком 1000 элементов: найди slow render через Profiler flamegraph. Оптимизируй через React.memo для дочерних элементов и useCallback для обработчиков. Сравни время рендера до и после.' },
      { level:'hard',   uk:'Знайди і виправ проблему зайвого рендерингу через Context: компонент підписаний на великий Context об\'єкт але використовує лише одне поле. Розбий Context на менші або використай useMemo для ізоляції.', ru:'Найди и исправь проблему лишнего рендеринга через Context: компонент подписан на большой Context объект но использует только одно поле. Раздели Context на меньшие или используй useMemo для изоляции.' },
      { level:'extra',  uk:'Реалізуй систему моніторингу продуктивності: використай React Profiler API (onRender callback) для збору метрик всіх компонентів. Збудуй dashboard що показує топ-10 найповільніших компонентів у реальному часі.', ru:'Реализуй систему мониторинга производительности: используй React Profiler API (onRender callback) для сбора метрик всех компонентов. Построй dashboard показывающий топ-10 самых медленных компонентов в реальном времени.' },
    ],
    '03-20': [
      { level:'medium', uk:'Ініціалізуй SPA проект: Vite + React + TypeScript. Налаштуй ESLint, Prettier, absolute imports через tsconfig paths. Створи базову структуру папок: pages/, components/, hooks/, types/, services/.', ru:'Инициализируй SPA проект: Vite + React + TypeScript. Настрой ESLint, Prettier, absolute imports через tsconfig paths. Создай базовую структуру папок: pages/, components/, hooks/, types/, services/.' },
      { level:'hard',   uk:'Реалізуй повну систему авторизації: AuthContext, useAuth хук, захищені маршрути, токен у localStorage, автоматичне оновлення токена через interceptor. Всі типи TypeScript без жодного any.', ru:'Реализуй полную систему авторизации: AuthContext, useAuth хук, защищённые маршруты, токен в localStorage, автоматическое обновление токена через interceptor. Все типы TypeScript без единого any.' },
      { level:'hard',   uk:'Реалізуй глобальне керування станом без Redux: використай useReducer + Context. Визнач Actions, Reducer і typed dispatch. Підключи DevTools через middleware що логує всі actions у консоль.', ru:'Реализуй глобальное управление состоянием без Redux: используй useReducer + Context. Определи Actions, Reducer и typed dispatch. Подключи DevTools через middleware логирующий все actions в консоль.' },
      { level:'extra',  uk:'Побудуй повноцінний SPA: авторизація, список ресурсів з фільтрами у URL, деталі ресурсу, форма редагування з валідацією. Покрий критичні компоненти тестами (React Testing Library). CI через GitHub Actions запускає тести і type-check.', ru:'Построй полноценный SPA: авторизация, список ресурсов с фильтрами в URL, детали ресурса, форма редактирования с валидацией. Покрой критические компоненты тестами (React Testing Library). CI через GitHub Actions запускает тесты и type-check.' },
    ],
  };
  Object.keys(EXT).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.tasks = (l.tasks || []).concat(EXT[id]);
  });
})();
