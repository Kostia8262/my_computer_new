/* Додаткові завдання · Модуль 01 — Advanced JavaScript */
(function () {
  'use strict';
  var EXT = {
    '01-01': [
      { level:'medium', uk:'Створи об\'єкт з кастомним Symbol.toPrimitive, який повертає число при математичних операціях і рядок при конкатенації. Перевір +(obj) та `${obj}`.', ru:'Создай объект с кастомным Symbol.toPrimitive, который возвращает число при математических операциях и строку при конкатенации. Проверь +(obj) и `${obj}`.' },
      { level:'hard',   uk:'Реалізуй клас IterableRange, який приймає start і end і є iterable через Symbol.iterator. Він має підтримувати for...of і spread [...range].', ru:'Реализуй класс IterableRange, принимающий start и end и являющийся iterable через Symbol.iterator. Он должен поддерживать for...of и spread [...range].' },
      { level:'hard',   uk:'Напиши WeakMap-based кеш для приватних даних об\'єктів: функція withPrivate(obj, data) зберігає data і getPrivate(obj) повертає його. Переконайся що дані видаляються з пам\'яті при GC.', ru:'Напиши WeakMap-based кеш для приватных данных объектов: функция withPrivate(obj, data) сохраняет data и getPrivate(obj) возвращает его. Убедись что данные удаляются из памяти при GC.' },
      { level:'extra',  uk:'Реалізуй систему реактивних об\'єктів через Proxy: createReactive(obj, onChange) — будь-яка зміна властивості викликає onChange(key, oldVal, newVal). Підтримай вкладені об\'єкти.', ru:'Реализуй систему реактивных объектов через Proxy: createReactive(obj, onChange) — любое изменение свойства вызывает onChange(key, oldVal, newVal). Поддержи вложенные объекты.' },
    ],
    '01-02': [
      { level:'medium', uk:'Напиши базовий клас Animal з конструктором і методом speak(). Створи підкласи Dog і Cat що перевизначають speak(). Виведи тварин у масиві через for...of.', ru:'Напиши базовый класс Animal с конструктором и методом speak(). Создай подклассы Dog и Cat переопределяющие speak(). Выведи животных в массиве через for...of.' },
      { level:'hard',   uk:'Реалізуй систему Mixin\'ів: Serializable додає методи toJSON/fromJSON, Validatable додає validate(). Застосуй обидва до класу User через Object.assign до прототипу.', ru:'Реализуй систему Mixin\'ов: Serializable добавляет методы toJSON/fromJSON, Validatable добавляет validate(). Примени оба к классу User через Object.assign к прототипу.' },
      { level:'hard',   uk:'Змоделюй абстрактний клас Shape з абстрактним методом area(). Якщо хтось instantiates Shape напряму — кидай TypeError. Реалізуй Circle і Rectangle як конкретні підкласи.', ru:'Смоделируй абстрактный класс Shape с абстрактным методом area(). Если кто-то инстанциирует Shape напрямую — бросай TypeError. Реализуй Circle и Rectangle как конкретные подклассы.' },
      { level:'extra',  uk:'Реалізуй повну систему множинного наслідування через mixin-ланцюг: кожен mixin отримує superclass і повертає новий клас що його extends. Compose(Base, Mixin1, Mixin2) має працювати коректно.', ru:'Реализуй полную систему множественного наследования через mixin-цепочку: каждый mixin принимает superclass и возвращает новый класс который его extends. Compose(Base, Mixin1, Mixin2) должен работать корректно.' },
    ],
    '01-03': [
      { level:'medium', uk:'Напиши функцію bind-polyfill: myBind(fn, ctx, ...args) яка працює як Function.prototype.bind, включно з частковим застосуванням аргументів.', ru:'Напиши функцию bind-polyfill: myBind(fn, ctx, ...args) которая работает как Function.prototype.bind, включая частичное применение аргументов.' },
      { level:'hard',   uk:'Реалізуй власний EventEmitter клас з методами on(event, fn), off(event, fn), emit(event, ...args). Підпишись на подію \'data\' двічі і переконайся обидва обробники спрацьовують. Метод off має коректно видаляти обробник.', ru:'Реализуй собственный класс EventEmitter с методами on(event, fn), off(event, fn), emit(event, ...args). Подпишись на событие \'data\' дважды и убедись что оба обработчика срабатывают. Метод off должен корректно удалять обработчик.' },
      { level:'hard',   uk:'Напиши функцію partial(fn, ...presetArgs) що реалізує часткове застосування без зміни this. Потім реалізуй partialRight що застосовує аргументи з правого боку.', ru:'Напиши функцию partial(fn, ...presetArgs) реализующую частичное применение без изменения this. Потом реализуй partialRight применяющий аргументы справа.' },
      { level:'extra',  uk:'Реалізуй систему автоматичного bind\'ингу методів класу: autoBind(instance) має пройтись по всіх методах прототипу і прив\'язати їх до instance. Переконайся що це не торкається getters/setters.', ru:'Реализуй систему автоматической привязки методов класса: autoBind(instance) должен пройтись по всем методам прототипа и привязать их к instance. Убедись что это не затрагивает getters/setters.' },
    ],
    '01-04': [
      { level:'medium', uk:'Напиши функцію create(proto, props) яка працює як Object.create але додатково приймає об\'єкт з властивостями. Перевір що створений об\'єкт наслідує від proto.', ru:'Напиши функцию create(proto, props) работающую как Object.create но дополнительно принимающую объект со свойствами. Проверь что созданный объект наследует от proto.' },
      { level:'hard',   uk:'Реалізуй функцію inherits(Child, Parent) в стилі Node.js (до ES6 класів): встанови правильно прототипний ланцюг і збережи constructor. Перевір instanceof.', ru:'Реализуй функцию inherits(Child, Parent) в стиле Node.js (до ES6 классов): правильно установи прототипную цепочку и сохрани constructor. Проверь instanceof.' },
      { level:'hard',   uk:'Напиши функцію deepMerge(target, ...sources) що рекурсивно зливає об\'єкти, враховуючи прототипний ланцюг. Масиви мають конкатенуватись, а не перезаписуватись.', ru:'Напиши функцию deepMerge(target, ...sources) рекурсивно сливающую объекты с учётом прототипной цепочки. Массивы должны конкатенироваться, а не перезаписываться.' },
      { level:'extra',  uk:'Реалізуй власний механізм класів без ключового слова class: defineClass({ constructor, methods, statics, extends: Parent }). Підтримай super() виклики через явне делегування до батьківського конструктора.', ru:'Реализуй собственный механизм классов без ключевого слова class: defineClass({ constructor, methods, statics, extends: Parent }). Поддержи вызовы super() через явное делегирование к родительскому конструктору.' },
    ],
    '01-05': [
      { level:'medium', uk:'Напиши генератор range(start, end, step=1) що генерує числа від start до end з кроком step. Він має бути нескінченним якщо end не передано. Перевір з for...of.', ru:'Напиши генератор range(start, end, step=1) генерирующий числа от start до end с шагом step. Он должен быть бесконечным если end не передан. Проверь с for...of.' },
      { level:'hard',   uk:'Реалізуй генераторну функцію flatten(arr) яка рекурсивно обходить вкладені масиви довільної глибини. Використай yield* для делегування рекурсивних викликів.', ru:'Реализуй генераторную функцию flatten(arr) рекурсивно обходящую вложенные массивы произвольной глубины. Используй yield* для делегирования рекурсивных вызовов.' },
      { level:'hard',   uk:'Напиши генератор zip(...iterables) що поєднує елементи кількох iterable об\'єктів попарно (як Python zip). Зупиняється коли найкоротший iterable вичерпується.', ru:'Напиши генератор zip(...iterables) попарно объединяющий элементы нескольких iterable объектов (как Python zip). Останавливается когда кратчайший iterable исчерпан.' },
      { level:'extra',  uk:'Реалізуй lazy pipeline з генераторів: pipe(source, ...transforms) де кожен transform — генераторна функція. Побудуй pipeline map+filter+take що обробляє нескінченний потік чисел.', ru:'Реализуй lazy pipeline из генераторов: pipe(source, ...transforms) где каждый transform — генераторная функция. Построй pipeline map+filter+take обрабатывающий бесконечный поток чисел.' },
    ],
    '01-06': [
      { level:'medium', uk:'Створи Proxy для об\'єкта-конфігурації що перехоплює get і автоматично повертає значення за замовчуванням якщо властивість відсутня. Передай дефолти як другий аргумент.', ru:'Создай Proxy для объекта-конфигурации перехватывающий get и автоматически возвращающий значение по умолчанию если свойство отсутствует. Передай дефолты как второй аргумент.' },
      { level:'hard',   uk:'Реалізуй валідуючий Proxy: createValidated(schema) де schema — об\'єкт з функціями-валідаторами для кожного поля. Спроба задати невалідне значення має кидати TypeError з описом.', ru:'Реализуй валидирующий Proxy: createValidated(schema) где schema — объект с функциями-валидаторами для каждого поля. Попытка задать невалидное значение должна бросать TypeError с описанием.' },
      { level:'hard',   uk:'Використовуючи Reflect API, напиши функцію traceObject(obj) що логує всі get/set/has операції з зазначенням імені властивості і значення. Результат має бути ідентичним оригіналу.', ru:'Используя Reflect API, напиши функцию traceObject(obj) логирующую все get/set/has операции с указанием имени свойства и значения. Результат должен быть идентичен оригиналу.' },
      { level:'extra',  uk:'Реалізуй систему спостереження за об\'єктом через Proxy: observe(obj) повертає [proxy, changes$] де changes$ — AsyncIterator що видає { key, oldVal, newVal } при кожній зміні.', ru:'Реализуй систему наблюдения за объектом через Proxy: observe(obj) возвращает [proxy, changes$] где changes$ — AsyncIterator выдающий { key, oldVal, newVal } при каждом изменении.' },
    ],
    '01-07': [
      { level:'medium', uk:'Реалізуй debounce(fn, delay) і throttle(fn, limit) з нуля. Напиши тести які перевіряють що debounce викликає fn лише раз після серії подій, а throttle — не частіше ніж раз в limit мс.', ru:'Реализуй debounce(fn, delay) и throttle(fn, limit) с нуля. Напиши тесты проверяющие что debounce вызывает fn только один раз после серии событий, а throttle — не чаще раза в limit мс.' },
      { level:'hard',   uk:'Напиши функцію memoizeWithTTL(fn, ttlMs) — кешує результат функції але видаляє його через ttlMs мілісекунд. Підтримай функції з кількома аргументами через JSON.stringify ключ.', ru:'Напиши функцию memoizeWithTTL(fn, ttlMs) — кеширует результат функции но удаляет его через ttlMs миллисекунд. Поддержи функции с несколькими аргументами через JSON.stringify ключ.' },
      { level:'hard',   uk:'Реалізуй lazy(fn) що відкладає виконання fn до першого виклику результату, після чого кешує відповідь назавжди. Напиши lazyMap(arr, fn) що обчислює елементи on-demand.', ru:'Реализуй lazy(fn) откладывающий выполнение fn до первого вызова результата, после чего кеширует ответ навсегда. Напиши lazyMap(arr, fn) вычисляющий элементы on-demand.' },
      { level:'extra',  uk:'Побудуй систему оптимізацій для важких обчислень: combine(memoize, debounce, async retry з exponential backoff). Продемонструй на реальному прикладі fetch з кешем, дебаунсом і автоматичними повторами.', ru:'Построй систему оптимизаций для тяжёлых вычислений: combine(memoize, debounce, async retry с exponential backoff). Продемонстрируй на реальном примере fetch с кешем, дебаунсом и автоматическими повторами.' },
    ],
    '01-08': [
      { level:'medium', uk:'Створи ієрархію кастомних помилок: AppError -> NetworkError, ValidationError, AuthError. Кожна має code і message. Напиши функцію handleError(err) що реагує по-різному на кожен тип.', ru:'Создай иерархию кастомных ошибок: AppError -> NetworkError, ValidationError, AuthError. Каждая имеет code и message. Напиши функцию handleError(err) по-разному реагирующую на каждый тип.' },
      { level:'hard',   uk:'Реалізуй Result<T, E> монаду: { ok: true, value: T } або { ok: false, error: E } з методами map(fn), flatMap(fn), getOrElse(default). Перепиши fetch-функцію що повертає Result замість throws.', ru:'Реализуй монаду Result<T, E>: { ok: true, value: T } или { ok: false, error: E } с методами map(fn), flatMap(fn), getOrElse(default). Перепиши fetch-функцию возвращающую Result вместо throws.' },
      { level:'hard',   uk:'Напиши middleware-систему для обробки помилок: chain(handler1, handler2, ...) де кожен handler отримує (err, next) і може або обробити помилку або передати її далі. Додай логер і reporter як middleware.', ru:'Напиши middleware-систему для обработки ошибок: chain(handler1, handler2, ...) где каждый handler получает (err, next) и может либо обработать ошибку либо передать её дальше. Добавь логгер и reporter как middleware.' },
      { level:'extra',  uk:'Реалізуй систему відстеження помилок: wrap(fn) перехоплює всі throw та rejected Promise, збирає стек, контекст виклику і час. Виведи звіт з топ-5 найчастіших помилок після 100 викликів.', ru:'Реализуй систему отслеживания ошибок: wrap(fn) перехватывает все throw и rejected Promise, собирает стек, контекст вызова и время. Выведи отчёт с топ-5 наиболее частых ошибок после 100 вызовов.' },
    ],
    '01-09': [
      { level:'medium', uk:'Реалізуй патерн Observer: клас EventBus з методами subscribe(event, fn), unsubscribe(event, fn), publish(event, data). Додай підтримку wildcard \'*\' що підписується на всі події.', ru:'Реализуй паттерн Observer: класс EventBus с методами subscribe(event, fn), unsubscribe(event, fn), publish(event, data). Добавь поддержку wildcard \'*\' подписывающегося на все события.' },
      { level:'hard',   uk:'Реалізуй патерн Factory з реєстром: WidgetFactory.register(type, class), WidgetFactory.create(type, config). Зареєструй Button, Input, Select і створи їх динамічно. Кинь помилку для невідомого типу.', ru:'Реализуй паттерн Factory с реестром: WidgetFactory.register(type, class), WidgetFactory.create(type, config). Зарегистрируй Button, Input, Select и создай их динамически. Брось ошибку для неизвестного типа.' },
      { level:'hard',   uk:'Напиши патерн Decorator для класу Logger: додай TimeLogger(logger) що prefix\'ить timestamp, FilterLogger(logger, level) що фільтрує за рівнем, ColorLogger(logger) що додає ANSI кольори.', ru:'Напиши паттерн Decorator для класса Logger: добавь TimeLogger(logger) с prefix\'ом timestamp, FilterLogger(logger, level) фильтрующий по уровню, ColorLogger(logger) добавляющий ANSI цвета.' },
      { level:'extra',  uk:'Реалізуй Command pattern з undo/redo: CommandHistory що зберігає список команд з execute() і undo(). Демо: текстовий редактор де Insert, Delete, Replace є командами з повною підтримкою undo/redo.', ru:'Реализуй Command pattern с undo/redo: CommandHistory хранящий список команд с execute() и undo(). Демо: текстовый редактор где Insert, Delete, Replace являются командами с полной поддержкой undo/redo.' },
    ],
    '01-10': [
      { level:'medium', uk:'Реалізуй функції compose(...fns) і pipe(...fns). compose застосовує функції справа наліво, pipe — зліва направо. Перевір на ланцюжку: trim -> lowercase -> split -> join.', ru:'Реализуй функции compose(...fns) и pipe(...fns). compose применяет функции справа налево, pipe — слева направо. Проверь на цепочке: trim -> lowercase -> split -> join.' },
      { level:'hard',   uk:'Реалізуй curry(fn) що автоматично каррює функцію будь-якої арності. curry((a,b,c) => a+b+c)(1)(2)(3) і curry(fn)(1,2)(3) мають давати однаковий результат.', ru:'Реализуй curry(fn) автоматически каррирующий функцию любой арности. curry((a,b,c) => a+b+c)(1)(2)(3) и curry(fn)(1,2)(3) должны давать одинаковый результат.' },
      { level:'hard',   uk:'Напиши функціональну бібліотеку роботи з Maybe монадою: Maybe.of(val), Maybe.empty(), .map(fn), .getOrElse(default), .chain(fn). Реалізуй безпечний доступ до вкладених властивостей без null-checks.', ru:'Напиши функциональную библиотеку работы с монадой Maybe: Maybe.of(val), Maybe.empty(), .map(fn), .getOrElse(default), .chain(fn). Реализуй безопасный доступ к вложенным свойствам без null-checks.' },
      { level:'extra',  uk:'Реалізуй повноцінний transducer механізм: transduce(xform, reducer, init, coll). Побудуй xform через compose(map(fn), filter(fn), take(n)). Продемонструй що він працює однаково на масивах і генераторах.', ru:'Реализуй полноценный механизм transducer: transduce(xform, reducer, init, coll). Построй xform через compose(map(fn), filter(fn), take(n)). Продемонстрируй что он работает одинаково на массивах и генераторах.' },
    ],
    '01-11': [
      { level:'medium', uk:'Напиши Jest unit-тести для функції formatDate(date, locale): перевір коректний формат для \'uk\' і \'en\', граничні значення (1 Jan, 31 Dec), і що кидає TypeError для невалідної дати.', ru:'Напиши Jest unit-тесты для функции formatDate(date, locale): проверь корректный формат для \'uk\' и \'en\', граничные значения (1 Jan, 31 Dec), и что бросает TypeError для невалидной даты.' },
      { level:'hard',   uk:'Напиши інтеграційний тест для модуля UserService: мок fetch через jest.fn(), перевір що getUser(id) робить GET /users/:id, кешує результат, і при помилці API повертає cached або null.', ru:'Напиши интеграционный тест для модуля UserService: мок fetch через jest.fn(), проверь что getUser(id) делает GET /users/:id, кеширует результат, и при ошибке API возвращает cached или null.' },
      { level:'hard',   uk:'Реалізуй TDD-цикл для стека (Stack): спочатку напиши тести для push/pop/peek/isEmpty/size, потім реалізуй клас Stack щоб всі тести проходили. Покрий edge-cases: pop від порожнього стека.', ru:'Реализуй TDD-цикл для стека (Stack): сначала напиши тесты для push/pop/peek/isEmpty/size, потом реализуй класс Stack чтобы все тесты проходили. Покрой edge-cases: pop от пустого стека.' },
      { level:'extra',  uk:'Налаштуй Jest з coverage і напиши тести для мінімальної HTTP-бібліотеки (fetch wrapper): mock fetch, перевір retry логіку, timeout, базову авторизацію. Досягни 100% branch coverage.', ru:'Настрой Jest с coverage и напиши тесты для минимальной HTTP-библиотеки (fetch wrapper): mock fetch, проверь retry логику, timeout, базовую авторизацию. Достигни 100% branch coverage.' },
    ],
    '01-12': [
      { level:'medium', uk:'Додай до своєї utility-бібліотеки функцію groupBy(arr, keyFn) і sortBy(arr, ...keyFns) з підтримкою ascending/descending. Напиши unit-тести для обох.', ru:'Добавь в свою utility-библиотеку функцию groupBy(arr, keyFn) и sortBy(arr, ...keyFns) с поддержкой ascending/descending. Напиши unit-тесты для обеих.' },
      { level:'hard',   uk:'Реалізуй у бібліотеці модуль для роботи з деревами: buildTree(flatArr, idKey, parentKey), traverseBFS(tree, fn), traverseDFS(tree, fn), findNode(tree, predicate). Покрий тестами.', ru:'Реализуй в библиотеке модуль для работы с деревьями: buildTree(flatArr, idKey, parentKey), traverseBFS(tree, fn), traverseDFS(tree, fn), findNode(tree, predicate). Покрой тестами.' },
      { level:'hard',   uk:'Збери бібліотеку за допомогою Rollup або esbuild у три формати: ESM, CJS, UMD. Налаштуй tree-shaking і перевір що розмір бандлу мінімальний. Опублікуй як npm-пакет локально через npm link.', ru:'Собери библиотеку с помощью Rollup или esbuild в три формата: ESM, CJS, UMD. Настрой tree-shaking и проверь что размер бандла минимален. Опубликуй как npm-пакет локально через npm link.' },
      { level:'extra',  uk:'Розшир бібліотеку типізованим API на TypeScript: генеруй .d.ts файли, налаштуй tsconfig paths, додай JSDoc для автодоповнення. Напиши README з прикладами і CI pipeline (GitHub Actions) що запускає тести і перевіряє типи.', ru:'Расширь библиотеку типизированным API на TypeScript: генерируй .d.ts файлы, настрой tsconfig paths, добавь JSDoc для автодополнения. Напиши README с примерами и CI pipeline (GitHub Actions) запускающий тесты и проверяющий типы.' },
    ],
  };
  Object.keys(EXT).forEach(function (id) {
    var l = WEB_LESSONS.find(function (x) { return x.id === id; });
    if (l) l.tasks = (l.tasks || []).concat(EXT[id]);
  });
})();
