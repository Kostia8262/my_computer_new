/* ═══════════════════════════════════════════════════════════
   Контент · Модуль 8 — JavaScript: Старт · 8–11 Веб-Старт
   ═══════════════════════════════════════════════════════════ */
(function () {
  'use strict';

  function patch(id, theory, html, css, js, tasks) {
    const l = WEB_LESSONS.find(x => x.id === id);
    if (!l) return;
    l.theory = theory;
    l.starterCode.html = html;
    l.starterCode.css  = css;
    l.starterCode.js   = js;
    l.tasks = tasks;
  }

  /* ─── Загальний вивід-бокс для уроків JS ───────────────── */
  const BH = `<div class="con">
  <div class="hd">
    <span class="d r"></span><span class="d y"></span><span class="d g"></span>
    <span>📋 Результат</span>
  </div>
  <div class="body" id="out"></div>
</div>`;

  const BC = `*{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Segoe UI',Arial,sans-serif;background:#0f172a;color:#f8fafc;padding:20px}
.con{background:#1e293b;border-radius:12px;overflow:hidden;max-width:640px}
.hd{display:flex;align-items:center;gap:6px;padding:10px 14px;background:#334155;font-size:12px;color:#94a3b8}
.d{width:12px;height:12px;border-radius:50%;flex-shrink:0}
.r{background:#ef4444}.y{background:#f59e0b}.g{background:#22c55e}
.body{padding:16px;font-family:'Courier New',monospace;font-size:13px;min-height:100px;display:flex;flex-direction:column;gap:3px}
.ln{padding:3px 8px;border-radius:4px}
.gr{color:#4ade80}.bl{color:#60a5fa}.yw{color:#fbbf24}.rd{color:#f87171}.gy{color:#94a3b8}`;

  const SF = `function show(msg, cls) {
  const d = document.createElement('div');
  d.className = 'ln' + (cls ? ' ' + cls : '');
  d.textContent = String(msg);
  document.getElementById('out').appendChild(d);
}
`;

  /* ─── 08-01 ─────────────────────────────────────────────── */
  patch('08-01',
    { uk:`<h2>Що таке JavaScript і де він живе</h2>
<p>HTML будує структуру, CSS робить красиво, а <strong>JavaScript</strong> (JS) — оживляє сторінку! Він змушує реагувати на кліки, рахувати, змінювати вміст без перезавантаження.</p>
<h3>Де пишемо JS</h3>
<ul>
  <li>Вкладка <strong>JS</strong> у нашому редакторі — саме там!</li>
  <li>В тезі <code>&lt;script&gt;</code> або файлі <code>script.js</code>.</li>
</ul>
<h3>Що вміє JavaScript</h3>
<ul>
  <li>🔢 Рахувати і обчислювати.</li>
  <li>🎨 Змінювати кольори, тексти, елементи.</li>
  <li>🖱️ Реагувати на кліки та клавіші.</li>
  <li>💾 Зберігати дані у браузері.</li>
</ul>
<p>Натисни вкладку <strong>JS</strong> і подивись на перші команди. Натисни «Запустити»!</p>`,
      ru:`<h2>Что такое JavaScript и где он живёт</h2>
<p>HTML строит структуру, CSS делает красиво, а <strong>JavaScript</strong> (JS) — оживляет страницу! Он заставляет реагировать на клики, считать, менять содержимое без перезагрузки.</p>
<h3>Где пишем JS</h3>
<ul>
  <li>Вкладка <strong>JS</strong> в нашем редакторе — именно там!</li>
  <li>В теге <code>&lt;script&gt;</code> или файле <code>script.js</code>.</li>
</ul>
<h3>Что умеет JavaScript</h3>
<ul>
  <li>🔢 Считать и вычислять.</li>
  <li>🎨 Изменять цвета, тексты, элементы.</li>
  <li>🖱️ Реагировать на клики и клавиши.</li>
  <li>💾 Сохранять данные в браузере.</li>
</ul>` },
    BH, BC,
    SF +
`show('👋 Привіт, JavaScript!', 'gr');
show('📅 Рік: ' + new Date().getFullYear(), 'bl');
show('🔢 7 × 8 = ' + (7 * 8), 'yw');
show('📐 Корінь з 144 = ' + Math.sqrt(144), 'yw');

// Поточний час:
const тепер = new Date();
const год = тепер.getHours();
const хв  = String(тепер.getMinutes()).padStart(2, '0');
show('⏰ Зараз: ' + год + ':' + хв, 'bl');

// JS може змінювати сторінку:
document.title = '🚀 Мій перший JS!';
show('✅ Заголовок вкладки змінено!', 'gr');`,
    [
      { level:'easy',   uk:'Замін текст у першому show() на "Привіт, [своє ім\'я]!"',        ru:'Замени текст в первом show() на "Привет, [своё имя]!"' },
      { level:'medium', uk:'Додай ще 2 show() з будь-якими математичними виразами.',          ru:'Добавь ещё 2 show() с любыми математическими выражениями.' },
      { level:'hard',   uk:'Додай const вік = 10; і show("Через 5 років мені буде " + (вік+5))', ru:'Добавь const вік = 10; и show("Через 5 лет мне будет " + (вік+5))' },
    ]
  );

  /* ─── 08-02 ─────────────────────────────────────────────── */
  patch('08-02',
    { uk:`<h2>Змінні: let та const — зберігаємо дані</h2>
<p><strong>Змінна</strong> — як скринька з підписом. Кладемо дані і потім використовуємо за іменем.</p>
<h3>const — не змінюється</h3>
<pre>const імя = 'Аліна';
// імя = 'Інша'; ← ПОМИЛКА!</pre>
<h3>let — може змінюватись</h3>
<pre>let рахунок = 0;
рахунок += 10; // тепер 10
рахунок *= 2;  // тепер 20</pre>
<h3>Правила іменування</h3>
<ul>
  <li>Без пробілів: camelCase → <code>моєІмя</code></li>
  <li>Без апострофів та дефісів</li>
  <li>Регістр важливий: <code>Name ≠ name</code></li>
</ul>
<p>Правило: використовуй <code>const</code> за замовчуванням. <code>let</code> — тільки якщо значення змінюватиметься.</p>`,
      ru:`<h2>Переменные: let и const — сохраняем данные</h2>
<p><strong>Переменная</strong> — как ящик с подписью. Кладём данные и потом используем по имени.</p>
<h3>const — не меняется</h3>
<pre>const имя = 'Алина';
// имя = 'Другое'; ← ОШИБКА!</pre>
<h3>let — может меняться</h3>
<pre>let счёт = 0;
счёт += 10; // теперь 10
счёт *= 2;  // теперь 20</pre>
<h3>Правило</h3>
<p>Используй <code>const</code> по умолчанию. <code>let</code> — только если значение будет меняться.</p>` },
    BH, BC,
    SF +
`const імя    = 'Аліна';
const місто  = 'Київ';
const рік    = 2025;
show('👤 ' + імя + ', ' + місто + ', ' + рік, 'bl');

// let — змінюємо:
let рахунок = 0;
show('🎮 Старт: ' + рахунок, 'gy');
рахунок += 10;
show('🎮 +10 = ' + рахунок, 'yw');
рахунок *= 2;
show('🎮 ×2 = ' + рахунок, 'yw');
рахунок -= 5;
show('🎮 -5 = ' + рахунок, 'gr');

// Перезапис:
let настрій = '😢 сумний';
show('Настрій: ' + настрій, 'rd');
настрій = '😊 щасливий';
show('Настрій: ' + настрій, 'gr');`,
    [
      { level:'easy',   uk:'Заміни const імя та const місто на своє ім\'я і місто.',            ru:'Замени const імя и const місто на своё имя и город.' },
      { level:'medium', uk:'Створи const улюблений = "..." зі своєю улюбленою грою і виведи через show().',  ru:'Создай const улюблений = "..." со своей любимой игрой и выведи через show().' },
      { level:'hard',   uk:'let лічильник = 100; зроби 4 операції (-=20, +=5, *=2, /=2) і виводь результат після кожної.', ru:'let лічильник = 100; сделай 4 операции (-=20, +=5, *=2, /=2) и выводи результат после каждой.' },
    ]
  );

  /* ─── 08-03 ─────────────────────────────────────────────── */
  patch('08-03',
    { uk:`<h2>Типи даних: рядки, числа, true/false, null</h2>
<p>Кожне значення в JS має <strong>тип</strong>. Оператор <code>typeof</code> дозволяє дізнатись тип.</p>
<h3>Основні типи</h3>
<ul>
  <li><strong>string</strong> — текст: <code>'Привіт'</code>, <code>"World"</code></li>
  <li><strong>number</strong> — число: <code>42</code>, <code>3.14</code>, <code>-7</code></li>
  <li><strong>boolean</strong> — <code>true</code> або <code>false</code></li>
  <li><strong>null</strong> — навмисно порожньо</li>
  <li><strong>undefined</strong> — значення не задано</li>
</ul>
<h3>Перетворення типів</h3>
<pre>Number('42')  // 42  (рядок → число)
String(42)    // "42" (число → рядок)
Boolean(0)    // false (нуль = хибне)
Boolean('hi') // true (непустий рядок = правда)</pre>`,
      ru:`<h2>Типы данных: строки, числа, true/false, null</h2>
<p>Каждое значение в JS имеет <strong>тип</strong>. Оператор <code>typeof</code> позволяет узнать тип.</p>
<h3>Основные типы</h3>
<ul>
  <li><strong>string</strong> — текст: <code>'Привет'</code></li>
  <li><strong>number</strong> — число: <code>42</code>, <code>3.14</code></li>
  <li><strong>boolean</strong> — <code>true</code> или <code>false</code></li>
  <li><strong>null</strong> — намеренно пусто</li>
  <li><strong>undefined</strong> — значение не задано</li>
</ul>
<h3>Преобразование типов</h3>
<pre>Number('42')  // 42
String(42)    // "42"
Boolean(0)    // false</pre>` },
    BH, BC,
    SF +
`show('--- typeof ---', 'gy');
show('typeof "Привіт"  → ' + typeof 'Привіт',   'bl');
show('typeof 42        → ' + typeof 42,           'bl');
show('typeof true      → ' + typeof true,          'bl');
show('typeof null      → ' + typeof null,          'rd'); // відомий баг!
show('typeof undefined → ' + typeof undefined,     'rd');

show('--- Перетворення ---', 'gy');
show('Number("42") = '   + Number('42'),          'yw');
show('String(100) = "'   + String(100) + '"',     'yw');
show('Boolean(0)  = '    + Boolean(0),            'rd');
show('Boolean(1)  = '    + Boolean(1),            'gr');
show('Boolean("")  = '   + Boolean(''),           'rd');
show('Boolean("Hi") = '  + Boolean('Hi'),         'gr');

// "42" + 8 vs Number("42") + 8:
show('--- Небезпечне змішування ---', 'gy');
show('"42" + 8 = '        + ('42' + 8),           'rd');
show('Number("42")+8 = '  + (Number('42') + 8),  'gr');`,
    [
      { level:'easy',   uk:'Додай show(typeof 99.9) і show(typeof false) — що виведе?',         ru:'Добавь show(typeof 99.9) и show(typeof false) — что выведет?' },
      { level:'medium', uk:'Виведи Boolean() для 5 різних значень: 0, -1, "", "0", null — скільки з них false?', ru:'Выведи Boolean() для 5 разных значений: 0, -1, "", "0", null — сколько из них false?' },
      { level:'hard',   uk:'Спробуй "100" - 50, "100" * 2, "сто" + 50 — що повертає кожен? Виведи і напиши коментар // до кожного рядка.', ru:'Попробуй "100" - 50, "100" * 2, "сто" + 50 — что возвращает каждый? Выведи и напиши комментарий // к каждой строке.' },
    ]
  );

  /* ─── 08-04 ─────────────────────────────────────────────── */
  patch('08-04',
    { uk:`<h2>console.log — перевіряємо код</h2>
<p><code>console.log()</code> — найважливіший інструмент програміста. Він виводить дані у <strong>консоль браузера</strong>.</p>
<h3>Як відкрити консоль</h3>
<ol>
  <li>Натисни <strong>F12</strong> у браузері.</li>
  <li>Перейди на вкладку <strong>Console</strong>.</li>
</ol>
<h3>Варіанти</h3>
<pre>console.log('текст');        // звичайний
console.log(a, b, c);       // кілька значень
console.warn('Увага!');      // жовте
console.error('Помилка!');   // червоне
console.table([{a:1},{a:2}]);// таблиця</pre>
<h3>show() vs console.log()</h3>
<ul>
  <li><code>show()</code> — видно на сторінці (для учнів).</li>
  <li><code>console.log()</code> — у F12, невидимо відвідувачам (для розробника).</li>
</ul>`,
      ru:`<h2>console.log — проверяем код</h2>
<p><code>console.log()</code> — важнейший инструмент программиста. Выводит данные в <strong>консоль браузера</strong>.</p>
<h3>Как открыть консоль</h3>
<ol>
  <li>Нажми <strong>F12</strong> в браузере.</li>
  <li>Перейди на вкладку <strong>Console</strong>.</li>
</ol>
<h3>Варианты</h3>
<pre>console.log('текст');
console.warn('Внимание!');
console.error('Ошибка!');
console.table([{a:1},{a:2}]);</pre>` },
    BH, BC,
    SF +
`// show() — видно тут:
show('👀 show() — видно на сторінці', 'gr');
show('🔧 console.log() — у F12 → Console', 'yw');

// console.log — відкрий F12!
console.log('Привіт з консолі!');
console.log('Кілька значень:', 42, true, [1,2,3]);
console.warn('Це попередження (жовте)');
console.error('Це помилка (червоне)');

const x = 15, y = 7;
console.log('x =', x, 'y =', y, 'sum =', x + y);
show('Перевір F12: x=' + x + ' y=' + y + ' sum=' + (x+y), 'bl');

console.table([
  { імя:'Аліна', вік:10 },
  { імя:'Максим', вік:11 },
]);
show('console.table() теж у F12 — виглядає як таблиця!', 'bl');`,
    [
      { level:'easy',   uk:'Відкрий F12 → Console і побач що виводить console.log.',            ru:'Открой F12 → Console и посмотри что выводит console.log.' },
      { level:'medium', uk:'Додай console.log() зі своїм іменем, віком і містом.',              ru:'Добавь console.log() со своим именем, возрастом и городом.' },
      { level:'hard',   uk:'Заміни console.table() на масив з 4 учнями — додай поле оцінка у кожного.', ru:'Замени console.table() на массив из 4 учеников — добавь поле оцінка у каждого.' },
    ]
  );

  /* ─── 08-05 ─────────────────────────────────────────────── */
  patch('08-05',
    { uk:`<h2>Арифметика та рядкові операції</h2>
<p>JavaScript рахує як калькулятор і вміє працювати з текстом.</p>
<h3>Арифметичні оператори</h3>
<pre>+  додавання     10 + 3 = 13
-  віднімання    10 - 3 = 7
*  множення      10 * 3 = 30
/  ділення       10 / 3 = 3.333
%  залишок       10 % 3 = 1
** степінь       2 ** 8 = 256</pre>
<h3>Об'єкт Math</h3>
<pre>Math.round(3.7)  // 4  — округлення
Math.abs(-5)     // 5  — модуль
Math.max(1,9,3)  // 9  — максимум
Math.min(1,9,3)  // 1  — мінімум
Math.random()    // 0..0.99 — випадкове
Math.floor(x)    // округлити вниз</pre>
<h3>Рядки</h3>
<pre>'ab' + 'cd'         // 'abcd' (склейка)
'hello'.length       // 5
'hello'.toUpperCase()// 'HELLO'
'  текст  '.trim()   // 'текст'</pre>`,
      ru:`<h2>Арифметика и строковые операции</h2>
<p>JavaScript считает как калькулятор и умеет работать с текстом.</p>
<h3>Арифметика</h3>
<pre>+  сложение     * умножение
-  вычитание    / деление
%  остаток      ** степень</pre>
<h3>Math</h3>
<pre>Math.round(3.7) // 4
Math.abs(-5)    // 5
Math.max(1,9,3) // 9
Math.random()   // 0..0.99</pre>
<h3>Строки</h3>
<pre>'ab'+'cd'           // 'abcd'
'hello'.length       // 5
'hello'.toUpperCase()// 'HELLO'</pre>` },
    BH, BC,
    SF +
`show('--- Арифметика ---', 'gy');
show('10 + 3 = '   + (10 + 3),           'yw');
show('10 - 3 = '   + (10 - 3),           'yw');
show('10 * 3 = '   + (10 * 3),           'yw');
show('10 / 3 ≈ '   + (10 / 3).toFixed(2),'yw');
show('10 % 3 = '   + (10 % 3),           'yw');
show('2 ** 8 = '   + (2 ** 8),           'yw');

show('--- Math ---', 'gy');
show('Math.round(3.7) = ' + Math.round(3.7), 'bl');
show('Math.abs(-99)   = ' + Math.abs(-99),   'bl');
show('Math.max(1,9,3) = ' + Math.max(1,9,3), 'bl');
const rnd = Math.floor(Math.random() * 100) + 1;
show('🎲 Випадкове 1–100: ' + rnd,            'gr');

show('--- Рядки ---', 'gy');
const привіт = 'Привіт';
show(привіт + ', ' + 'Світ!',                       'gr');
show('"hello".length = '     + 'hello'.length,       'bl');
show('"hello".toUpperCase = '+ 'hello'.toUpperCase(),'bl');
show('"  пробіли  ".trim = "'+'  пробіли  '.trim()+'"','bl');`,
    [
      { level:'easy',   uk:'Обчисли скільки хвилин в 3 годинах і виведи через show().',        ru:'Вычисли сколько минут в 3 часах и выведи через show().' },
      { level:'medium', uk:'Виведи 5 різних випадкових чисел від 1 до 50 (5 разів виклич Math.floor(Math.random()*50)+1).', ru:'Выведи 5 разных случайных чисел от 1 до 50.' },
      { level:'hard',   uk:'Обчисли площу кола (Math.PI * r**2) з r=7 і виведи до 2 знаків після коми (.toFixed(2)).', ru:'Вычисли площадь круга (Math.PI * r**2) с r=7 и выведи до 2 знаков (.toFixed(2)).' },
    ]
  );

  /* ─── 08-06 ─────────────────────────────────────────────── */
  patch('08-06',
    { uk:`<h2>Шаблонні літерали: \`\${змінна}\` у рядку</h2>
<p>Замість склеювання рядків через <code>+</code> є кращий спосіб — <strong>шаблонний літерал</strong>. Пишемо в зворотніх апострофах: <code>\`текст \${змінна} текст\`</code>.</p>
<h3>Порівняння</h3>
<pre>const імя = 'Аліна', вік = 10;

// Старий спосіб (складно):
'Привіт, ' + імя + '! Тобі ' + вік + ' р.'

// Шаблонний літерал (зручно):
\`Привіт, \${імя}! Тобі \${вік} р.\`</pre>
<h3>Всередині \${} може бути будь-який вираз</h3>
<pre>\`Через 5 років мені буде \${вік + 5}\`
\`Ім'я: \${імя.toUpperCase()}\`
\`2+2=\${2+2}\`</pre>
<h3>Багаторядковий рядок</h3>
<pre>const txt = \`Рядок 1
Рядок 2
Рядок 3\`;</pre>`,
      ru:`<h2>Шаблонные литералы: \`\${переменная}\` в строке</h2>
<p>Вместо склеивания строк через <code>+</code> есть лучший способ — <strong>шаблонный литерал</strong>. Пишем в обратных апострофах: <code>\`текст \${переменная} текст\`</code>.</p>
<h3>Сравнение</h3>
<pre>// Старый:
'Привет, ' + имя + '! Тебе ' + возраст + ' лет.'
// Шаблонный:
\`Привет, \${имя}! Тебе \${возраст} лет.\`</pre>
<h3>Внутри \${} — любое выражение</h3>
<pre>\`Через 5 лет мне будет \${возраст + 5}\`</pre>` },
    BH, BC,
    SF +
`const імя  = 'Аліна';
const вік  = 10;
const хобі = 'CSS-анімації';

// Старий vs новий:
show('Старий: ' + імя + ', ' + вік + ' р.', 'rd');
show(\`Новий:  \${імя}, \${вік} р.\`,           'gr');

// Вирази в \${}:
show(\`Через 5 років: \${вік + 5} р.\`,         'bl');
show(\`Велике: \${імя.toUpperCase()}\`,          'bl');
show(\`Мале:   \${імя.toLowerCase()}\`,          'bl');
show(\`2²+3² = \${2**2 + 3**2}\`,               'yw');

// Картка-представлення:
const картка = \`
╔════════════════════╗
║  👤  \${імя.padEnd(14)}║
║  🎂  \${String(вік).padEnd(14)}║
║  ❤️  \${хобі.padEnd(13)}║
╚════════════════════╝\`;
show(картка, 'gr');`,
    [
      { level:'easy',   uk:'Заміни const імя, вік, хобі на свої дані і перезапусти.',           ru:'Замени const імя, вік, хобі на свои данные и перезапусти.' },
      { level:'medium', uk:'Додай const предмет = "Математика" і рядок show(`Улюблений: ${предмет}`).',  ru:'Добавь const предмет = "Математика" и строку show(`Любимый: ${предмет}`).' },
      { level:'hard',   uk:'Зроби: const html = `<h2>Привіт, ${імя}!</h2><p>Вік: ${вік}</p>`; і document.body.innerHTML = html;', ru:'Сделай: const html = `<h2>Привет, ${імя}!</h2><p>Возраст: ${вік}</p>`; и document.body.innerHTML = html;' },
    ]
  );

  /* ─── 08-07 ─────────────────────────────────────────────── */
  patch('08-07',
    { uk:`<h2>Умова if / else: перевіряємо і вирішуємо</h2>
<p><code>if</code> дозволяє коду «приймати рішення».</p>
<h3>Синтаксис</h3>
<pre>if (умова) {
  // якщо true
} else if (інша умова) {
  // якщо перша false, ця true
} else {
  // якщо всі false
}</pre>
<h3>Умови</h3>
<pre>вік >= 18     // більше або рівно
імя === 'Аліна' // точно рівне
!завершено    // НЕ завершено</pre>
<h3>Тернарний оператор (коротко)</h3>
<pre>const msg = вік >= 18 ? 'Дорослий' : 'Дитина';
//           умова    ? якщо true   : якщо false</pre>`,
      ru:`<h2>Условие if / else: проверяем и решаем</h2>
<p><code>if</code> позволяет коду «принимать решения».</p>
<h3>Синтаксис</h3>
<pre>if (условие) {
  // если true
} else if (другое) {
  // если второе true
} else {
  // если все false
}</pre>
<h3>Тернарный оператор</h3>
<pre>const msg = возраст >= 18 ? 'Взрослый' : 'Ребёнок';</pre>` },
    BH, BC,
    SF +
`function оцінитиТемп(t) {
  let cls, txt;
  if (t >= 38)      { cls='rd'; txt=\`🌡 \${t}° — Висока! Лікарю.\`; }
  else if (t >= 37) { cls='yw'; txt=\`🌡 \${t}° — Трохи підвищена.\`; }
  else if (t >= 36) { cls='gr'; txt=\`🌡 \${t}° — Норма!\`; }
  else              { cls='bl'; txt=\`🌡 \${t}° — Низька, вдягнись!\`; }
  show(txt, cls);
}

оцінитиТемп(36.6);
оцінитиТемп(37.3);
оцінитиТемп(38.9);
оцінитиТемп(35.2);

show('--- Тернарний ---', 'gy');
const очки = 78;
const res  = очки >= 60 ? '✅ Здав' : '❌ Не здав';
show(\`\${очки} балів → \${res}\`, 'bl');

const число = 7;
show(\`\${число} — \${число % 2 === 0 ? 'парне' : 'непарне'}\`, 'yw');`,
    [
      { level:'easy',   uk:'Виклич оцінитиТемп() ще 2 рази зі своїми числами.',                 ru:'Вызови оцінитиТемп() ещё 2 раза со своими числами.' },
      { level:'medium', uk:'Напиши функцію оцінка(б): ≥90→"Відмінно", ≥75→"Добре", ≥60→"Задовільно", інакше→"Незадовільно".', ru:'Напиши функцию оцінка(б): ≥90→"Отлично", ≥75→"Хорошо", ≥60→"Удовл.", иначе→"Неудовл.".' },
      { level:'hard',   uk:'Функція пароль(p): p.length<6→"Короткий", <8→"Нормальний", >=8→"Надійний". Протестуй з "ab","abcde","securepass".', ru:'Функция пароль(p): p.length<6→"Короткий", <8→"Нормальный", >=8→"Надёжный". Протестируй.' },
    ]
  );

  /* ─── 08-08 ─────────────────────────────────────────────── */
  patch('08-08',
    { uk:`<h2>Оператори порівняння та логіки: ===, &&, ||</h2>
<p>Умови будуються з операторів порівняння і логіки.</p>
<h3>Порівняння</h3>
<pre>=== точно рівне (і значення і тип)
!== не рівне
>  більше    <  менше
>= більше/рівне   <= менше/рівне</pre>
<p>⚠️ Завжди <code>===</code>, а не <code>==</code>! Подвійне == робить приведення типу: <code>0 == false → true</code>.</p>
<h3>Логічні оператори</h3>
<pre>&&  І   (обидві умови true)
||  АБО (хоча б одна true)
!   НЕ  (заперечення)</pre>
<pre>вік>=6 && вік<=18     // школяр
день==='сб' || день==='нд' // вихідний</pre>`,
      ru:`<h2>Операторы сравнения и логики: ===, &&, ||</h2>
<p>Условия строятся из операторов сравнения и логики.</p>
<h3>Сравнение</h3>
<pre>=== точно равно (значение И тип)
!== не равно
>, <, >=, <=</pre>
<p>⚠️ Всегда <code>===</code>, не <code>==</code>!</p>
<h3>Логика</h3>
<pre>&&  И   ||  ИЛИ   !  НЕ</pre>` },
    BH, BC,
    SF +
`show('--- == небезпечно ---','rd');
show('0 == false: '  + (0 == false),   'rd');
show('1 == "1": '    + (1 == '1'),     'rd');
show('--- === безпечно ---','gr');
show('0 === false: ' + (0 === false),  'gr');
show('1 === "1": '   + (1 === '1'),    'gr');

show('--- Логіка ---','gy');
const вік  = 10;
const день = 'сб';
const школяр   = вік >= 6 && вік <= 18;
const вихідний = день === 'сб' || день === 'нд';
show(\`Школяр (6–18): \${школяр}\`,    школяр   ? 'gr' : 'rd');
show(\`Вихідний: \${вихідний}\`,        вихідний ? 'gr' : 'rd');
show(\`Непарний вік: \${!(вік%2===0)}\`,!(вік%2===0)?'yw':'gy');

show('--- Доступ ---','gy');
function доступ(в, квиток) {
  if (в >= 12 && квиток) show(\`✅ \${в}р, квиток → Дозволено\`, 'gr');
  else                   show(\`❌ \${в}р, квиток:\${квиток} → Ні\`, 'rd');
}
доступ(15, true);
доступ(8,  true);
доступ(16, false);`,
    [
      { level:'easy',   uk:'Виклич доступ() ще 2 рази з різними аргументами.',                  ru:'Вызови доступ() ещё 2 раза с разными аргументами.' },
      { level:'medium', uk:'Напиши умову: число кратне 3 АБО кратне 5. Перевір для чисел 9,10,11,15.',  ru:'Напиши условие: число кратно 3 ИЛИ 5. Проверь для 9,10,11,15.' },
      { level:'hard',   uk:'Функція logIn(лог, пар): якщо лог==="admin" && пар==="1234" → "Вхід!" інакше → "Невірно".', ru:'Функция logIn(лог, пар): если лог==="admin" && пар==="1234" → "Вход!" иначе → "Неверно".' },
    ]
  );

  /* ─── 08-09 ─────────────────────────────────────────────── */
  patch('08-09',
    { uk:`<h2>Цикл for: повторювати N разів</h2>
<p><code>for</code> виконує блок коду кілька разів — незамінний для повторень.</p>
<h3>Синтаксис</h3>
<pre>for (початок; умова; крок) {
  // код
}
// Від 1 до 5:
for (let i = 1; i <= 5; i++) {
  console.log(i); // 1 2 3 4 5
}</pre>
<ul>
  <li><code>let i = 1</code> — початок</li>
  <li><code>i <= 5</code> — умова продовження</li>
  <li><code>i++</code> — крок (i += 1)</li>
</ul>
<h3>for...of — перебір масиву</h3>
<pre>const кольори = ['червоний','синій','зелений'];
for (const к of кольори) {
  console.log(к);
}</pre>`,
      ru:`<h2>Цикл for: повторить N раз</h2>
<p><code>for</code> выполняет блок кода несколько раз.</p>
<h3>Синтаксис</h3>
<pre>for (начало; условие; шаг) {
  // код
}
for (let i = 1; i <= 5; i++) {
  console.log(i);
}</pre>
<h3>for...of — перебор массива</h3>
<pre>for (const к of кольори) { console.log(к); }</pre>` },
    BH, BC,
    SF +
`show('--- Рахуємо 1–7 ---', 'gy');
for (let i = 1; i <= 7; i++) {
  show(\`\${i}: \${'⭐'.repeat(i)}\`, 'yw');
}

show('--- Зворотній рахунок ---', 'gy');
for (let i = 5; i >= 1; i--) {
  show(i + '...', 'bl');
}
show('🚀 Старт!', 'gr');

show('--- for...of ---', 'gy');
const речі = ['🍕 Піца','🎮 Ігри','🌍 Подорожі','💻 Код'];
for (const р of речі) {
  show(р, 'gr');
}

show('--- Таблиця × 4 ---', 'gy');
for (let i = 1; i <= 10; i++) {
  show(\`4 × \${i} = \${4 * i}\`, 'bl');
}`,
    [
      { level:'easy',   uk:'Зміни цикл "1–7" на "1–12" і поспостерігай як ростуть зірочки.',   ru:'Измени цикл "1–7" на "1–12" и посмотри как растут звёздочки.' },
      { level:'medium', uk:'Напиши for-цикл що виводить лише парні числа від 2 до 20 (i += 2).', ru:'Напиши for-цикл который выводит только чётные числа от 2 до 20 (i += 2).' },
      { level:'hard',   uk:'Зроби таблицю множення 3×3: вкладений for (i від 1 до 3, j від 1 до 3), виводь i × j = ?.', ru:'Сделай таблицу умножения 3×3: вложенный for (i от 1 до 3, j от 1 до 3).' },
    ]
  );

  /* ─── 08-10 ─────────────────────────────────────────────── */
  patch('08-10',
    { uk:`<h2>Цикл while: поки умова виконується</h2>
<p><code>while</code> повторює код поки умова правдива. Використовуй коли не знаєш наперед кількість повторень.</p>
<h3>Синтаксис</h3>
<pre>while (умова) {
  // код
  // ОБОВ'ЯЗКОВО змінюй щось, щоб умова колись стала false!
}</pre>
<h3>Де while кращий ніж for</h3>
<ul>
  <li>Ігри: «грати поки є життя»</li>
  <li>Пошук: «поки не знайшли»</li>
  <li>Введення: «поки не правильно»</li>
</ul>
<p>⚠️ <strong>Нескінченний цикл</strong> зависне браузер — завжди переконайся що умова стане false!</p>`,
      ru:`<h2>Цикл while: пока условие выполняется</h2>
<p><code>while</code> повторяет код пока условие истинно. Используй когда не знаешь заранее количество повторений.</p>
<h3>Синтаксис</h3>
<pre>while (условие) {
  // код
  // ОБЯЗАТЕЛЬНО меняй что-то, чтобы условие стало false!
}</pre>
<p>⚠️ Бесконечный цикл зависнет браузер!</p>` },
    BH, BC,
    SF +
`// Симуляція пригоди:
show('🗡️ Пригода починається!', 'gr');
let хп   = 10;
let крок = 0;

while (хп > 0 && крок < 8) {
  крок++;
  const подія = Math.floor(Math.random() * 4);
  if (подія === 0)      { хп -= 3; show(\`Крок \${крок}: 💀 Монстр (-3 ХП) → \${хп} ХП\`, 'rd'); }
  else if (подія === 1) { хп += 2; show(\`Крок \${крок}: 💊 Зілля  (+2 ХП) → \${хп} ХП\`, 'gr'); }
  else if (подія === 2) { хп -= 1; show(\`Крок \${крок}: 🕳 Пастка (-1 ХП) → \${хп} ХП\`, 'yw'); }
  else                  {          show(\`Крок \${крок}: 🌿 Відпочинок (ХП без змін)\`,    'gy'); }
  if (хп > 10) хп = 10; // максимум 10
}

show('', 'gy');
if (хп > 0) show(\`🏆 Перемога! Залишилось \${хп} ХП.\`, 'gr');
else         show('💀 Герой загинув. Спробуй ще!',     'rd');

// Пошук while:
show('', 'gy');
let n = 100;
while (n % 7 !== 0) { n++; }
show(\`Перше кратне 7 після 100: \${n}\`, 'bl');`,
    [
      { level:'easy',   uk:'Збільш крок з 8 до 15 і подивись чи герой виживе.',                 ru:'Увеличь шаг с 8 до 15 и посмотри выживет ли герой.' },
      { level:'medium', uk:'Знайди за допомогою while перше число > 200, яке кратне і 3, і 11.', ru:'Найди с помощью while первое число > 200, кратное и 3, и 11.' },
      { level:'hard',   uk:'Симулюй підкидання кубика (Math.floor(Math.random()*6)+1) поки не впаде 6. Порахуй кидки і виведи кожен.', ru:'Симулируй бросок кубика пока не выпадет 6. Считай броски и выводи каждый.' },
    ]
  );

  /* ─── 08-11 ─────────────────────────────────────────────── */
  patch('08-11',
    { uk:`<h2>Масиви: [], push, pop, length</h2>
<p><strong>Масив</strong> — список значень в одній змінній. Індекси починаються з <strong>0</strong>!</p>
<h3>Створення та доступ</h3>
<pre>const фрукти = ['яблуко','банан','вишня'];
фрукти[0]  // 'яблуко'
фрукти[2]  // 'вишня'
фрукти.length // 3</pre>
<h3>Методи</h3>
<pre>фрукти.push('груша')     // додати в кінець
фрукти.pop()             // видалити з кінця
фрукти.includes('банан') // true
фрукти.indexOf('банан')  // 1 (позиція)</pre>
<h3>Перебір</h3>
<pre>// forEach з індексом:
фрукти.forEach((ф, i) => {
  console.log(i, ф);
});

// for...of:
for (const ф of фрукти) {
  console.log(ф);
}</pre>`,
      ru:`<h2>Массивы: [], push, pop, length</h2>
<p><strong>Массив</strong> — список значений в одной переменной. Индексы с <strong>0</strong>!</p>
<h3>Создание и доступ</h3>
<pre>const фрукты = ['яблоко','банан','вишня'];
фрукты[0] // 'яблоко'
фрукты.length // 3</pre>
<h3>Методы</h3>
<pre>фрукты.push('груша')
фрукты.pop()
фрукты.includes('банан') // true</pre>
<h3>Перебор</h3>
<pre>фрукты.forEach((ф, i) => console.log(i, ф));</pre>` },
    BH, BC,
    SF +
`const герої = ['🦸 Супермен','🦇 Бетмен','🕷 Спайдермен','🦸‍♀️ Чудо-жінка'];

show(\`Всього: \${герої.length}\`,                 'bl');
show(\`Перший: \${герої[0]}\`,                     'gr');
show(\`Останній: \${герої[герої.length-1]}\`,       'gr');

герої.push('🐜 Людина-мураха');
show(\`Після push: \${герої.length} героїв\`,       'yw');
const вид = герої.pop();
show(\`pop() прибрав: \${вид}\`,                    'rd');

show(\`Є Бетмен? \${герої.includes('🦇 Бетмен')}\`,'bl');

show('--- Всі ---', 'gy');
герої.forEach((г, i) => show(\`\${i+1}. \${г}\`, 'gr'));

// Числа:
const оцінки = [85, 92, 78, 96, 88, 74];
const сума   = оцінки.reduce((a,b) => a+b, 0);
show(\`Середня: \${(сума/оцінки.length).toFixed(1)}\`,'yw');
show(\`Найвища: \${Math.max(...оцінки)}\`,            'gr');
show(\`Найнижча: \${Math.min(...оцінки)}\`,           'rd');`,
    [
      { level:'easy',   uk:'Заміни масив герої на 4 власних улюблених речей.',                  ru:'Замени массив герои на 4 своих любимых вещи.' },
      { level:'medium', uk:'Додай метод герої.unshift("🦊 Новий") (додає в початок) і виведи герої[0] після.',  ru:'Добавь метод герої.unshift("🦊 Новый") и выведи герої[0] после.' },
      { level:'hard',   uk:'Знайди серед оцінок усі вищі за 85: оцінки.filter(о => о > 85) і виведи результат.', ru:'Найди среди оценок все выше 85: оцінки.filter(о => о > 85) и выведи результат.' },
    ]
  );

  /* ─── 08-12 ─────────────────────────────────────────────── */
  patch('08-12',
    { uk:`<h2>Об'єкти: {} та властивості</h2>
<p><strong>Об'єкт</strong> — набір пов'язаних даних. Якщо масив — список однакових речей, то об'єкт — один предмет з різними характеристиками.</p>
<h3>Створення</h3>
<pre>const кіт = {
  імя:    'Мурчик',
  вік:    3,
  колір:  'рудий'
};</pre>
<h3>Доступ</h3>
<pre>кіт.імя     // 'Мурчик'
кіт['вік']  // 3
кіт.вага = 4.5 // додати поле</pre>
<h3>Методи</h3>
<pre>const кіт = {
  імя: 'Мурчик',
  мяу() {
    return \`\${this.імя}: Мяу!\`;
  }
};
кіт.мяу(); // 'Мурчик: Мяу!'</pre>`,
      ru:`<h2>Объекты: {} и свойства</h2>
<p><strong>Объект</strong> — набор связанных данных.</p>
<h3>Создание</h3>
<pre>const кот = {
  имя:   'Мурчик',
  возраст: 3,
  цвет:  'рыжий'
};</pre>
<h3>Доступ</h3>
<pre>кот.имя       // 'Мурчик'
кот['возраст'] // 3</pre>
<h3>Методы</h3>
<pre>const кот = {
  имя: 'Мурчик',
  мяукнуть() { return \`\${this.имя}: Мяу!\`; }
};</pre>` },
    BH, BC,
    SF +
`const учень = {
  імя:    'Аліна',
  вік:    10,
  клас:   '4-А',
  оцінки: [90, 85, 92, 78, 96],
  хобі:   ['читання','плавання','код'],
  середня() {
    const с = this.оцінки.reduce((a,b)=>a+b,0)/this.оцінки.length;
    return с.toFixed(1);
  },
  картка() {
    return \`\${this.імя}, \${this.вік} р., \${this.клас}, ср: \${this.середня()}\`;
  }
};

show('👤 ' + учень.картка(), 'gr');
show(\`📚 Хобі: \${учень.хобі.join(', ')}\`, 'bl');
show(\`🎯 Оцінок: \${учень.оцінки.length}\`, 'bl');

учень.місто = 'Київ';
show(\`🏙 Додали місто: \${учень.місто}\`,   'yw');

// Масив об'єктів:
show('--- Клас ---', 'gy');
const клас = [
  { імя:'Аліна',  бали:92 },
  { імя:'Максим', бали:85 },
  { імя:'Дарина', бали:97 },
];
for (const у of клас) {
  show(\`\${у.імя}: \${у.бали}\`, 'bl');
}`,
    [
      { level:'easy',   uk:'Заміни дані об\'єкту учень на свої реальні (імя, вік, клас).',       ru:'Замени данные объекта учень на свои реальные (імя, вік, клас).' },
      { level:'medium', uk:'Додай до учень метод вітати() що повертає `Привіт! Я ${this.імя} з ${this.клас}`.', ru:'Добавь к учень метод вітати() который возвращает `Привет! Я ${this.імя} из ${this.клас}`.' },
      { level:'hard',   uk:'Знайди учня з найвищим балом у масиві клас (for...of + if для порівняння). Виведи його імя.', ru:'Найди ученика с наибольшим баллом в массиве клас. Выведи его имя.' },
    ]
  );

  /* ─── 08-13 ─────────────────────────────────────────────── */
  patch('08-13',
    { uk:`<h2>Функції: function і return</h2>
<p><strong>Функція</strong> — багаторазовий блок коду з іменем. Написав один раз — викликаєш скільки треба.</p>
<h3>Оголошення і виклик</h3>
<pre>function привітати(імя) {
  return 'Привіт, ' + імя + '!';
}
const res = привітати('Аліна'); // викликаємо
console.log(res); // 'Привіт, Аліна!'</pre>
<h3>Параметри та аргументи</h3>
<ul>
  <li><strong>Параметри</strong> — імена у дужках при оголошенні.</li>
  <li><strong>Аргументи</strong> — реальні значення при виклику.</li>
</ul>
<h3>return</h3>
<p>Завершує функцію і повертає значення. Без <code>return</code> → <code>undefined</code>.</p>
<pre>function сума(a, b) { return a + b; }
const s = сума(3, 4); // 7</pre>`,
      ru:`<h2>Функции: function и return</h2>
<p><strong>Функция</strong> — многоразовый блок кода с именем.</p>
<h3>Объявление и вызов</h3>
<pre>function приветствовать(имя) {
  return 'Привет, ' + имя + '!';
}
const res = приветствовать('Алина');</pre>
<h3>return</h3>
<p>Завершает функцию и возвращает значение. Без <code>return</code> → <code>undefined</code>.</p>` },
    BH, BC,
    SF +
`function привітати(імя, вік) {
  return \`👋 Привіт, \${імя}! Тобі \${вік} р.\`;
}
show(привітати('Аліна', 10), 'gr');
show(привітати('Максим', 11), 'gr');

function степінь(осн, пок) { return osн ** пок; }
// Ой, опечатка вище. Виправляємо:
function degree(base, exp) { return base ** exp; }
show(\`2^10 = \${degree(2,10)}\`, 'bl');
show(\`3^4  = \${degree(3,4)}\`,  'bl');

// Функція без return — просто виконує дію:
function роздільник(довж, знак) {
  show(знак.repeat(довж), 'gy');
}
роздільник(24, '─');

// Функція → об'єкт:
function книга(назва, автор, рік) {
  return { назва, автор, рік };
}
const кн = книга('Гаррі Поттер', 'Ролінг', 1997);
show(\`📚 \${кн.назва} (\${кн.рік})\`, 'yw');
show(\`✍️ \${кн.автор}\`,              'yw');

// Практична функція:
function дні(роки) { return roky * 365; }
function days(years) { return years * 365; }
show(\`10 р = \${days(10)} днів\`, 'gr');`,
    [
      { level:'easy',   uk:'Виклич привітати() з власним іменем і справжнім віком.',            ru:'Вызови привітати() со своим именем и настоящим возрастом.' },
      { level:'medium', uk:'Напиши функцію максимум(а,б,в) яка повертає найбільше з трьох чисел (Math.max).', ru:'Напиши функцию максимум(а,б,в) которая возвращает наибольшее из трёх чисел.' },
      { level:'hard',   uk:'Напиши функцію паліндром(рядок) яка перевіряє чи слово читається однаково з обох кінців (порівняй рядок === рядок.split("").reverse().join("")).', ru:'Напиши функцию палиндром(строка) которая проверяет читается ли слово одинаково с обоих концов.' },
    ]
  );

  /* ─── 08-14 ─────────────────────────────────────────────── */
  patch('08-14',
    { uk:`<h2>Стрілкові функції: const fn = () => {}</h2>
<p><strong>Стрілкові функції</strong> — коротший запис. Особливо корисні всередині масивних методів.</p>
<h3>Синтаксис</h3>
<pre>// Звичайна:
function додати(a, b) { return a + b; }

// Стрілкова:
const додати = (a, b) => a + b;

// Один параметр — без дужок:
const квадрат = x => x * x;

// Без параметрів:
const привіт = () => 'Привіт!';</pre>
<h3>map, filter, find</h3>
<pre>const числа = [1,2,3,4,5];
числа.map(n => n*2)         // [2,4,6,8,10]
числа.filter(n => n%2===0)  // [2,4]
числа.find(n => n > 3)      // 4</pre>`,
      ru:`<h2>Стрелочные функции: const fn = () => {}</h2>
<p><strong>Стрелочные функции</strong> — краткая запись. Особенно полезны внутри методов массивов.</p>
<h3>Синтаксис</h3>
<pre>const добавить = (a, b) => a + b;
const квадрат  = x => x * x;
const привет   = () => 'Привет!';</pre>
<h3>map, filter, find</h3>
<pre>числа.map(n => n*2)
числа.filter(n => n%2===0)
числа.find(n => n > 3)</pre>` },
    BH, BC,
    SF +
`// Стрілкові функції:
const квадрат = x => x * x;
const куб     = x => x ** 3;
const вітання = імя => \`Привіт, \${імя}!\`;

show(вітання('Аліна'), 'gr');
show(\`5² = \${квадрат(5)}\`, 'bl');
show(\`3³ = \${куб(3)}\`,     'bl');

// .map():
const tempC = [0, 20, 37, 100];
const tempF = tempC.map(c => c * 9/5 + 32);
show('--- Celsius → Fahrenheit ---', 'gy');
tempC.forEach((c,i) => show(\`\${c}°C = \${tempF[i]}°F\`, 'yw'));

// .filter():
const числа = [3,7,12,5,18,1,9,22,4];
const великі = числа.filter(n => n > 10);
show(\`> 10: [\${великі.join(', ')}]\`, 'bl');

// .find():
const першеПарне = числа.find(n => n % 2 === 0);
show(\`Перше парне: \${першеПарне}\`, 'gr');

// .every() і .some():
show(\`Всі > 0: \${числа.every(n=>n>0)}\`,  'yw');
show(\`Є > 20:  \${числа.some(n=>n>20)}\`,  'yw');`,
    [
      { level:'easy',   uk:'Виклич вітання("Максим") і вітання зі своїм іменем.',               ru:'Вызови вітання("Максим") и вітання со своим именем.' },
      { level:'medium', uk:'Використай .map() щоб перетворити ["аліна","максим"] — перша літера велика: s => s[0].toUpperCase()+s.slice(1).', ru:'Используй .map() чтобы преобразовать ["алина","максим"] — первая буква заглавная.' },
      { level:'hard',   uk:'Напиши стрілкову функцію суматор = масив => масив.reduce((а,б)=>а+б,0) і обчисли суму [10,20,30,40,50].', ru:'Напиши стрелочную функцию суматор = масив => масив.reduce((а,б)=>а+б,0) и вычисли сумму [10,20,30,40,50].' },
    ]
  );

  /* ─── 08-15 (ПРОЕКТ) ───────────────────────────────────────*/
  patch('08-15',
    { uk:`<h2>ПРОЕКТ: Калькулятор та вікторина на JS</h2>
<p>Час застосувати все вивчене! Два міні-додатки в одному: інтерактивний <strong>калькулятор</strong> і <strong>вікторина</strong> з 5 питань.</p>
<h3>Що ти закріплюєш</h3>
<ul>
  <li>✅ Змінні, типи, шаблонні літерали</li>
  <li>✅ if/else, оператори, цикли</li>
  <li>✅ Функції та стрілкові функції</li>
  <li>✅ Масиви і об'єкти</li>
  <li>✅ Перший дотик до DOM (наступний модуль)</li>
</ul>
<p>Відкрий вкладки <strong>HTML</strong>, <strong>CSS</strong> та <strong>JS</strong> щоб побачити як вони пов'язані. Пройди вікторину і спробуй змінити питання!</p>`,
      ru:`<h2>ПРОЕКТ: Калькулятор и викторина на JS</h2>
<p>Время применить всё выученное! Два мини-приложения: интерактивный <strong>калькулятор</strong> и <strong>викторина</strong> с 5 вопросами.</p>
<h3>Что ты закрепляешь</h3>
<ul>
  <li>✅ Переменные, типы, шаблонные литералы</li>
  <li>✅ if/else, операторы, циклы</li>
  <li>✅ Функции и стрелочные функции</li>
  <li>✅ Массивы и объекты</li>
</ul>` },
    `<div class="app">
  <section class="card">
    <h2>🔢 Калькулятор</h2>
    <div class="inputs">
      <input type="number" id="a" value="10" placeholder="A">
      <span class="op" id="op-display">?</span>
      <input type="number" id="b" value="3" placeholder="B">
    </div>
    <div class="op-btns">
      <button onclick="calc('+')">+</button>
      <button onclick="calc('-')">−</button>
      <button onclick="calc('*')">×</button>
      <button onclick="calc('/')">÷</button>
      <button onclick="calc('%')">%</button>
      <button onclick="calc('**')">xⁿ</button>
    </div>
    <div class="result" id="result">← оберіть операцію</div>
  </section>

  <section class="card">
    <h2>🧠 Вікторина</h2>
    <div class="qbox">
      <div class="qnum"  id="qnum">1 / 5</div>
      <div class="qtext" id="qtext"></div>
      <div class="qopts" id="qopts"></div>
      <div class="qscore"id="qscore">Очок: 0</div>
    </div>
  </section>
</div>`,
    `*{box-sizing:border-box;margin:0;padding:0}
body{font-family:'Segoe UI',Arial,sans-serif;background:#0f172a;color:#f8fafc;padding:20px;display:flex;flex-direction:column;gap:20px}
.app{display:flex;flex-direction:column;gap:20px;max-width:460px}
.card{background:#1e293b;border-radius:14px;padding:20px}
h2{font-size:17px;margin-bottom:16px}

/* Калькулятор */
.inputs{display:flex;align-items:center;gap:10px;margin-bottom:12px}
.inputs input{flex:1;background:#0f172a;border:1px solid #334155;color:#f8fafc;padding:10px;border-radius:8px;font-size:16px;text-align:center}
.op{font-size:22px;color:#059669;font-weight:900;min-width:28px;text-align:center}
.op-btns{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:14px}
.op-btns button{background:#0f172a;border:1px solid #334155;color:#f8fafc;padding:11px;border-radius:8px;font-size:16px;cursor:pointer;transition:.2s}
.op-btns button:hover{border-color:#059669;color:#4ade80}
.result{background:#0f172a;border:2px solid #059669;border-radius:10px;padding:14px;text-align:center;font-size:20px;font-weight:700;color:#4ade80;min-height:52px}

/* Вікторина */
.qnum{font-size:12px;color:#94a3b8;margin-bottom:8px}
.qtext{font-size:16px;font-weight:600;margin-bottom:14px;line-height:1.4}
.qopts{display:flex;flex-direction:column;gap:8px;margin-bottom:14px}
.qopts button{background:#0f172a;border:1px solid #334155;color:#f8fafc;padding:10px 14px;border-radius:8px;text-align:left;cursor:pointer;font-size:14px;transition:.2s}
.qopts button:hover{border-color:#3b82f6;background:#0f1f35}
.qopts button.ok{border-color:#059669;background:rgba(5,150,105,.15);color:#4ade80}
.qopts button.no{border-color:#ef4444;background:rgba(239,68,68,.1);color:#f87171}
.qscore{font-size:13px;color:#94a3b8}`,
    `// ── КАЛЬКУЛЯТОР ──────────────────────────
function calc(op) {
  const a = parseFloat(document.getElementById('a').value);
  const b = parseFloat(document.getElementById('b').value);
  const symbols = { '+':'+', '-':'−', '*':'×', '/':'÷', '%':'%', '**':'ⁿ' };
  document.getElementById('op-display').textContent = symbols[op] || op;

  let res;
  if (op === '+')  res = a + b;
  if (op === '-')  res = a - b;
  if (op === '*')  res = a * b;
  if (op === '/')  res = b === 0 ? '❌ Ділення на 0!' : (a/b).toFixed(4);
  if (op === '%')  res = a % b;
  if (op === '**') res = a ** b;

  document.getElementById('result').textContent = \`\${a} \${symbols[op]} \${b} = \${res}\`;
}

// ── ВІКТОРИНА ────────────────────────────
const питання = [
  { п:'Скільки кольорів у веселці?',        в:['5','6','7','8'],              пр:2 },
  { п:'Яка столиця України?',                в:['Харків','Львів','Одеса','Київ'],пр:3 },
  { п:'Скільки байт в 1 кілобайті?',         в:['512','1000','1024','2048'],   пр:2 },
  { п:'2⁸ = ?',                              в:['128','256','512','64'],        пр:1 },
  { п:'Яка мова програмування в цьому уроці?',в:['Python','Java','C++','JavaScript'],пр:3 },
];

let idx   = 0;
let очки  = 0;

function показати() {
  const п = питання[idx];
  document.getElementById('qnum').textContent  = \`\${idx+1} / \${питання.length}\`;
  document.getElementById('qtext').textContent = п.п;
  document.getElementById('qscore').textContent= \`Очок: \${очки}\`;

  const opts = document.getElementById('qopts');
  opts.innerHTML = '';
  п.в.forEach((v, i) => {
    const btn = document.createElement('button');
    btn.textContent = v;
    btn.onclick = () => відповісти(i, btn);
    opts.appendChild(btn);
  });
}

function відповісти(i, btn) {
  const кнопки = document.querySelectorAll('.qopts button');
  кнопки.forEach(b => b.disabled = true);
  if (i === питання[idx].пр) {
    btn.classList.add('ok');
    очки++;
  } else {
    btn.classList.add('no');
    кнопки[питання[idx].пр].classList.add('ok');
  }
  setTimeout(() => {
    idx++;
    if (idx < питання.length) {
      показати();
    } else {
      document.getElementById('qopts').innerHTML = '';
      document.getElementById('qtext').textContent = '🏆 Вікторина закінчена!';
      document.getElementById('qnum').textContent  = \`Результат: \${очки}/\${питання.length}\`;
      const m = очки>=4?'⭐ Відмінно!':очки>=2?'👍 Непогано!':'📚 Вчись!';
      document.getElementById('qscore').textContent = m;
    }
  }, 900);
}

показати();`,
    [
      { level:'easy',   uk:'Пройди вікторину. Потім заміни одне питання у масиві питання на своє.',   ru:'Пройди викторину. Потом замени один вопрос в массиве питання на свой.' },
      { level:'medium', uk:'Додай кнопку "Зіграти знову" (button onclick="restart()") і функцію restart() що скидає idx=0, очки=0 і викликає показати().',  ru:'Добавь кнопку "Играть снова" и функцию restart() которая сбрасывает idx=0, очки=0 и вызывает показати().' },
      { level:'hard',   uk:'Додай до вікторини таймер: 10 секунд на питання (setInterval). При 0 секундах — автоматично перейти до наступного.',  ru:'Добавь к викторине таймер: 10 секунд на вопрос (setInterval). При 0 секундах — автоматически переходить.' },
      { level:'extra',  uk:'Додай анімацію правильної відповіді: кнопка.animate([{transform:"scale(1)"},{transform:"scale(1.1)"},{transform:"scale(1)"}],{duration:300}).', ru:'Добавь анимацию правильного ответа через кнопка.animate(...).' },
    ]
  );

})();
