import { styled } from "@mui/material";

export const WavesWrapper = styled('section')({
    paddingBottom: 0,
    display: 'flex',
});

export const WavesCanvas = styled('canvas')({
    background: 'linear-gradient(180deg, #231F3D03 20 %, #3d2769, #3d2769)',
});